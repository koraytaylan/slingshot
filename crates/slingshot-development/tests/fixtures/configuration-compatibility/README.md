# Configuration compatibility

Documents an existing installation may already have on disk. Nothing here is
generated: each one was written the way somebody would write it, and the point
is that a release which stopped accepting one would break that installation on
the morning after the upgrade.
