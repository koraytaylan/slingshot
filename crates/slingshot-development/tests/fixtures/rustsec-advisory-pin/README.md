# Advisory pin samples

Pin documents the schema must accept or refuse. Every refused sample carries a
value a Git author chooses freely, or an identifier that names something other
than one exact object, so none of them can authenticate a snapshot.
