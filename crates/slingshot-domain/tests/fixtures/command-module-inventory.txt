#
#
# Command leaf inventory.
# One row per source leaf below the command family root. This file is the sole
# The order is byte-lexicographic, so the inventory never depends on the order a
# and these rows are compared in both directions, so a leaf cannot appear in one
# authority for that list: the family root's declarations, the files on disk,
# directory was walked.
# without appearing in the other two.
add_component
artifact
authorizable_identity
canonical_json
catalog
command_identity
component_resource_type
create_page
delete_page
discovery_budget
download_content_package
find_assets_by_metadata
find_assets_referenced_by_page
find_pages_by_template
find_pages_containing_phrase
find_pages_using_components
inspect_open_service_gateway_initiative_configuration
load_content_as_javascript_object_notation
move_page
operational_listing
package_selection
platform_service_identity
process_identity
property_value
query_paths
replicate_content
repository_path
resource_mutation
result_window
schema
search_predicate
update_page
