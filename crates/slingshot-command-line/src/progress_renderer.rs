//! Showing that something is happening, without pretending to know more.
//!
//! The module map assigns this leaf the progress a caller sees while an
//! operation runs. It reports what the daemon reported and never estimates,
//! because an invented percentage is a claim about a remote system.
