//! Finding assets by metadata or by the page that references them.
//!
//! The module map assigns this leaf the argument surface of the asset-discovery
//! commands. They share their window and continuation handling with the page
//! queries and differ in what they search.
