//! Finding paths under a root by their properties.
//!
//! The module map assigns this leaf the argument surface of the path query:
//! the root to search under, the predicates every match must answer, and the
//! window of results the caller wants.
