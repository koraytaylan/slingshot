//! Creating a page and adding a component to one.
//!
//! The module map assigns this leaf the argument surface of the two commands
//! that change content. Both take a property document, and both are ordered
//! against a sibling when the caller says where.
