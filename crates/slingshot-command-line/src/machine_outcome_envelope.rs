//! The envelope every machine-readable answer is written in.
//!
//! The module map assigns this leaf the one outer shape a scripted caller
//! parses, whatever command produced it. A shape that varied by command would
//! make every consumer learn every command before it could read an error.
