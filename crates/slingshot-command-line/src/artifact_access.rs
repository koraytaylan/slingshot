//! Naming how a caller reaches a result that is not inline.
//!
//! The module map assigns this leaf the access reference a command prints when
//! a result lives beside the operation rather than inside the answer. It is a
//! reference rather than the bytes, because printing megabytes into a terminal
//! is not the same as making them available.
