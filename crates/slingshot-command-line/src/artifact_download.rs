//! Fetching an operation's artifact to a place the caller chose.
//!
//! The module map assigns this leaf the local transfer from the daemon to a
//! destination path. The daemon owns whether the artifact exists; this owns
//! where it lands and what happens if something is already there.
