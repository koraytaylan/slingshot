---
id: workflow-integration-reference
title: "Workflow Integration Reference"
workstream: "0034"
kind: chore
depends_on:
  - explicit-compensating-command
  - workflow-artifact-reference
  - finite-state-machine-retry-idempotency
  - daemon-restart-reattachment
gated: false
touches:
  - docs/WORKFLOWS.md
  - crates/slingshot-development/tests/fixtures/workflow-documentation-commands.txt
  - crates/slingshot-development/tests/workflow_documentation.rs
status: done
merged_as: "d429199f913c8edf832db8a7f0982f50207b89a8"
---
# Workflow Integration Reference

Document the proven executable boundary and keep every command fragment synchronized with the pinned compatibility and example files.

**Steps:**

1. Commit the expected documentation command and path inventory before writing docs/WORKFLOWS.md.
2. Document the pinned repository/commit, exact tracked-tree export and before/after digest observation, exact `slingshot.daemon-runtime-contract/1` and `slingshot.author-agent-transport-contract/1` formats/sidecar digests, repository/embedded/manifest/`Hello`/capability pre-effect equality and zero-effect drift refusal, explicit source and executable inputs, all seven manifest-owned Cargo-home seed-tree limits and deterministic first-fault order, source-pin-only build-evidence scope, absence of any hosted-provider/repository/workflow/runner or hermetic/reproducible-build claim, fsm.handlers/1 shape, exact initialize/initialized/direct-tools-call sequence, Model Context Protocol revision, fixed executable/tool boundary, and one-target-daemon topology.
3. Document stable `workflow_operation_namespace`, the exact 128-byte namespace/request limits and control exclusions, unsigned occurrence domain, fixed member order/string/integer byte encoding, SHA-256/prefix construction, 92-byte base/107-byte final key limits, closed empty/`-backup-restore` suffix inventory and compensation-only rule, same-store registry-command retry/restart stability, independent-store separation, the workflow-owned cross-command-occurrence uniqueness invariant, and why the two compatibility digests remain separate provenance and do not alter that preimage, including why no FSM internal store/effect identifier is available for substitution. State that this contract applies only to Plan 0007 registry command tools; maintenance preview/apply schemas reject operation keys and use exact target/reviewed-digest durable receipt replay, while maintenance-result resources remain target/association-qualified.
4. Document the validator's one bounded private source snapshot, exact `fsm --json --data-dir <fresh> execute --check --handlers <private-snapshot>` command, one-object-plus-line-feed standard-output contract, private data/home roots, named source/depth/count/runtime/output bounds and reap behavior, FSM-owned grammar/rooted-argv versus Slingshot-owned executable-file/access validation, real-check-output versus same-snapshot source split, the fields omitted by the pinned check output, conditional registry-command-key versus keyless-maintenance validation, and rejection of implicit retry or advance defaults. Document the exact `slingshot-development finite-state-machine-process-child --configuration-root <absolute-private-root> --hostile-account-home <absolute-sentinel-root> --role <command-line|model-context-protocol|daemon> -- <production-arguments>` boundary: every real direct/auto-started/replacement Slingshot role enters the unchanged production composition with Plan 0004's typed private root, the fake resolver's sentinel remains untouched, actual user directories are never touched, and the installed product rejects this subcommand and every root selector. Document success, both conditional recovery-evidence variants, and all conditional terminal/control/local failure-envelope mappings—including post-success `ResultUnavailable`—plus the pinned executor's generic `isError`/JSON-RPC-to-`mcp_error` mapping and static `on_failed` payload, FSM-owned handler timeout, `retry.on = ["timeout"]`, command retry attachment, one durable logical operation and at most one fenced command-effect attempt despite bounded no-op duplicate physical records, repeated same-command-handler occurrence behavior, transparent daemon reconnect, explicit compensation, operation-qualified package access, exact `structured_result_artifact_access` for an ordinary oversized command result whose artifact uses deterministic slot `structured_result`, and the disjoint operation-free `maintenance_result_access` outcome with keyless preview/apply, exact target/reviewed-digest receipt replay, and exact association fields/URI/target-and-identifier-only `MaintenanceResultMetadata` followed by expected-digest `MaintenanceResultRead`/replay/ownership/supersession/retirement semantics. State that lookup-to-read admits only byte-identical metadata or the checked current-preview-to-application-receipt owner transition at the next association revision before atomic read start. Preserve the strictly-below-4096 exact-envelope contract at `ack.result.structured` with absent `structured_sha256`.
5. State that `mcp_error` and `on_failed` prove neither authority nor domain safety. Document the compensation example's no-effect review state, undigested `ack.result.structured` verification of matching target-qualified registered `admission_rejected` evidence with a positive accepted prefix plus authoritative-remote-failure disposition, no-effect `compensation_decision_required` state, distinct `backup_restore_approved` domain-decision event, and rejection of both `RecoveryExecutionEvidence` branches, terminal `ResultUnavailable`/`AuthoritativeRemoteSuccess`, zero-accepted/inconsistent replication, authoritative-nonexecution, control/local/tool, fail-closed-indeterminate, malformed, timeout, spawn, protocol, missing/digested, or mismatched evidence.
6. Warn that arbitrary externally supplied FSM definitions and events remain outside Slingshot's enforcement boundary; only workflows that separate verified authority from an explicit domain approval obtain the documented compensation guarantee.
7. Record the synthetic over-cap prefix string at `ack.result.structured` plus `ack.result.structured_sha256` behavior as an executor compatibility sentinel, not a conforming Slingshot outcome.
8. Reference every example machine and handler template by repository-relative path and include no home directory, sibling checkout, credential value, TODO, or prospective statement.
9. Add a documentation test that extracts command blocks and references, validates them through the shared real FSM preflight plus Slingshot compatibility validator so every check receives a fresh explicit private data/home boundary, and verifies the pinned values match compatibility/finite-state-machine.toml. The test contains no aggregate-gate assertion or invocation.

**Tests:**

- Every documented repository path exists and every expected example is referenced.
- Extracted handler content passes real FSM preflight and Slingshot validation after executable-path expansion.
- Repository, commit, scoped exported-tree before/after digest observation, protocol revision, handler format, both exact upstream contract identities and pre-effect drift matrix, seven seed-tree limits/precedence, complete unchanged registry-command-only workflow preimage/key/suffix contract plus operation-key-free maintenance classification, exact JSON check command/frame, one bounded private source snapshot with fresh explicit FSM data/home roots, independent executable-file validation, typed private Slingshot test-root routing and hostile-root isolation, and the real-check-output versus same-snapshot field split equal their authoritative fixtures.
- A text-policy scan rejects developer-specific absolute paths, secret-shaped values, TODO markers, and past or future remarks.
- The failure and compensation sections never equate `isError`, `mcp_error`, or `on_failed` with authority and name the exact registered positive-prefix replication evidence, matching target-qualified daemon result, rejection of both authoritative-remote-success forms, no-effect verification state, separate domain approval, and enforcement boundary.
- The artifact example contains the operation-qualified descriptor/URI inside `ack.result.structured` and no inline package bytes/path, uses exact `structured_result_artifact_access` for the ordinary oversized-command artifact in deterministic slot `structured_result`, documents fieldless authoritative remote success for post-success recovery and terminal result unavailability, and distinguishes exact below-cap nested objects from the isolated over-cap sentinel. The maintenance example instead uses `maintenance_result_access` with the exact operation-free target-qualified identifier/kind/reviewed-digest/content-digest/length/media/revision/owner/URI contract, proves its handlers reject operation keys, derives apply replay from exact target/reviewed digest plus the durable receipt, and proves target-and-identifier-only `MaintenanceResultMetadata` precedes expected-digest `MaintenanceResultRead`; exact canonical resource reads accept only identical lookup/start facts or the sole checked owner/revision transfer and preserve replay/ownership-transfer/supersession/retirement behavior. It never uses `structured_result_artifact_access`, slot `structured_result`, or another operation/artifact identity for maintenance.
- Registry-command success, retry, restart, compensation, and artifact prose consistently distinguish one durable logical operation and at most one fenced command-effect attempt from a transport-contract-bounded physical Sling record/attempt set whose losing consumers no-op.

- **Done when:** `SLINGSHOT_FINITE_STATE_MACHINE_EXECUTABLE=<finite-state-machine-executable> SLINGSHOT_EXECUTABLE=<slingshot-executable> cargo test -p slingshot-development --test workflow_documentation` proves the present-state reference matches both exact upstream contract identities and drift refusal, the pinned sequence, private export, finite seed/registry-command-key manifests, scoped source-build observation, exact JSON check plus independent executable validation, typed private Slingshot test-root boundary with untouched hostile sentinel and no product override, bounded one-read check/source split with no implicit defaults, stable store namespace keys and logical-operation/fenced-command-effect safety despite bounded no-op physical duplicates for registry command effects, nested acknowledgement semantics, conditional recovery/result-unavailable evidence, registered positive-prefix replication verification with no effect, rejection of remote-success evidence by that verification boundary, separate compensation approval and residual boundary, transparent restart, exact-object/externalization bounds, exact `structured_result_artifact_access` with deterministic slot `structured_result`, and the disjoint keyless operation-free `maintenance_result_access` target/reviewed-digest receipt replay plus `MaintenanceResultMetadata`-then-read/lifecycle contract.
