---
id: pinned-finite-state-machine-compatibility-gate
title: "Pinned Finite State Machine Compatibility Gate"
workstream: "0034"
kind: task
depends_on:
  - explicit-compensating-command
  - workflow-artifact-reference
  - finite-state-machine-retry-idempotency
  - daemon-restart-reattachment
  - workflow-integration-reference
gated: false
touches:
  - compatibility/finite-state-machine-test-targets.toml
  - scripts/check_finite_state_machine_compatibility
  - crates/slingshot-development/tests/finite_state_machine_compatibility_gate_contract.rs
  - "crates/slingshot-development/tests/fixtures/finite-state-machine-compatibility-gate/**"
  - docs/WORKFLOWS.md
  - crates/slingshot-development/tests/fixtures/workflow-documentation-commands.txt
  - crates/slingshot-development/tests/workflow_documentation.rs
status: done
merged_as: "b3e6ccd0e29593a69d324895f08bf40e7f024458"
---
# Pinned Finite State Machine Compatibility Gate

Expose the complete real pinned-source compatibility suite through one provider-neutral repository-local command that a developer, release coordinator, or later owner-authorized hosted adapter invokes unchanged.

**Steps:**

1. Commit `compatibility/finite-state-machine-test-targets.toml` and a gate-contract fixture containing the explicit source input, pinned repository/origin/full commit, private tracked-tree export and before/after digest observation, exact daemon-runtime and author-transport formats/sidecar digests plus repository/embedded/manifest/`Hello`/capability drift matrix, closed observed build environment, locked temporary-root build commands, the exact seven manifest-owned Cargo-home seed limits and their precedence, exact scenario environment including fresh per-check FSM data/home roots plus the typed private Slingshot configuration-root bootstrap and hostile temporary production-root sentinel, registry-command-only key classification, keyless maintenance target/reviewed-digest receipt replay, target-and-identifier-only `MaintenanceResultMetadata` before expected-digest `MaintenanceResultRead`, exact `structured_result_artifact_access` versus `maintenance_result_access` outcomes with `structured_result` reserved for the operation artifact slot, all twelve ordered Plan 0008 target/owning-task pairs, and forbidden package-wide/skip/fallback/provider/provenance-overclaim patterns before implementing the entry point.
2. Add `scripts/check_finite_state_machine_compatibility --finite-state-machine-source <path> [--cargo-home-seed <path>]` as a thin repository-local coordinator. It requires exactly one explicit FSM source path, permits at most one explicit absolute Cargo-home seed accepted under `compatibility/finite-state-machine.toml`'s file/directory/component/path/depth/per-file/aggregate limits and deterministic first-fault order for frozen/offline use, and never discovers, fetches, repairs, or advances either input; it accepts no home-relative, sibling, installed, hosting-provider, workflow, or runner input. Without the optional seed it records network-capable dependency acquisition and retains only the source-compatibility claim.
3. Invoke the Plan 0008 development compatibility command to verify normalized origin, exact clean `HEAD` tree, no gitlinks/submodules, lockfile, and any explicit bounded Cargo-home seed; export the digest-verified tracked tree to a private temporary source root, apply read-only permissions only as defense in depth, build the real `fsm-cli` there with the task's closed observed environment and locked resolution into separate explicit temporary Cargo/target roots, recompute the export and seed digests, and leave caller inputs untouched. A supplied seed is copied into the private Cargo home and forces frozen/offline mode; absence records network mode. Carry forward the command's source-pin-only evidence label; this gate alone never assigns dependency-cache provenance, hostile-build isolation, complete build provenance, or reproducibility.
4. Build both the installed `slingshot` binary and the `slingshot-development finite-state-machine-process-child` entry from the current checkout and committed lockfile into one separate temporary target directory. Export only the verified source, FSM executable, and development process-child executable paths through `SLINGSHOT_FINITE_STATE_MACHINE_SOURCE`, `SLINGSHOT_FINITE_STATE_MACHINE_EXECUTABLE`, and `SLINGSHOT_EXECUTABLE`; the last name is explicitly the executable whose fixed development-only child arguments enter the production Slingshot compositions in these tests. Every target that performs handler preflight must call the shared validator's exact `fsm --json --data-dir <fresh> execute --check --handlers <private-snapshot>` path and prove its fixed tool classification: registry command handlers require the manifest key contract, whereas maintenance preview/apply handlers reject operation keys/identity and validate their exact target/criteria or target/reviewed-digest contracts. Supplementary maintenance-result resource reads remain operation-free and validate their target-qualified association by target-and-identifier-only `MaintenanceResultMetadata` followed by expected-digest `MaintenanceResultRead`, admitting only identical lookup/start facts or the sole checked current-preview-to-application-receipt owner/revision transition. Every scenario must pass the same explicit private/sentinel roots and correct role through that child entry to all direct, auto-started, and replacement command-line/Model Context Protocol/daemon roles. Contract fixtures invoke the installed binary separately and require it to reject the child subcommand and every configuration-root argument/environment selector. No target may use an ambient FSM data/home default or expose a production Slingshot root override.
5. Recompute both policy sidecars and validate their exact format/digest values against embedded typed contracts and the committed compatibility manifest before any target. Validate the manifest against Plan 0008 task frontmatter and the exact source targets, then invoke each entry exactly once as `cargo test -p slingshot-development --test <target>` with the exact environment. The inventory is exactly `finite_state_machine_compatibility`, `finite_state_machine_process_harness`, `finite_state_machine_handler_validation`, `finite_state_machine_handler`, `finite_state_machine_success`, `finite_state_machine_failure`, `finite_state_machine_compensation`, `finite_state_machine_artifact`, `finite_state_machine_retry_idempotency`, `finite_state_machine_daemon_restart`, `workflow_documentation`, and `finite_state_machine_compatibility_gate_contract`. Every registry-command-effect-bearing target must emit its pre-effect `Hello`/capability comparison, exact scenario provenance receipt, one-logical-operation/at-most-one-fenced-command-effect assertion, bounded physical-record set, and no-op losing-consumer evidence. Every maintenance-control case must instead prove operation-key refusal, byte-identical target/reviewed-digest apply retry, durable applied/replayed receipt and association equality, and no command logical operation or effect fence. A supplementary production-server maintenance-resource read may verify an access envelope only after the real FSM control path has produced it and cannot satisfy the preview/apply case by itself; it must obtain `MaintenanceResultMetadata` first, pass its content digest to `MaintenanceResultRead`, and accept only identical lookup/start facts or the sole checked owner/revision transfer. Treat a missing/duplicate/extra target, `--tests`, ignored marker, environment skip, missing or stale scenario receipt, contract drift, unavailable checkout, alternate executable, synthetic-client substitution for required FSM evidence, or early successful return as gate failure.
6. Implement a repository test that parses the script, manifest, task frontmatter, source inventory, and fixture; requires one exact per-target invocation in manifest order; pins registry-command-key versus keyless-maintenance classification plus the exact `structured_result_artifact_access`/slot-`structured_result`/`maintenance_result_access` vocabulary; and rejects mutable revisions, package-wide or narrower/ad-hoc selection, FSM source discovery or fetching, unverified executable paths, provider/workflow/runner coupling, and skip or fallback behavior.
7. Update the already proven present-state `docs/WORKFLOWS.md` reference and its command/test fixtures with the now-existing repository-local gate command, exact twelve-target inventory, explicit source and optional seed inputs, source-pin evidence boundary, and owner-gated hosted-adaptation boundary. The documentation test parses and byte-compares the gate script and manifest without invoking the gate, so the gate's `workflow_documentation` target cannot recurse into its own coordinator.

**Tests:**

- `finite_state_machine_compatibility_gate_contract` byte-compares the repository-local gate and manifest against authoritative fixtures and requires the exact task/target bijection plus one invocation per entry; adding a Plan 0008 target requires the manifest change, while every non-Plan 0008 development test is excluded.
- Negative command fixtures fail for an aliased outcome tag, any operation-artifact slot other than exact `structured_result` under `structured_result_artifact_access`, or any use of `structured_result` by another outcome; shortened commit; dirty-source bypass; missing locked build; missing explicit source or environment input; independently changed/missing/malformed upstream policy bytes, sidecar, embedded value, compatibility value, `Hello`, capability, or terminal-record provenance; multiple/discovered/malformed/changing Cargo-home seeds; every exact/next seed-tree limit and same-path precedence case; non-frozen/non-offline seeded build; ambient Cargo wrapper/configuration/flags; non-JSON or ambient FSM check state; missing registry-command key; surplus maintenance operation key/identity; changed maintenance target/reviewed digest or receipt/association; missing `MaintenanceResultMetadata`, caller/cache/hash-derived read digest, or an invalid lookup-to-start metadata/owner/revision transition; missing typed Slingshot test-root routing; access to the hostile temporary production-root sentinel; a production root override; test omission; ignored/environment-skipped test; installed/sibling executable fallback; FSM source fetch; provider-specific expression; workflow path; runner selector; provenance/reproducibility overclaim; or conditional skip.
- The gate executes the real process tests against the exact private export of the clean caller-supplied pinned tree and just-built development child entry into the unchanged production Slingshot compositions, and separately proves the just-built installed binary has no test-root surface; no unit, synthetic-client substitution, package-wide, skipped, or unrelated-plan result satisfies it. A post-acknowledgement production-server maintenance-resource read is supplementary evidence, performs target-and-identifier-only `MaintenanceResultMetadata` before expected-digest `MaintenanceResultRead` under the exact lookup/start transition rule, and never replaces the real FSM preview/apply control path.
- Source verification/build and every scenario use temporary paths, support paths containing spaces, leave the external checkout's verified Git state untouched, give each check a distinct empty explicit FSM data/private-home boundary, route every real Slingshot role through the typed private test root while leaving the hostile temporary production-root sentinel untouched, never access actual user directories, and make no claim about ignored caller files or hostile-tool isolation.
- `workflow_documentation` exists before this task, is invoked exactly once by the final gate, and its gate-parity assertions only parse the script and manifest. Removing or changing the documented gate command, ordered target inventory, source/seed boundary, or hosted-adaptation boundary fails without recursively starting the gate.

- **Done when:** `scripts/check_finite_state_machine_compatibility --finite-state-machine-source <verified-source> [--cargo-home-seed <explicit-bounded-seed>]` verifies and privately exports/builds the exact pinned Git tree under the closed observed environment, authenticates both exact upstream contracts and rejects every drift before an effect, enforces every manifest-owned seed-tree dimension and frozen/offline mode when the optional seed is supplied, verifies source and seed before and after, invokes each of the twelve manifest-owned Plan 0008 targets exactly once with exact JSON check framing plus typed private Slingshot configuration roots and hostile-root sentinels, proves logical-operation/fenced-command-effect safety for registry command targets despite bounded duplicate physical records, separately proves keyless maintenance target/reviewed-digest durable receipt/association replay plus target-and-identifier-only `MaintenanceResultMetadata` before expected-digest resource reads under the exact lookup/start transition rule and without operation identity, pins exact `structured_result_artifact_access` versus `maintenance_result_access` outcomes with `structured_result` only as the deterministic operation artifact slot, exposes no production override or package-wide/fallback path, leaves caller inputs untouched, keeps the present-state workflow reference byte-aligned with the nonrecursive final gate, contains no hosting-provider or complete-build-provenance/reproducibility claim, and all workspace gates succeed.
