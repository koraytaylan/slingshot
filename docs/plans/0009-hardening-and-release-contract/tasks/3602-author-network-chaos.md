---
id: author-network-chaos
title: "Author Network Chaos"
workstream: "0036"
kind: task
depends_on:
  - agent-stream-fuzzing
  - daemon-process-chaos
gated: false
touches:
  - crates/slingshot-test-support/src/fake_author/mod.rs
  - crates/slingshot-test-support/src/lib.rs
  - crates/slingshot-test-support/src/network_fault_script.rs
  - crates/slingshot-development/tests/author_network_chaos.rs
status: done
merged_as: "8ffaefbbf14a5eccb0e637f7c1b466b3583a7261"
---
# Author Network Chaos

Event and submission unit tests isolate failures; this task composes them into deterministic network scripts that exercise authentication refresh, heartbeat supervision, reconciliation, and artifact transfer through the real author connection.

**Steps:**

1. Commit the fault-script table first for exact/mutated `slingshot.author-agent-transport-contract/1` bytes and digest; the immutable provider-policy-verified platform server-authentication root snapshot/identity used only by Identity Management Services; the distinct effective platform-plus-selected-additional-author-CA snapshot/identity used by the author client; role swaps, either identity drift, live operating-system-root mutation, explicit-restart drift, and a hostile additional/private author-CA endpoint that must not intercept Identity Management Services; provider root records that are eligible, distrust/denied, externally policy/application/name constrained, purpose-ineligible, unevaluable, repeated-equivalent, or conflicting duplicates; and exact TLS 1.2/1.3 plus older-version canaries. Identity Management Services fixtures cover sampled UTC unavailable/below-zero/zero/253,402,300,799/next and checked maximum `exp = 253,402,329,599`; exact/next 25,868-byte three-field form body; HTTP/1.1-or-HTTP/2-only negotiation; no protocol migration, `Alt-Svc`, automatic decompression, non-POST, `Expect`, or upgrade; incremental exact/next 8,192-byte single-header, 64-field, 32,768-byte decoded-head and compression/header-list bounds; 65,536-byte body and 16,384-byte token bounds; bounded 100/101/103 followed by otherwise valid final responses; repeated informational responses; final `Trailer` declarations; absent, explicitly empty, nonempty, and over-bound trailer sections; conflicting or invalid repeated `Content-Length`; simultaneous `Transfer-Encoding` plus `Content-Length`; invalid transfer-coding chains; HTTP/2 connection-specific fields and pseudo-header ordering/duplication errors; and trailing bytes after the declared boundary. Separately, every author success/error route covers HTTP/1.1-or-HTTP/2-only negotiation; upgrade/`Alt-Svc`/migration/automatic-decompression refusal; exact/next `MAXIMUM_AUTHOR_RESPONSE_HEADER_BYTES = 8,192`, `MAXIMUM_AUTHOR_RESPONSE_HEADER_COUNT = 64`, and `MAXIMUM_AUTHOR_RESPONSE_HEAD_BYTES = 32,768` through HTTP/1.1 raw/decoded parsing and HTTP/2 encoded-block/decoded-header-list compression; first informational rejection; `Trailer` declaration and actual empty/nonempty trailer rejection; ambiguous framing, HTTP/2 protocol errors, and trailing bytes before any partial route/finite-body/result/artifact/cursor view; and submission checkpoints before/after the first POST byte, with every post-byte unvalidated case preserving `SubmissionUnknown` and entering same-identifier lookup-first reconciliation. The remaining scripts cover before/equality/after DNS-plus-TCP connect and separate TLS-handshake deadlines; request-start-relative retention with delayed complete-body receipt and equality expiry; request and event byte cuts, unauthorized responses, server failures, heartbeat silence, reconnect jitter, replay/conflict, untracked events, rolling-ledger compaction, cursor expiry, generation change, snapshot-ahead events, snapshot gaps, immediate terminal completion, authoritative retired reservation, proven-success result/artifact acquisition, persistent local capacity refusal, snapshot/result retention expiry, invalid required artifact content, redirect, artifact interruption; all three exact continuation deployment profiles through the same universal cluster-capable durable linearizable authority; authority initialization/read/CAS/fence/lease ambiguity; shared rotation, restart, node replacement, scale-out, rolling-version compatibility and fail-closed readiness; and logical-reservation/outbox/JobManager/worker-fence crash cuts with zero, one, and the manifest-bounded maximum physical Sling Job matches.
2. Compose the existing fake identity service and extend the fake author with byte-boundary release controls. The identity service records exact method/version, ordered form body, absence of migration/`Alt-Svc`/automatic decompression/`Expect`/upgrade, incremental header/count/head/compression/body/token charges, first-informational termination, trailer declaration/section observation, framing-failure classification, whether any partial status/body/token view escaped, the exact platform-only trust identity presented to its connector, and whether any token installed without retaining credential bytes. A separately selected additional/private author-CA trap fails on any successful Identity Management Services handshake or credential-bearing request. The author records its distinct effective author trust identity, exact transport-manifest/digest, negotiated version and migration/decompression policy, incremental HTTP/1.1 and HTTP/2 encoded/decoded head charges, informational/final/trailer/framing observations, partial-route/body/result/artifact/cursor exposure, first-POST-byte checkpoint and resulting certainty/reconciliation path, five-field command identity, separate canonical-contract artifact/digest/dual-annotation capability, and submitted/result provenance; raw-canonical-before-schema-before-typed observations; exact twelve-row safety classifications; canonical SearchPhrase, asset-set, `AssetByteLength`, and opaque-token observations; the complete closed failure inventory; durable logical-operation/outbox/sorted-physical-attempt/worker-fence, snapshot/cursor/continuation-authority state that survives process and node replacement; explicit generation-loss mode; and a connector dial audit that fails on every destination other than the selected author. Plan 0002 owns Identity Management Services assertion/exchange policy, Plan 0005 owns author HTTP/job transport plus HMAC/key material and token parsing, and Plan 0009 supplies injected clocks, byte checkpoints, crash points, and observations.
3. Drive the assertion builder with one injected UTC sample and every transport timer with virtual monotonic time and an injected deterministic random source, including Identity Management Services overall/connect/TLS/write/head/body-idle/body-total/trailer-end, author DNS-plus-TCP connect, author TLS handshake, request/header/body phases, request-start retention subtraction, token refresh, heartbeat expiry, reconnect backoff/jitter, live retry scheduling, recovery-window expiry, and retry reset. No phase can reset or borrow another phase's budget.
4. Run each script from a fresh daemon, a restarted daemon with the same durable operation, a restarted fake author, and every compatible deployment profile under one, multiple, replaced, and rolling-version node arrangements. All three profiles validate current/previous tokens on every ready node through the same authenticated cluster-capable authority contract, competing initialization, CAS/fence/rotation, scale-out, restart, and replacement, and refuse all issuance/admission when authority identity, durability, linearizability, authorization, fencing, or compatibility is absent. Current node count never permits a private/node-local authority. Repeat recovery with exact and drifted `SelectedEnvironmentRevision` inputs; every drift refuses before readiness/provider/network/mutation, while exact equality can recover.
5. Record outbound origin, request identifier, authorization classification, agent operation and subscription identifiers, stream generation/cursor, per-job sequence, snapshot watermark/count, retry fact, and artifact publication without recording credential bytes.

**Tests:**

- Every recoverable cut and durable fake-author restart converges to the terminal snapshot without a duplicate job or daemon restart.
- Immediate terminal completion before stream attachment is found by snapshot reconciliation.
- Cursor expiry performs the high-water/snapshot/reset transition; snapshot-ahead buffered events fold as stale or matching duplicates, and untracked events cannot wedge the subscription.
- An authoritative Retired reservation or generation loss that cannot be recovered through the persisted Sling Job identifier produces the explicit fail-closed outcome and never creates a replacement job; ordinary snapshot/result expiry never permits resubmission.
- Exhausting a transient ambiguous-submission, known-job, or event retry budget yields durable `RecoveryRequired` with `ExecutionCertainty` rather than an authoritative terminal failure. After proven remote success, result/artifact transfer or persistent-capacity pause yields `RecoveryRequired` with fieldless `AuthoritativeRemoteSuccess`; resumption can use only persisted identifiers and cannot resubmit the job.
- Irrecoverable retention loss or failed validation of a required result/artifact after proven remote success yields only terminal `ResultUnavailable` with fieldless `AuthoritativeRemoteSuccess`; it never becomes authoritative remote failure, fail-closed execution uncertainty, or permission to create a replacement job.
- One unauthorized Cloud response refreshes once; repeated unauthorized or Basic unauthorized responses terminate without loops.
- Heartbeat loss changes connection health but never directly fails an operation.
- Redirects and every non-author dial attempt are refused; partial artifacts remain unpublished.
- A capability or result with a missing/stale/different wire name, `1.0.0`, limits digest, canonical-JSON-contract digest, argument-schema digest, result-schema digest, or submitted-command digest is rejected before external execution, persistence, retry, or presentation; each digest role drifts independently with the others fixed, and the fake-author job count remains zero for pre-execution provenance refusal.
- Any persisted/current SelectedEnvironmentRevision mismatch—including canonical-metascope, platform-only Identity Management Services trust-identity drift, or effective-author trust-identity drift—preserves durable bytes and produces zero readiness, provider, connector, recovery submission, or fake-author job observations. One committed snapshot supplies immutable provider-policy-verified platform roots/identity only to Identity Management Services and distinct immutable platform-plus-selected-additional-author-CA roots/identity to author. A role swap, additional author root admitted to Identity Management Services, provider record lacking unconditional server-authentication proof, lossy DER-only reduction, dynamic live reload, old TLS version, or explicit-restart identity drift reaches neither endpoint; the hostile additional/private author-CA trap observes no successful Identity Management Services handshake or credential-bearing request.
- The assertion clock is sampled once; only whole UTC seconds `0..=253,402,300,799` are accepted, and the maximum plus 28,800 produces exactly `exp = 253,402,329,599`. Identity Management Services sees exactly one `POST` with ordered `client_id|client_secret|jwt_token`, only HTTP/1.1 or HTTP/2, no migration/`Alt-Svc`/automatic decompression/`Expect`/upgrade, and no body above 25,868 bytes. Incremental exact/next tests enforce 8,192 bytes per header, 64 fields, 32,768 decoded-head bytes, compression/header-list bounds, 65,536 body bytes, and 16,384 token bytes. The first 100/101/103 rejects and closes before any later head/body; a final `Trailer` declaration or any actual trailer section, including empty, rejects before token installation. Conflicting or invalid repeated `Content-Length`, `Transfer-Encoding` plus `Content-Length`, an invalid transfer-coding chain, HTTP/2 connection-specific or pseudo-header error, and trailing bytes each produce transport failure before any partial status/body/token view.
- Every author success/error route negotiates only HTTP/1.1 or HTTP/2 and refuses upgrade, `Alt-Svc`, migration, and automatic decompression. Exact/next cases enforce 8,192 bytes for one decoded name plus value, 64 decoded fields, and 32,768 bytes independently for the raw HTTP/1.1 head, encoded HTTP/2 block, and checked decoded status/field/separator aggregate before collection. The first bounded informational head, more or less than one final head, a `Trailer` declaration, any actual empty/nonempty trailer, ambiguous HTTP/1.1 framing, invalid HTTP/2 semantics, an over-bound encoded/decoded head, or a trailing byte exposes no partial route outcome or finite result. Finite/submission results wait for exact trailer-free end; artifact declarations fail before body exposure and actual trailers before publication; event-stream declarations fail before exposure and actual trailers reconnect without a new cursor or retraction. After any POST byte, each unvalidated outcome remains `SubmissionUnknown` and performs same-identifier lookup-first reconciliation; only proven zero-byte transport failure or a validated nonexecution rejection establishes `ConfirmedNotExecuted`.
- DNS resolution plus TCP connection and authenticated TLS handshake select distinct named before/equality/after outcomes under `AUTHOR_CONNECT_TIMEOUT_MILLISECONDS` and `AUTHOR_TLS_TIMEOUT_MILLISECONDS`; neither consumes or resets the other. For every relative operation-detail/snapshot/result/artifact retention response, complete-body delay is subtracted from the monotonic request-start anchor, equality persists zero, and no transfer can extend recovery.
- Continuation corruption follows the exact malformed, integrity-invalid, wrong-target, wrong-query, then expired precedence, with constant-time comparison of all 32 tag bytes. The independently reconstructed exact 431-byte initialized ring, monotonic key-identifier exhaustion, unsafe-permission/missing/unreadable/corrupt readiness refusal, too-early and equality rotation boundaries, exact 960,000-millisecond previous-key retention, durable old-or-new replacement cuts, and restart-stable prior-token validation all execute under injected time; no failure regenerates or creates a replacement interpretation.
- The exact single-node AEM 6.5, clustered AEM 6.5, and Cloud Service profiles all prove the identical cluster-capable durable linearizable authority contract and shared read/CAS/fence/lease/rotation across ready nodes. Node-count observation, replacement, scale-out, and compatible rolling versions never weaken the contract; authority loss, stale fence, incompatible node, private/node-local adapter, or wrong adapter fails readiness and creates no job.
- Ambiguous JobManager outcomes may yield multiple bounded physical records, but their exact six properties reconcile to one logical outbox and only one fence reaches the command-effect no-return checkpoint; lease loss, requeue, restart, or replacement never causes a second effect attempt.
- For identical authenticated fake-agent command results, CLI and both Model Context Protocol revisions preserve the same closed semantic object and choose the same maximum-inline or `structured_result_artifact_access` outcome for the operation `structured_result` slot; network retry never changes canonical phrase/asset spelling, token bytes, failure fields, or result digest.

- **Done when:** `cargo test -p slingshot-development --test author_network_chaos` passes every committed script under injected UTC/virtual monotonic time with exact transport-contract authentication, route-distinct immutable `VerifiedIdentityManagementTrustPolicyIdentity` and `VerifiedAuthorTrustPolicyIdentity` from one snapshot, hostile additional/private author-CA interception refusal for Identity Management Services, exact Identity Management Services version/request/head/compression/body/token/informational/trailer/framing policy with no partial token view, separate exact author HTTP/1.1-or-HTTP/2 head/compression/informational/trailer/framing policy with post-byte `SubmissionUnknown` lookup-first reconciliation, request-start retention, distinct DNS/TCP and TLS phases, universal 431-byte all-profile authority/readiness/replacement/scale-out/rolling proof, one logical operation plus bounded duplicate physical records and at most one fenced effect attempt, pre-execution provenance refusal, terminal reconciliation where available, truthful recovery/result-unavailable evidence, author-only traffic, bounded retries, and no partial artifact publication, and `scripts/quality` succeeds.
