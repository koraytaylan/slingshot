---
id: release-acceptance-matrix
title: "Release Acceptance Matrix"
workstream: "0039"
kind: task
depends_on:
  - pinned-coverage-fuzzing-tool
  - daemon-process-chaos
  - storage-fault-chaos
  - local-endpoint-threat-suite
  - owner-confirmed-github-automation
  - owner-confirmed-native-evidence-trust
  - owner-supplied-release-metadata
  - release-input-cache
  - release-artifact-contract
  - present-state-product-documentation
gated: false
touches:
  - support/release-acceptance-container.toml
  - schemas/release/release-acceptance-manifest.schema.json
  - scripts/release_acceptance
  - .github/workflows/release.yml
  - crates/slingshot-development/src/lib.rs
  - crates/slingshot-development/src/main.rs
  - crates/slingshot-development/src/release_acceptance.rs
  - crates/slingshot-development/tests/release_acceptance.rs
  - "crates/slingshot-development/tests/fixtures/release-acceptance/**"
status: planned
merged_as: ""
---
# Release Acceptance Matrix

One command must assemble the exact evidence required to call a source revision releasable from a clean Slingshot checkout plus explicitly supplied clean FSM and RustSec checkouts, the earlier task's verified coordinator cache member, and a complete authenticated native-evidence directory. It runs every gate descendant inside one owner-authorized rootless OCI container with no non-loopback network or host socket/privilege rather than treating Cargo offline mode as process isolation or preparing missing inputs itself.

**Steps:**

1. Commit the acceptance schema plus bounded missing, duplicate, reordered, failed, wrong-revision, dirty-checkout, gitlink, incomplete/extra/oversized/source-substituted coordinator-cache, missing/wrong/changed/over-bound Plan 0008 seed projection and manifest/source/tree/lock binding, stale/role-swapped five-field command identity or separate canonical-contract provenance, attempted sixth identity member, independent canonical-contract/limits/argument-schema/result-schema drift, reversed canonical/schema/typed-validation order, incomplete parity report, wrong OCI image/runtime/coordinator row, unsigned/wrong-subject/missing/duplicate/wrong-row platform evidence, and Plan 0002 fake/current-native substitution. Add exact native-evidence rejection fixtures for a missing/reordered/third per-file or whole-generation attempt; missing first-attempt recovery, second-attempt success, or exhaustion outcome; a source above 1,048,576 bytes or aggregate above 16,777,216 bytes; role-untagged/parser-dependent inventory, same-role non-deduplication, cross-role or fixed snapshot/selection/profile collision acceptance, selected JSON/PEM parsing before equal S2, stable-file equality/in-place-mutation/atomic-replacement mismatch, incomplete/mixed generation, and same-account-boundary widening. Add missing/source-bearing/misordered 31/32/33 diagnostics; `SensitiveConfigurationDocument`/`SecretValue` lifetime substitution; invalid UTC/maximum-exp/25,868-byte request cases; a protocol other than HTTP/1.1 or HTTP/2; protocol migration, `Alt-Svc`, automatic decompression, `Expect`, or upgrade; missing/drifted incremental 8,192-byte single-header, 64-field, 32,768-byte decoded-head, compression/header-list, 65,536-byte body, or 16,384-byte token exact-next evidence; first-1xx continuation; `Trailer` declaration/section acceptance; conflicting or invalid repeated `Content-Length`; simultaneous `Transfer-Encoding` plus `Content-Length`; invalid transfer-coding chain; HTTP/2 connection-specific or pseudo-header ordering/duplication error; and trailing-byte partial-token evidence. Add separate Plan 0005 author-response rejection fixtures on every success/error route for another HTTP version, upgrade/`Alt-Svc`/migration/automatic-decompression acceptance; missing/drifted exact/next 8,192-byte decoded-name-plus-value, 64-decoded-field, or independent 32,768-byte raw-HTTP/1.1-head, encoded-HTTP/2-block, or decoded-status/field/separator-aggregate evidence; first-informational or zero/multiple-final acceptance; declared or actual empty/nonempty trailer acceptance; ambiguous framing, invalid HTTP/2 semantics, encoded/decoded excess, or trailing-byte exposure; finite/submission completion before exact trailer-free end; artifact exposure before declaration refusal or publication before actual-trailer refusal; event-stream exposure after a declaration or cursor advancement/retraction after an actual trailer; and any post-POST-byte unvalidated outcome not preserved as `SubmissionUnknown` for same-identifier lookup-first reconciliation. Trust rejection fixtures distinguish a missing/field-incomplete real safe provider API or actual-enumeration report from a missing deterministic provider-shaped permit/distrust/purpose/external-constraint/unevaluable/equal-duplicate/conflicting-same-DER case report; also reject certificate-only reduction, trust-store mutation, or a claim that every negative case preexists in the live store. Add host-socket/capability/device/privileged mount, non-loopback network-attempt, input-write, and success fixtures before the runner; missing/mutated/role-swapped runtime/transport manifest, sidecar, embedded, persisted, capability, and native-report digests; one-at-a-time runtime-only and transport-only CLI/current-MCP/legacy-MCP drift; private/node-local or wrong-size continuation state, incomplete universal-authority/replacement/scale-out/rolling evidence; different or dynamically reloaded Identity Management Services/author roots; combined/borrowed DNS/TCP/TLS budgets; response-receipt retention anchoring or delayed/equality extension; wrong SQLite source/build/configuration, spill-capable statement journals/temp store, incomplete/prohibited SQL inventory, permissive/wrong VFS/object whitelist, ambient-temp escape, and wrong WAL/disk/replacement/reserve arithmetic; logical-outbox or physical-record over-count and second-effect evidence; resumed-operation-artifact prefix/suffix mutation omission; pre/at/post-publication and incomplete-render receipt race mismatch; current-nonce/retained-handle cleanup, stale-nonce/replacement/PID-reuse failures; and a maintenance result that is truncated, summarized, digest-divergent, operation/artifact-shaped, incorrectly derived, metadata/owner/URI/read mismatched, or retained/retired contrary to the apply-transfer/replay/supersession/completed-receipt-retirement lifecycle. Cache schema/preparation/verifier and exhaustive seed-boundary fixtures remain solely owned by `release-input-cache`.

   Maintenance rejection fixtures separately cover missing or mutated target-plus-identifier-only `MaintenanceResultMetadata`; a request with digest/operation/artifact/slot/offset/path; a response that differs from the exact target/identifier/kind/reviewed-source/content/length/media/revision/live-owner field set; missing, malformed, superseded, retired, target-mismatched, derivation-invalid, owner-invalid, and blob-mismatched closed refusals with partial metadata; hash inversion; a read begun after refusal; an owner/revision transition other than the sole current-preview-to-application-receipt transition; and any operation, artifact-slot, workflow-effect-operation-key, or partial-content substitution.

   Workflow rejection fixtures put `operation_key`, `workflow_effect_operation_key`, occurrence, operation identity, and artifact identity into both fixed maintenance controls; omit exact preview target/criteria or apply target/reviewed digest; mutate either apply identity on retry; or allocate a command logical operation/effect fence for maintenance. Registry command-effect fixtures separately require the exact derived key and projection.
2. Define closed `support/release-acceptance-container.toml` by copying the owner-declared coordinator row from the validated GitHub authority and naming the exact rootless daemonless OCI runtime/version/install digest, immutable image reference/digest and local OCI-layout member, fixed unprivileged user, read-only-root setting, capability drop, no-new-privileges/security profile, environment allowlist, read-only input mount table, one new writable output root plus bounded temporary filesystems, and absence of host engine sockets, devices, host namespaces, and privileged mode. A different coordinator row, runtime, image, or weakened isolation flag re-enters the owner gate.
3. Require `--finite-state-machine-source <path>`, `--rustsec-advisory-database <path>`, `--rustsec-owner-review-record <path>`, `--locked-source-cache <coordinator-member>`, `--platform-evidence-directory <path>`, and a new output directory. Mount all inputs read-only in the exact rootless network-none container with only bounded writable roots; every substantive descendant remains inside it.
4. Inside isolation, verify exact source/external trees, owner authority/trust, coordinator cache, and every authenticated owner-mapped native row. Require all caches/manifests/attestations to bind one protected same-run RustSec review record for the exact source and pin; verify provider workflow/run/attempt identity and trusted attestation integrated-time evidence, never Git/self-authored timestamps. Require authenticated all-row configuration-root/credential/additional-trust-file evidence owned by Plan 0009. Each row must bind Plan 0002's exactly two named per-file attempts, each with one open, two same-handle bounded reads, and pre/middle/post metadata, and exactly two named whole-generation S1/inventory/S2 attempts. Require the 1,048,576-byte generic-source and 16,777,216-byte aggregate limits, parser-independent profile/selection inspection yielding the exact role-tagged source inventory, same-role deduplication, cross-role and fixed snapshot/selection/profile collision refusal, equal S2 before any selected service-credential JSON or additional-certificate-authority PEM parser, stable equality, in-place mutation, atomic replacement, named first-attempt recovery/second-attempt success/exhaustion, complete/mixed generation outcomes, and the explicit same-account-writer boundary. Reject Plan 0001 or Plan 0002 policy-fake/current-native/aggregate substitutes and never count Plan 0002's explicitly untrusted report as a row. Each row must also bind the public-diagnostic 31/32/no-marker and 33/first-31-plus-marker boundary without source references or ordering oracles, temporary `SensitiveConfigurationDocument` disposal, and long-lived extracted-secret-only `SecretValue` ownership. Require the Windows row's authenticated `PIPE_REJECT_REMOTE_CLIENTS` local-success/remote-refusal result. Require every row's real safe provider API and actual enumerated-record/effective-field report. Separately require the same compiled decision adapter's deterministic provider-shaped unconditional-permit, distrust/deny, purpose-restriction, external application/policy/name constraint, unevaluable, equal-duplicate, and conflicting-same-DER cases. Certificate-only enumeration, a missing real field or deterministic case, lossy DER reduction, operating-system trust-store mutation, or an assumption that negative records preexist invalidates the row. Each row must also authenticate Plan 0004's exact SQLite source/build and initialization order, `SQLITE_CONFIG_STMTJRNL_SPILL = -1`, compile option `TEMP_STORE=3`, runtime memory temp store, closed bounded parameterized SQL/query-plan inventory, restrictive VFS and exact main/`-wal`/`-shm` plus replacement-main whitelist, ambient-temp traps, and refusal of every prohibited statement/open. Independently recompute maximum-transaction WAL growth 16,875,520, WAL 67,502,112, high-water 50,626,592, active aggregate 1,141,506,080, and replacement aggregate/equal reserve 2,215,247,904 bytes from the manifest primitives; a missing operand, copied production result, or one-unit mismatch fails. Each row must authenticate all three exact Plan 0005 profiles through the identical cluster-capable durable linearizable authority, independently reconstruct the 431-byte initialized ring, and reject private/node-local state or a node-count relaxation. From one committed snapshot it must authenticate immutable route-typed `VerifiedIdentityManagementTrustPolicyIdentity` over provider-policy-verified platform roots only and distinct `VerifiedAuthorTrustPolicyIdentity` over platform-plus-selected-additional author roots, bind both raw identities into `SelectedEnvironmentRevision`, reject role swap or dynamic reload, and prove a selected hostile additional/private author CA cannot complete an Identity Management Services handshake or receive credentials. It must also distinguish DNS-plus-TCP connection from the subsequent authenticated TLS-handshake phase without budget borrowing and subtract delayed complete-body time from the monotonic request-start retention anchor with equality at zero. Authenticate one sampled UTC whole-second value in `0..=253,402,300,799`, maximum `exp = 253,402,329,599`, the exact 25,868-byte ordered three-field Identity Management Services `POST`, and HTTP/1.1-or-HTTP/2-only negotiation. Refuse protocol migration, `Alt-Svc`, automatic decompression, `Expect`, and upgrade. Enforce incremental 8,192-byte single-header, 64-field, 32,768-byte decoded-head, compression/header-list, 65,536-byte body, and 16,384-byte token exact-next bounds. Reject the first bounded 100/101/103 before any later head/body; reject `Trailer` declaration plus actual empty/nonempty trailer sections before token installation; and reject conflicting or invalid repeated `Content-Length`, simultaneous `Transfer-Encoding` plus `Content-Length`, invalid transfer-coding chains, HTTP/2 connection-specific fields or pseudo-header ordering/duplication errors, and bytes after the declared message boundary without a partial status/body/token view. Verify the coordinator member's Plan 0008 compatibility-manifest/source/tree/lock-bound seed record, then bounded-copy only that recorded projection into a new owner-only private seed root. Through Plan 0008's verifier, recheck every manifest-owned seed-tree dimension, first-fault semantics, canonical tree digest, counts, and aggregate before starting Cargo. With empty ambient Cargo/Rust homes, non-loopback network denied, and Plan 0008's exact sanitized private build/check environment and fresh internal roots, invoke exactly `scripts/check_finite_state_machine_compatibility --finite-state-machine-source <verified-path> --cargo-home-seed <verified-private-seed>`; recompute the private seed digest after completion and reject source-mode substitution, repair, supplementation, ambient lookup, network access, or mutation. Then run the remaining frozen/offline host-independent gates, including one exhaustive registry-driven command-contract report that authenticates every exact five-field wire-name/`1.0.0`/limits/argument-schema/result-schema identity plus separately authenticated canonical-contract artifact/digest/dual annotations before execution; the canonical digest never becomes a sixth identity field. It must reject independent drift in the canonical contract, limits, argument schema, or result schema with the other roles fixed; byte-match all twelve access/destructive/idempotency rows and their Model Context Protocol annotations; validate raw bytes and typed array order under `slingshot.command-canonical-json/1` before ordinary Draft 2020-12 decoded shape and typed conversion; and accept/preserve `1.0.0+01` while rejecting `1.0.0-01` under the exact SemVer grammar. The report also covers canonical phrase/asset/token inputs, including exact `AssetByteLength` syntax/domain/range-order boundaries; continuation tamper/precedence plus the exact 431-byte initialized ring, monotonic identifier exhaustion, unsafe-state readiness refusal, old-or-new crash replacement, restart stability, too-early/equality rotation, exact prior-key retention, and constant-time 32-byte tag comparison; side-effect-free escaped Open Service Gateway Initiative lookup with exactly one `getProperties()` acquisition and one complete keys-only enumeration before value classification; zero value reads for rejected/redacted keys and exactly one for each visible key; exactly one acquired metatype provider and one exact non-uninstalled bundle-location match; ordinary/factory persistent-identifier opposite-inventory exclusion; literal-null-locale object-class lookup without fallback/raw Designate/guess/cross-bundle behavior; exact folded attribute match and type mapping; Unavailable-before-value for every absence, duplicate, ambiguity, failure, disappearance, `BIGINTEGER`, `BIGDECIMAL`, or unknown type; FileVault quoting/profile/budgets/no widening; non-orderable parents; and every exact closed failure, maximum-inline/first-externalized fit, and CLI/Model Context Protocol/fake-agent parity. Selected-environment gates independently calculate the profile-contract/principal/target/`VerifiedIdentityManagementTrustPolicyIdentity`/`VerifiedAuthorTrustPolicyIdentity`/revision preimages and digests, prove no target rehash, prove exact canonical metascope and route-specific trust equality under reordering, distinguish genuine same-principal secret/key stability from Basic username or Cloud organization/client/technical-account target drift, exercise exact compact JSON Web Signature and decoded-response-head bytes, and require nonterminal refusal before provider construction/access or network execution on any revision drift. Independently drift daemon-runtime only and author-agent-transport only through the CLI and both Model Context Protocol revisions with every other provenance byte fixed; require pre-dispatch refusal. Require publication-success versus incomplete-render receipt races to re-render truthfully without resume, remote access, republish, or collision; cleanup to use only current nonce or the retained instance handle with diagnostic-only process identifiers under stale-nonce/replacement/PID-reuse cases. For a complete over-inline maintenance preview or applied/replayed receipt, rederive `MaintenanceResultIdentifier`, verify the exact association metadata/owner and 257 bound, compare the CLI and both Model Context Protocol `maintenance_result_access` values and canonical URI, consume `MaintenanceResultRead` with same-handle prefix/full-stream validation, and prove apply transfer/exact replay/supersession/completed-receipt retirement without an operation or artifact slot. Finally require the authenticated selected-row FSM report and this independent coordinator rerun to agree on exact source, compatibility manifest, target inventory, and result while retaining their distinct cache/row/environment evidence.

   The authenticated native row separately exercises every Plan 0005 success and error route over HTTP/1.1 and HTTP/2 with upgrade, `Alt-Svc`, migration, and automatic decompression disabled. It enforces exact/next 8,192-byte decoded-name-plus-value, 64-decoded-field, and independent 32,768-byte raw-HTTP/1.1-head, encoded-HTTP/2-block, and decoded-status/field/separator-aggregate limits; rejects the first informational head, anything other than exactly one final head, a `Trailer` declaration, actual empty/nonempty trailers, ambiguous framing, invalid HTTP/2 semantics, encoded/decoded excess, and trailing bytes before a partial route outcome; and proves finite/submission exact trailer-free end, artifact prebody/prepublication refusal, event-stream pre-exposure/reconnect-without-cursor-or-retraction behavior, and `SubmissionUnknown` plus same-identifier lookup-first reconciliation after any POST byte.

   The same row resolves each over-inline maintenance resource through authenticated `MaintenanceResultMetadata` keyed only by target and identifier, verifies exactly target/identifier/kind/reviewed-source/content/length/media/revision/live-owner, takes the expected content digest from that response, and then consumes the same-handle `MaintenanceResultRead`. At read start it accepts only unchanged identity-bound metadata or the sole current-preview-to-application-receipt owner/revision transition; transfer after read start retains the acquired handle. An unreadable association or any other transition refuses before partial metadata, a start, or partial content, with no identifier-hash inversion or operation/workflow identity.

   Acceptance also proves `workflow_effect_operation_key` projects to `operation_key` only for registry command effects. Maintenance preview/apply reject both keys, occurrence, operation identity, and artifact identity; exact target/criteria and exact target/reviewed digest are their respective identities, identical retry resolves the durable receipt/associations, and changed target/digest conflicts without a command logical operation or effect fence.
5. Record each gate's name, Slingshot revision/tree digest, pinned FSM and RustSec repositories/revisions, Plan 0008 compatibility-manifest/source/tree/lock/seed-before/seed-after/gate-report digests, owner-metadata and attestation-policy/root digests, cache-set/coordinator-member/content/coverage-tool/image digests, exact five-field identity plus separate command-limits/canonical-contract/argument-schema/result-schema provenance and parity-report digests, Plan 0002 per-file/generation/limit/inventory/collision/S2/diagnostic/sensitive-lifecycle/assertion/exchange/framing native-report digests, the distinct Plan 0005 author-version/head/compression/informational/final/trailer/framing/exact-end/submission-certainty report digest, distinct per-row real-provider-API/enumerated-field and deterministic-provider-shaped-case digests, isolation-policy/coordinator-row digest, authenticated complete platform-evidence digest, command, outcome, and artifact digest in one canonical schema-valid manifest. Add separate runtime/transport complete-byte/sidecar/cache/embedded/report digests; CLI/current-MCP/legacy-MCP runtime-only and transport-only drift report digests; SQLite source/build/no-spill/SQL/VFS/arithmetic, universal 431-byte continuation authority, `VerifiedIdentityManagementTrustPolicyIdentity`/`VerifiedAuthorTrustPolicyIdentity` construction/role-separation/no-reload/interception-refusal, request-start retention, split connect/TLS phases, logical-outbox/fence, resumed-operation-artifact, publication/render-receipt, instance-bound-cleanup/PID-reuse, and operation-free maintenance-result parity/identifier/association/read/lifecycle evidence digests. Never include credentials, absolute host/container paths, or volatile timestamps in digest-bearing content.

   The manifest binds the exact maintenance metadata request/response field set, closed-unreadable-refusal inventory, unchanged and sole permitted owner/revision-transition cases, exact expected-digest handoff, and same-handle-read evidence separately from every operation-artifact or workflow-effect identity.
6. Refuse dirty or mismatched inputs, an incomplete/wrong-role cache, unresolved owner metadata/trust, unsigned or mismatched native evidence, missing or PATH-resolved coverage tool, ambient/default trust/cache, a weakened runtime invocation, any non-loopback connection, or input mutation. Read-only mounts plus denied remount capability enforce preservation against all descendants and the isolated coordinator compares bounded before/after input digests before exit; the host accepts only the single bounded output root and starts no post-container verifier child.
7. Make the release workflow depend on task `release-input-cache`'s distinct network-enabled preparation jobs, check out the pinned FSM and RustSec commits, and produce plus provider-attest each native row's archive/evidence/runtime-and-security report using its matching verified native cache member. Transfer the verified coordinator member and immutable inputs to the exact owner-declared coordinator row, load only the digest-pinned local image without pulling, invoke this command once with the isolation contract above, and publish only aggregate-verified release archives, checksum/evidence manifests, attestation bundles, runtime/security reports, and the acceptance manifest.
8. Keep the optional live harness outside automatic acceptance while recording whether separate operator evidence was supplied.

**Tests:**

- Every required gate appears exactly once in canonical order and a missing, duplicate, failed, or wrong-revision entry invalidates the manifest.
- Cache-consumer fixtures require the exact already verified coordinator member and prove its product/fuzz/FSM source/tool/coverage-binary/image identities and sole Plan 0008 seed projection reach the correct gates; exhaustive cache production/boundary fixtures remain owned by `release-input-cache`.
- A dirty Slingshot, FSM, or RustSec checkout, wrong external origin/revision, incomplete cache, unresolved owner metadata/trust, unsigned/wrong-subject/incomplete/duplicate/mismatched native evidence, missing pinned tool, wrong coordinator row, or weakened container policy is refused before a gate; a clean run leaves every read-only input unchanged.
- A missing/copied/prior-run/wrong-source-or-pin/self-timestamped RustSec review record, mismatched cache/row binding, or absence of authenticated provider release-time evidence fails. Replay later proves only that the pin was owner-reviewed for that historical release, not that it is currently fresh.
- Missing Windows remote-client refusal, an omitted `PIPE_REJECT_REMOTE_CLIENTS` flag, a policy-only substitute, or any unauthenticated Plan 0001 aggregate report fails all-row acceptance. Every row also fails if its real safe provider API omits an effective decision field, if its same compiled adapter omits a deterministic unconditional-permit, distrust/deny, purpose-restriction, external application/policy/name constraint, unevaluable, equivalent-duplicate, or conflicting-same-DER case, or if evidence uses certificate-only reduction, mutates the operating-system trust store, or assumes those cases preexist.
- Missing or mismatched named two-per-file/two-read/three-metadata traces, named two-generation S1/inventory/S2 traces, 1,048,576/16,777,216-byte bounds, parser-independent role inventory/collision/S2-before-selected-parser outcomes, 31/32/33 source-reference-free diagnostics, sensitive wrapper ownership, equality/in-place-mutation/replacement/recovery/exhaustion/complete-or-mixed-generation outcomes, or same-account-writer boundary fail. A Plan 0002 deterministic fake or explicitly untrusted current-native report cannot replace, augment, or satisfy any authenticated row.
- Exactly one authenticated native evidence set, for the separately owner-declared/probed row, contains the matching successful cache-seeded Plan 0008 gate report. Acceptance independently reruns the unchanged gate with its verified private coordinator seed; missing, duplicate, other-row, source-mode, source/manifest/lock/seed/report mismatch, ambient-cache access, allowed non-loopback connection, mutation, or disagreement between native and coordinator reports fails.
- The invocation-contract test admits only the pinned rootless daemonless OCI runtime/image with network none, no host socket/device/privilege/host namespace, all capabilities dropped, no-new-privileges, read-only inputs/root, and bounded explicit writable roots. An arbitrary descendant non-loopback probe fails while loopback fake identity/author services remain usable.
- Fake-author restart, cursor reset, snapshot-ahead replay, untracked-event advancement, live retry, recovery-window expiry, retained-control upgrade, and malicious configuration-secret cases each appear as named required gates rather than being inferred from a broader suite result.
- Provenance drift reaches no dispatcher or fake job. Canonical-contract drift fails independently from limits and role schemas; raw canonical bytes/order are checked before Draft 2020-12 decoded shape and typed conversion; exact `+01`/`-01` vectors select their registered outcomes; every command and revised failure appears exactly once in the parity report; maximum failure envelopes fit inline; and the first over-inline success preserves identical canonical bytes/digest through verified externalization in CLI and both Model Context Protocol revisions/resources.
- Runtime/transport manifest bytes, sidecars, typed values, persisted/capability echoes, and native reports authenticate independently. Acceptance contains named gates for all three continuation profiles through one universal authority and exact 431-byte ring with CAS/fence/shared rotation/node replacement/scale-out/rolling readiness; route-typed immutable `VerifiedIdentityManagementTrustPolicyIdentity` platform-only roots and `VerifiedAuthorTrustPolicyIdentity` platform-plus-selected-additional roots from one snapshot with no reload or role swap; hostile additional/private author-CA interception refusal; Plan 0002 sampled UTC/maximum exp plus HTTP/1.1-or-HTTP/2-only, no-migration/`Alt-Svc`/automatic-decompression, exact request/head/compression/body/token/informational/trailer/framing refusal with no partial token view; the separate Plan 0005 all-route HTTP/1.1-or-HTTP/2-only, raw-and-encoded-head/informational/final/trailer/framing/exact-end/post-byte-certainty policy; request-start delayed/equality retention; separate DNS/TCP and TLS phases; one logical outbox with bounded duplicate physical records and one effect; exact SQLite source/build/no-spill/closed-SQL/restrictive-VFS plus every current arithmetic/reader/checkpoint/backpressure boundary; same-handle resumed-operation-artifact prefix mutation; and complete operation-free maintenance-result parity/lifecycle.
- CLI, current Model Context Protocol, and legacy Model Context Protocol each refuse runtime-only and transport-only drift independently before operation-capable dispatch or current result interpretation. Publication/render races prove receipt-backed success rerender without resume/collision, cleanup proves current-nonce/retained-handle authority with diagnostic-only PIDs under replacement/reuse, and complete maintenance externalization preserves one canonical byte/digest identity through the identical operation-free `maintenance_result_access` association/URI/read lifecycle across all three clients.
- Maintenance resource acceptance first authenticates target-plus-identifier-only `MaintenanceResultMetadata`, its exact returned field set, and expected digest, then authenticates the complete same-handle `MaintenanceResultRead`; only unchanged identity-bound metadata or the sole current-preview-to-application-receipt owner/revision transition is accepted at read start, while every unreadable, mismatch, or other-transition case returns no partial metadata/content and no operation/workflow identity.
- Maintenance workflow acceptance rejects both operation-key spellings and every operation/artifact identity, and proves identical target/reviewed-digest retry resolves the durable receipt/associations without a command operation or effect fence.
- Reordered equivalent metascope and route-specific trust inputs plus genuine same-principal secret/key rotation preserve the selected revision. Adding, removing, or changing one effective metascope or platform root changes the owning trust identity and revision; selected additional-author-root drift changes `VerifiedAuthorTrustPolicyIdentity` and the revision while leaving `VerifiedIdentityManagementTrustPolicyIdentity` stable. Distrust/deny, wrong purpose, external application/policy/name constraint, unevaluable state, or conflicting same-DER decisions refuse before a platform DER set exists. Changing a Basic username or one Cloud organization/client/technical-account field changes target and revision; every mismatch refuses nonterminal recovery before provider construction/access or network execution. Independent exact preimages prove the contract-bound target/`VerifiedIdentityManagementTrustPolicyIdentity`/`VerifiedAuthorTrustPolicyIdentity`/revision hashes, compact assertion bytes, and decoded response-head charge.
- Every finalized ordinary/factory metatype provider, bundle-location, inventory, null-locale object-class, folded attribute, and exact type-mapping success has one fixture; every absent, duplicate, opposite-inventory, ambiguous, disappearing, failed, unsupported, or unknown case proves Unavailable before value access.
- Repeating acceptance at one source revision with the same complete native evidence and verified coordinator member yields identical seed projection, FSM gate report, digest-bearing manifest content, and release archive digests.
- The workflow publishes nothing when any gate or verification fails.

- **Done when:** `cargo test -p slingshot-development --test release_acceptance` and `scripts/release_acceptance --finite-state-machine-source <verified-path> --rustsec-advisory-database <verified-path> --rustsec-owner-review-record <verified-same-run-record> --locked-source-cache <verified-coordinator-member> --platform-evidence-directory <complete-native-evidence> --output-directory <new-output-path>` authenticate Plan 0002's exact generation/limit/inventory/diagnostic/sensitive-lifecycle/assertion/exchange/framing contract, the separate exact Plan 0005 author-version/head/compression/informational/final/trailer/framing/exact-end/submission-certainty contract, both route-typed trust identities and hostile additional/private author-CA interception refusal, every row's real safe provider API plus deterministic decision matrix without trust-store mutation, independent CLI/current-MCP/legacy-MCP runtime/transport refusal, exact SQLite no-spill/arithmetic, universal 431-byte continuation authority, request-start/split-phase transport, publication-success receipt rerender, instance-bound cleanup, complete operation-free maintenance-result association/metadata/read/lifecycle plus command-only workflow-key evidence, both runtime/transport contracts, and every outbox, resumed-operation-artifact, native/RustSec, Windows, five-field-plus-separate-provenance command, unchanged cache-seeded FSM, frozen/offline, and archive-verification gate.
