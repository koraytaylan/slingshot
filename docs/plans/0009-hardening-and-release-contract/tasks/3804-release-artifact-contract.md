---
id: release-artifact-contract
title: "Release Artifact Contract"
workstream: "0038"
kind: task
depends_on:
  - pinned-coverage-fuzzing-tool
  - owner-supplied-release-metadata
  - owner-confirmed-native-evidence-trust
  - owner-confirmed-github-automation
  - daemon-process-chaos
  - minimum-rust-and-dependency-gates
  - protocol-compatibility-snapshots
  - configuration-and-storage-compatibility
  - local-endpoint-threat-suite
  - release-input-cache
gated: false
touches:
  - scripts/build_release_artifacts
  - scripts/verify_release_artifacts
  - .github/workflows/release.yml
  - crates/slingshot-development/src/lib.rs
  - crates/slingshot-development/src/main.rs
  - crates/slingshot-development/src/release_artifacts.rs
  - crates/slingshot-development/tests/release_artifact_contract.rs
  - "crates/slingshot-development/tests/fixtures/release-artifacts/**"
  - scripts/record_rustsec_owner_review
  - crates/slingshot-development/tests/github_workflow_contract.rs
  - crates/slingshot-development/tests/fixtures/workspace-module-map/module-ownership.txt
status: done
merged_as: ""
---
# Release Artifact Contract

A release is the archive an operator receives, not the workspace tests that produced it. This task defines deterministic contents and native behavior, then authenticates every transferred row before trusting its claims.

**Steps:**

1. Commit per-row archive/evidence/runtime-and-security-report/attestation-bundle manifests, canonical Git-tree digests, clean/dirty/gitlink/export-substitution cases, platform-sensitive suite reports, source-mode and exact cache-seeded single-row FSM compatibility reports, matching/wrong/missing release-input-cache members and Plan 0008 seed projections, both source-protection claim modes, normalized metadata, deterministic-build environment, smoke-output including exact five-field command identities plus separate canonical-contract provenance and parity-gate digest, complete-matrix, exact-bound archive, absolute/parent/backslash/non-UTF-8/case-collision/duplicate/link/device/sparse/extra-data entry, and authentication rejection fixtures before the build scripts. Add exact/mutated runtime/transport manifest bytes, sidecars, embedded/report digests, distinct role bindings, and one-at-a-time runtime-only/transport-only drift for CLI and both Model Context Protocol revisions. Native report fixtures include Plan 0002's exactly two named per-file attempts with two same-handle bounded reads and pre/middle/post metadata, exactly two named whole-generation S1/inventory/S2 attempts, 1,048,576-byte generic-source and 16,777,216-byte aggregate limits, parser-independent role-tagged inventory with same-role deduplication and cross-role/fixed-location collision refusal, S2 equality before selected JSON/PEM parsing, 31/32/33 source-reference-free diagnostic outcomes, `SensitiveConfigurationDocument` versus `SecretValue` lifecycles, sampled-UTC/assertion boundaries, and the exact exchange cases: HTTP/1.1-or-HTTP/2-only; no protocol migration, `Alt-Svc`, automatic decompression, `Expect`, or upgrade; incremental 8,192-byte single-header, 64-field, 32,768-byte decoded-head and compression/header-list exact-next limits; 65,536-byte body and 16,384-byte token bounds; informational/trailer/ambiguous-framing refusal. Native trust fixtures have two explicit sources. The real row authenticates the safe provider API and its actual enumerated records/effective fields. The same compiled adapter separately consumes deterministic provider-shaped records spanning unconditional server-authentication permission, distrust/deny, purpose restriction, external application/policy/name constraint, unevaluable state, equal duplicates, and conflicting same-DER decisions. No case mutates an operating-system trust store or assumes its current contents happen to contain every negative record; a certificate-only list or lossy DER reduction remains a rejection fixture. Also include all three continuation deployment profiles through the identical universal authority with independently reconstructed 431-byte ring; the immutable provider-policy-verified platform server-authentication snapshot/identity used only by Identity Management Services and distinct immutable platform-plus-selected-additional-author-CA snapshot/identity used by author, with role-swap/live-reload/explicit-restart canaries and hostile additional/private author-CA interception refusal for Identity Management Services; request-start delayed/equality retention; distinct DNS/TCP-connect and TLS-handshake phases; exact pinned SQLite source/build, pre-initialization no-spill, memory temp-store, closed SQL inventory, restrictive VFS/object whitelist, ambient-temp traps, and current WAL/disk/replacement/reserve arithmetic; logical outbox/fenced-effect; same-handle remote resume; CLI publication immediately before/at/after commit and incomplete final rendering with exact receipt-backed rerender; current-nonce/retained-instance-handle cleanup with stale-nonce/replacement/PID-reuse sentinels; and maximum-inline plus first-over-inline operation-free `maintenance_result_access` parity with exact identifier/association/URI/read/lifecycle evidence.

   Maintenance release fixtures also require the authenticated operation-free target-plus-identifier-only `MaintenanceResultMetadata` request to yield exactly target/identifier/kind/reviewed-source/content/length/media/revision/live-owner and the expected content digest before `MaintenanceResultRead`, or a closed unreadable refusal with no partial metadata, start, or content. Missing, malformed, superseded, retired, wrong-target, derivation-invalid, owner-invalid, and blob-mismatched cases prove neither hash inversion nor an operation/workflow identity can enter the route. Unchanged lookup/read, the sole current-preview-to-application-receipt owner/revision transition before read start, transfer after read start, and every other invalid transition are distinct fixtures.

   Workflow release fixtures prove only registry command effects accept `workflow_effect_operation_key` as `operation_key`. Maintenance preview/apply reject either key, occurrence, operation identity, and artifact identity; preview uses exact target/criteria, apply uses exact target/reviewed digest, identical retry resolves the durable applied/replayed receipt and retained associations, and changed target/digest conflicts without a command logical operation or effect fence.
2. Implement the packager and one-row entry point. It verifies exact clean source/cache/tree, reads Plan 0001's abstract capability/artifact-layout row, and requires Plan 0009's owner mapping for the concrete host, architecture, provider image, toolchain, linker, system root/software development kit, environment, source/network protections, RustSec owner-review record, and the row's safe provider-record trust-decision API/probe. The native half proves that the real provider API enumerates records and exposes every effective server-authentication purpose, distrust/deny, external application/policy/name constraint, evaluability, and duplicate-decision field without reducing actual input to certificates alone. The deterministic half runs the same compiled decision adapter over provider-shaped fixtures for the full permit/reject/conflict matrix. The probe never mutates the operating-system trust store and never requires all negative cases to preexist there. Plan 0001's current-native report is explicitly untrusted and cannot supply or aggregate either half.
3. Export the verified tracked Git tree independently twice through the repository reader into two new owner-only source roots and verify both exports immediately before and after compilation against the canonical tree digest. Enforce the row's declared source-protection claim exactly: `operating_system_enforced` runs compilation as the declared non-owner build principal through the exact probed read-only filesystem boundary with no ownership/access-control/remount privilege and repeats the write/chmod/rename/replace denial probe; `digest_observation_only` uses read-only permission bits plus before/after digests only as tamper observation and states that a malicious same-principal tool could restore bytes. No evidence may call the latter immutable, sandboxed, or hostile-tool-resistant. Build each export with separate writable Cargo/target roots seeded only from the matching verified cache member, the exact cached Rust/Cargo toolchain, `--frozen --offline`, the row's active non-loopback-network denial, disabled incremental compilation, deterministic flags/source remapping, and a closed allowlisted environment with Cargo source replacement, wrappers, Rust flags, toolchain selectors, and ambient homes/caches removed. Require identical executable bytes before invoking the Rust packager to emit exactly the executable, opaque owner-supplied `LICENSE`, and a checksum manifest over those two files in canonical order with normalized archive metadata. Ignored caller files are neither exported nor part of the preservation claim; ambient `tar`, `zip`, locale, timezone, user, source path, and filesystem enumeration behavior never determine bytes.
4. Before extraction, enforce named compressed/decoded/entry/path/per-entry limits and exact flat canonical membership; reject absolute or parent paths, backslashes, non-UTF-8 names, case-fold collisions, duplicates, links, devices, sparse entries, trailing data, and undeclared entries. Extract without following or replacing filesystem objects into a newly created directory on that same native runner, then exercise version, help, configuration check, retained daemon hello/ping/status/stop, command schemas, and current plus legacy Model Context Protocol smoke sessions without using the build tree. First authenticate the embedded typed `slingshot.daemon-runtime-contract/1` and `slingshot.author-agent-transport-contract/1` bytes against the cache's complete manifests and sidecars and compare Hello, persisted-operation, capability, and report digests under their distinct roles. Run independent runtime-only and transport-only drift cases in the compiled CLI and both Model Context Protocol revisions with all other provenance fixed; each refuses before operation/list/maintenance dispatch or current agent-backed interpretation. Smoke then verifies canonical command limits, exact `slingshot.command-canonical-json/1`, role-schema bytes, every exact five-field identity plus separate canonical-contract artifact/digest/dual annotations, the twelve-row safety mapping, raw-before-schema-before-typed order, independent drift rejection, exact `1.0.0+01`/`1.0.0-01` and `AssetByteLength` vectors, canonical inputs, complete failures, maximum-inline/first-externalized behavior, and CLI/Model Context Protocol/fake-agent parity.
5. Run platform/runtime, configuration-root/credential/additional-trust-file, endpoint, storage, and agent-transport suites on each exact owner-mapped native row. The filesystem report authenticates Plan 0002's exactly two named per-file attempts, each with one open, two same-handle bounded reads, and pre/middle/post metadata, plus exactly two named whole-generation S1/inventory/S2 attempts. It records equality, in-place mutation, atomic replacement, first-attempt recovery, second-attempt success, per-file exhaustion, first-generation recovery, second-generation success, whole-generation exhaustion, complete/mixed generations, real platform metadata, and the same-account-writer boundary. It enforces `MAXIMUM_CONFIGURATION_SOURCE_DOCUMENT_BYTES = 1,048,576` and `MAXIMUM_CONFIGURATION_GENERATION_SOURCE_BYTES = 16,777,216`; uses a parser-independent profile/selection inspector to return the exact role-tagged inventory; deduplicates same-role reuse; rejects cross-role and snapshot/selection/profile fixed-location collisions; and proves S2 equality before selected service-credential JSON or additional-certificate-authority PEM parsing. The report also authenticates the inclusive diagnostic boundary—31 and 32 tuples without a marker, 33 as the first 31 plus `configuration_diagnostics_truncated` with occurrence count two—with no source reference or source-ordering oracle, temporary redacted `SensitiveConfigurationDocument` disposal, and long-lived extracted-secret-only `SecretValue` use. Plan 0002 fakes/current-native report cannot satisfy a row. Each row authenticates the real safe provider-record API by enumerating its actual records and effective decision fields. The same compiled adapter then processes deterministic provider-shaped fixtures for unconditional server-authentication permission, distrust/deny, wrong purpose, external application/policy/name constraints, unevaluable records, equal duplicates, and conflicting same-DER decisions. A certificate-only API, missing effective field or deterministic case, or lossy DER-only reduction invalidates the row; evidence performs no operating-system trust-store mutation and assumes no negative record exists in the live store. From one committed snapshot, require Identity Management Services to receive only the immutable provider-policy-verified platform server-authentication roots/identity and author to receive the distinct immutable effective platform-plus-selected-additional-author-CA roots/identity; bind both identities in the selected revision, prohibit live reload/role swap, and require a hostile additional/private author-CA endpoint to observe no successful Identity Management Services handshake or credential-bearing request. Authenticate one sampled whole-second UTC in `0..=253,402,300,799`, maximum `exp = 253,402,329,599`, the exact 25,868-byte three-field Identity Management Services `POST`, and only HTTP/1.1-or-HTTP/2 negotiation. Reject protocol migration, `Alt-Svc`, automatic decompression, `Expect`, and upgrade. Enforce incremental 8,192-byte single-header, 64-field, and 32,768-byte decoded-head bounds; compression/header-list and decoded exact-next boundaries; and 65,536-byte body and 16,384-byte token bounds. Reject the first bounded 100/101/103 before any later response; require final `Trailer` declaration plus absent/empty/nonempty trailer-section handling before token installation; and reject conflicting or invalid repeated `Content-Length`, simultaneous `Transfer-Encoding` plus `Content-Length`, invalid transfer-coding chains, HTTP/2 connection-specific fields or pseudo-header ordering/duplication errors, and bytes after the declared message boundary before any partial status/body/token view. Require Windows `PIPE_REJECT_REMOTE_CLIENTS` local success and separately provisioned remote refusal. Authenticate the exact pinned SQLite source/build and configuration-before-initialization, `SQLITE_CONFIG_STMTJRNL_SPILL = -1`, compile option `TEMP_STORE=3`, runtime memory temp store, closed bounded parameterized SQL inventory, restrictive VFS/open whitelist, poisoned ambient-temp oracles, and independently calculated 16,875,520-byte maximum-transaction WAL growth, 67,502,112-byte WAL, 50,626,592-byte high-water, 1,141,506,080-byte active aggregate, and 2,215,247,904-byte replacement aggregate/equal reserve. Authenticate every physical SQLite/checkpoint/backpressure recovery boundary, all three continuation deployment profiles through the same cluster-capable durable linearizable authority and exact 431-byte ring, shared-authority fence/replacement/scale-out/rolling readiness, separate DNS/TCP-connect and TLS-handshake phase boundaries, request-start-relative retention including delayed/equality receipt, logical-outbox/duplicate-physical/one-effect safety, and same-handle operation-artifact resume prefix authentication/mutation. For maintenance, authenticate the 64-lowercase-hexadecimal `MaintenanceResultIdentifier` derivation from target/kind/reviewed-manifest/content digests, exact 257-per-target association bound, kind/reviewed-source/content/length/`application/json`/revision/current-preview-or-application-receipt-owner metadata, `maintenance_result_access`, exact canonical URI, and `MaintenanceResultRead` same-handle prefix/full-stream verification with no operation or artifact slot. Require maximum-inline and first-over-inline preview/applied/replayed parity across CLI/current/legacy Model Context Protocol, apply ownership transfer, exact replay, supersession, and completed-receipt retirement. Additionally require compiled CLI publication/render races to prove publication is success, an incomplete render reruns from the exact authenticated receipt without resume/remote access/republish/collision, and mismatched receipts preserve the destination as collision. Require compiled CLI and Model Context Protocol cleanup to use responsive current-nonce stop or the retained instance handle for an unresponsive owned child, with diagnostic-only process identifiers and stale-nonce/replacement/PID-reuse sentinels. Bind the same-release RustSec record. On the probed FSM row, verify and bounded-copy only the exact cache seed into a new private root, invoke exactly `scripts/check_finite_state_machine_compatibility --finite-state-machine-source <verified-path> --cargo-home-seed <verified-private-seed>` under Plan 0008's sanitized private environment with empty ambient caches and non-loopback network denied, and recompute its digest before and after; every other row rejects the seed and omits the gate.
   The native author-transport suite separately runs every success/error route through HTTP/1.1 and HTTP/2 with no upgrade/`Alt-Svc`/migration/automatic decompression; exact/next 8,192-byte decoded-name-plus-value, 64-decoded-field, and independent 32,768-byte raw-HTTP/1.1/encoded-HTTP/2/decoded-aggregate limits; first-informational and final-head cardinality; declared/actual empty/nonempty trailers; ambiguous framing, HTTP/2 semantics, encoded/decoded excess, and trailing bytes. It proves finite/submission exact trailer-free end, artifact prebody/prepublication refusal, event-stream pre-exposure/reconnect-without-cursor-or-retraction behavior, and `SubmissionUnknown` plus lookup-first reconciliation after any POST byte.

   The native maintenance suite separately authenticates the exact target-plus-identifier-only `MaintenanceResultMetadata` request and returned target/identifier/kind/reviewed-source/content/length/media/revision/live-owner fields, expected-digest handoff, closed unreadable refusal with no partial metadata before streaming, unchanged metadata or the sole current-preview-to-application-receipt owner/revision transition at read start, transfer after read start, and subsequent same-handle `MaintenanceResultRead` without hash inversion, an operation/artifact slot, or a workflow effect operation key.

6. Emit one canonical native evidence manifest binding the abstract row, exact owner mapping/environment, cache and RustSec owner-review record digests, exact source/build inputs, protection/network probes, matching builds/archives, the exact five-field identity and separate limits/canonical-contract/argument-schema/result-schema provenance and parity-report digests, authenticated Plan 0002 stable-file/generation/limit/inventory/collision/S2/diagnostic/sensitive-lifecycle/assertion/exchange/framing report, distinct Plan 0005 author-version/head/compression/informational/final/trailer/framing/exact-end/submission-certainty report digest, and distinct authenticated real-provider-API/enumerated-field and deterministic-provider-shaped-case report digests including Windows remote-pipe outcome where applicable, FSM branch, and smoke outcomes. Also bind the two distinct runtime/transport manifest formats, complete-byte/sidecar/cache/embedded/report digests; exact SQLite build/no-spill/SQL/VFS/arithmetic report; universal 431-byte continuation-authority report; platform-only Identity Management Services and effective-author trust snapshot/identity/no-live-reload/role-separation/interception-refusal report; request-start-retention and split-connect/TLS-phase report; logical-outbox/fence and resumed-operation-artifact report digests; separate CLI/current-MCP/legacy-MCP runtime-only and transport-only drift report digests; artifact publication/render-receipt race digest; instance-bound cleanup/PID-reuse report digest; and complete inline/operation-free-maintenance-result parity/identifier/association/read/lifecycle evidence digests. Provider integrated time/workflow identity authenticates release-time evidence; no Git/self-authored timestamp creates freshness.

   Maintenance evidence digests separately bind the exact metadata request/response field set, closed-refusal inventory, unchanged and sole permitted owner/revision transition, expected-digest handoff, same-handle read, command-only workflow-operation-key rule, and keyless durable-receipt replay.
7. In each native release job, invoke full-commit-pinned `actions/attest` in explicit-file build-provenance mode with exactly `contents: read`, `id-token: write`, and `attestations: write`; forbid automatic artifact discovery, storage-record/registry inputs, and artifact/package write permissions. Produce one offline-verifiable bundle whose exact three-subject set is the row archive, evidence manifest, and runtime-and-security report. No other job receives either write permission, and no repository signing key exists.
8. Implement `scripts/verify_release_artifacts` to authenticate each bundle first through the owner-confirmed offline attestation policy/root, require its exact subject digests and source/workflow/builder identity, and only then validate the manifest's row identity, complete archive membership, file/aggregate digests, executable naming, source revision/tree digest against the current verified commit, owner metadata, native runtime/security/FSM/smoke evidence, and absence of undeclared files, credential sentinels, or source/temporary paths. Never use a network, ambient Git remote, operating-system trust store, or verifier default root.
9. Make the release workflow consume the earlier cache-preparation job and run the build script once on every exact native matrix row with only that row's verified member, empty ambient Cargo/Rust homes, and network denied. Each invocation performs and compares its two claim-scoped exact-tree builds and required native suites; only the authority's one probed compatible row materializes the recorded private seed and invokes the unchanged Plan 0008 gate with the exact `--cargo-home-seed` argument above. Transfer only each archive/evidence/runtime-and-security-report/authenticated-bundle set—not a writable seed—to one aggregate verification job and retain the verified aggregate checksum/evidence/acceptance inputs as workflow artifacts.

**Tests:**

- Two independent exports of the verified clean exact Git tree under each declared native row and its exact verified cache member produce matching executable and archive bytes plus checksum manifests with network denied and empty ambient caches. Dirty/untracked/gitlink/submodule, tree/cache-digest mismatch, export substitution, or observed source mutation fails before evidence. An `operating_system_enforced` row also rejects write/chmod/rename/replace from its non-owner build principal; a `digest_observation_only` row is accepted only with the explicit non-adversarial claim and is rejected if described as immutable or hostile-tool-resistant.
- Missing, extra, reordered, modified, over-bound, traversal-shaped, colliding, duplicate, link/device/sparse, trailing-data, path-leaking, and sentinel-bearing fixture archives are rejected before unsafe extraction.
- Locale, timezone, source root, file creation order, user/group identity, and host archive-tool availability cannot change archive bytes.
- Extracted binaries report the expected source version and pass all supported smoke paths on the same declared native row that built them.
- Extracted binaries reject stale/missing/role-swapped five-field identity or separate canonical-contract provenance before operation execution, reject canonical-contract drift independently from limits and either role schema, enforce raw-canonical-before-schema-before-typed ordering and the exact `+01`/`-01` plus `AssetByteLength` vectors, and reproduce the authenticated registry's twelve-row safety mapping, canonical phrase/asset/token rules, exhaustive revised failure shapes, and inline/externalized CLI/Model Context Protocol/fake-agent parity report; a missing or unequal report digest invalidates the row.
- Every supported-platform row has a schema-valid report for endpoint ownership, locking, detachment, readiness replacement, permissions/access-control lists, shutdown cleanup, the exact two named per-file/two-read/three-metadata and two named whole-generation configuration-root/credential/additional-trust-file schedules, exact 1,048,576/16,777,216 byte limits, role-tagged parser-independent inventory/collision/S2 ordering, inclusive source-reference-free diagnostic boundary, sensitive wrapper ownership, Plan 0002 UTC/maximum-exp/25,868-byte POST plus HTTP-version/migration/decompression/head/compression/body/token/informational/trailer/ambiguous-framing policy, the separate Plan 0005 all-route HTTP-version/raw-and-encoded-head/informational/final/trailer/framing/exact-end/post-byte-certainty policy, local endpoint attacks, and real native storage/artifact substitution through its declared adapters. The authenticated bundle binds the equality/in-place-mutation/replacement/recovery/exhaustion outcomes and same-account-writer boundary without accepting Plan 0002's fakes or explicitly untrusted current-native report as a row or aggregate substitute.
- Every row independently authenticates its real safe provider-record API and actual enumerated effective-decision fields, then runs the same compiled adapter over the complete deterministic provider-shaped permit/distrust/purpose/external-constraint/unevaluable/equal-duplicate/conflicting-same-DER matrix. A certificate-only enumeration, incomplete field/case report, lossy DER reduction, operating-system trust-store mutation, or assumption that a negative case preexists invalidates the row. Each row proves the platform-only Identity Management Services and distinct effective-author trust snapshots/identities arise from one committed snapshot, are bound separately into the selected revision, cannot reload or swap roles, and prevent selected additional/private author roots from intercepting Identity Management Services. Every row also authenticates both runtime/transport manifests and proves exact SQLite source/build/no-spill/closed-SQL/restrictive-VFS plus WAL/disk/replacement/reserve arithmetic, universal 431-byte all-profile continuation authority/readiness, request-start retention, distinct DNS/TCP and TLS phases, logical-outbox/duplicate-physical/one-fenced-effect, same-handle operation-artifact resumed-prefix mutation, and the complete operation-free maintenance-result association/read lifecycle; any missing, drifted, role-swapped, operation-shaped, or internally summarized substitute invalidates the row.
- Every row rejects runtime-only and transport-only drift independently in CLI/current-MCP/legacy-MCP; proves receipt-backed post-publication success rerender without resume, republish, or collision; proves current-nonce/retained-handle cleanup cannot signal by diagnostic PID or harm a replacement; and preserves complete over-inline maintenance bytes/digests through the identical operation-free `maintenance_result_access` association and canonical resource URI in all three clients.
- Every row resolves a maintenance resource only through authenticated `MaintenanceResultMetadata` keyed by target and identifier, takes the expected digest from its exact returned field set, and then authenticates the complete same-handle `MaintenanceResultRead`; only unchanged identity-bound metadata or the sole current-preview-to-application-receipt owner/revision transition is accepted at read start. Unreadable, mismatch, and every other transition return no partial metadata or content, and neither identifier inversion nor any operation/workflow identity is accepted.
- Release smoke and its FSM evidence reject operation keys, occurrences, operation identities, and artifact identities on both maintenance controls; exact target plus reviewed digest and the durable receipt/association lifecycle alone provide maintenance replay.
- Every row binds the same protected same-release RustSec review record; copied/prior-run/wrong-source/pin records fail. The Windows bundle additionally fails without authenticated local-success/remote-refusal `PIPE_REJECT_REMOTE_CLIENTS` evidence.
- Exactly the owner-declared/probed compatible row contains a successful Plan 0008 closed-target gate report from the exact pinned source plus verified private seed invocation. The bundle binds compatibility-manifest, source/tree/lock, cache member, seed-tree-before/after, command, sanitized-environment, network-denial, and report digests; absence, source-mode substitution, a second/unprobed row, ambient cache, network access, seed mutation, or any manifest/source/seed/report mismatch fails aggregate verification.
- The workflow's first real exact-repository bundle authenticates offline under the owner policy; unsigned, wrong-root/issuer/repository/workflow/source/builder/runner, missing/extra-subject, expired, or tampered bundles fail before a manifest assertion is trusted, even when all internal checksums agree.
- A single host cannot claim unmatched target rows; missing, duplicate, cross-built-only, wrong-runner, wrong-linker/software-development-kit, or mismatched reproducibility evidence fails aggregate verification.
- The release workflow uses the committed lockfile, owner-supplied metadata, owner-confirmed automation/attestation authority, exact native matrix, full-commit-pinned explicit-subject `actions/attest`, and exact local build/verify entry points with only the provenance job receiving its two additional permissions.
- A missing/wrong/extra/mutable cache member or Plan 0008 projection, ambient/default Cargo or Rust home, network-capable build/gate descendant, undeclared environment value, source replacement, compiler wrapper, Rust flag, or toolchain selector fails before compilation; every build and selected-row FSM gate command is exactly frozen and offline.

- **Done when:** `cargo test -p slingshot-development --test release_artifact_contract`, one network-denied `scripts/build_release_artifacts --target <supported-target> --locked-source-cache <matching-verified-row-member>` invocation from empty ambient Cargo/Rust homes on each exact native row, and offline `scripts/verify_release_artifacts --artifact-directory <complete-platform-evidence>` authenticate Plan 0002's exact generation/limit/inventory/diagnostic/sensitive-lifecycle/assertion/exchange/framing and route-distinct `VerifiedIdentityManagementTrustPolicyIdentity`/`VerifiedAuthorTrustPolicyIdentity` contract; the separate exact Plan 0005 author-version/head/compression/informational/final/trailer/framing/exact-end/submission-certainty contract; every row's real safe provider API plus deterministic decision matrix with no trust-store mutation; hostile additional/private author-CA interception refusal for Identity Management Services; independent CLI/current-MCP/legacy-MCP runtime/transport drift refusal; exact SQLite no-spill/arithmetic; universal 431-byte continuation authority; request-start/split-phase transport; publication-success receipt recovery; instance-bound cleanup; byte-identical complete operation-free maintenance-result association/metadata/read/lifecycle and command-only workflow-key evidence; both runtime/transport manifests; every exact archive/evidence/runtime-and-security subject; logical outbox/fence; deterministic bytes; and exactly one unchanged cache-seeded Plan 0008 gate with denied network.
