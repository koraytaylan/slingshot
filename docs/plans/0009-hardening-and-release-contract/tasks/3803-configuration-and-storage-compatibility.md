---
id: configuration-and-storage-compatibility
title: "Configuration And Storage Compatibility"
workstream: "0038"
kind: task
depends_on:
  - storage-fault-chaos
gated: false
touches:
  - crates/slingshot-development/tests/configuration_and_storage_compatibility.rs
  - "crates/slingshot-development/tests/fixtures/configuration-compatibility/**"
  - "crates/slingshot-development/tests/fixtures/storage-compatibility/**"
status: planned
merged_as: ""
---
# Configuration And Storage Compatibility

Profile documents, operation databases, and artifact metadata outlive one process and may outlive one release. This task proves supported old forms remain readable and newer forms fail without mutation.

**Steps:**

1. Commit one valid, boundary, and rejected fixture for every retained profile format, Cloud credential format, exact two-per-file/two-whole-generation attempt shape, generic-source/aggregate size boundary, parser-independent role inventory/collision/S2 order, public diagnostic marker, sensitive-document/secret lifecycle, assertion UTC/exp/body/head/trailer/ambiguous-framing policy, provider-policy-verified platform-only Identity Management Services trust shape, distinct platform-plus-selected-additional-author-CA effective-author trust shape, role swap, hostile additional/private author-CA Identity Management Services interception, relative token-expiry shape, SQLite schema version, installation identity, selected-target revision with both trust identities, operation/progress/retry state, both conditional recovery-evidence variants, every legal terminal kind/disposition pair including result unavailable after remote success, agent operation/subscription identity, job/event/cursor/digest/generation/watermark/result/retention state, and artifact metadata version. Token-exchange fixtures retain HTTP/1.1-or-HTTP/2-only negotiation, no protocol migration/`Alt-Svc`/automatic decompression, the 8,192-byte single-header/64-field/32,768-byte decoded-head plus compression/header-list exact-next boundaries, and 65,536-byte body/16,384-byte token bounds. Add both canonical runtime/transport manifest bytes, sidecars, and persisted digests; exact physical database/write-ahead-log/shared-memory, reader/checkpoint/backpressure state; independent WAL/disk/replacement/reserve arithmetic; pinned SQLite source/build and pre-initialization no-spill/memory-temp-store evidence; closed SQL-inventory and restrictive-VFS identity/object-whitelist evidence; agent logical-operation/outbox/sorted physical attempts/worker fence; every continuation deployment profile using the same universal authority revision and exact 431-byte ring; request-start retention; split DNS/TCP and TLS phases; same-handle artifact resume metadata; and complete inline plus operation-free `maintenance_result_access` state with identifier derivation, association metadata/owner, canonical URI, `MaintenanceResultRead`, and transfer/replay/supersession/retirement lifecycle. Selected-revision fixtures retain canonical metascope/two-trust-identity equality, same-principal rotation, and one-at-a-time drift.

   Retained Plan 0005 author-response fixtures independently preserve HTTP/1.1-or-HTTP/2-only negotiation with no upgrade/`Alt-Svc`/migration/automatic decompression; exact/next 8,192-byte decoded-name-plus-value, 64-decoded-field, and independent 32,768-byte raw-HTTP/1.1-head, encoded-HTTP/2-block, and decoded-status/field/separator-aggregate limits; informational/final/trailer/framing/trailing-byte refusal; finite/submission exact trailer-free end; artifact and event-stream exposure/publication behavior; and post-POST-byte `SubmissionUnknown` lookup-first reconciliation. Maintenance fixtures additionally preserve authenticated `MaintenanceResultMetadata` lookup by target plus identifier, its exact returned target/identifier/kind/reviewed-source/content/length/media/revision/live-owner fields or closed refusal with no partial metadata, and the subsequent expected-digest same-handle `MaintenanceResultRead` without hash inversion or an operation identity. They retain unchanged lookup/read, the sole current-preview-to-application-receipt owner/revision transition before read start, transfer after read start, and refusal of every other transition.
2. Parse each supported configuration fixture into the canonical current domain model and compare the complete nonsecret value. Require two exact attempt limits, source/aggregate limits, role inventory/collision and S2-before-selected-parser behavior, source-reference-free inclusive diagnostics, and `SensitiveConfigurationDocument` disposal before the retained typed snapshot. From one committed snapshot, construct the immutable provider-policy-verified platform server-authentication root snapshot/identity for Identity Management Services and the distinct immutable effective author server-authentication root snapshot/identity from that platform set plus only the selected additional author certificate authorities; bind both identities into `SelectedEnvironmentRevision` and permit no reload or role swap. Reordered equivalent metascope and role-specific trust inputs produce identical canonical values and revision; purpose-ineligible/constrained/unevaluable/conflicting platform records refuse instead of being silently reduced to DER, and additional author roots never enter Identity Management Services. Genuine same-principal password/private-key/client-secret/matched-public-certificate/access-token rotation remains stable; one effective metascope or trust input changes the exact owning identity/revision but not `AuthorTargetIdentity`, while additional-author-root drift leaves the platform-only Identity Management Services identity unchanged.
3. Copy each storage fixture, authenticate both stored contract digests against exact complete bytes/sidecars, open it through current migrations, and compare installation identity, operation/agent rows, selected revisions including both trust identities, progress/retry/recovery/terminal/fingerprint/correlation, event/cursor/digest/generation/watermark, failure/result/retention/artifact facts plus logical/outbox/physical/fence and every operation-free maintenance-result association fact. Require the exact 257-per-target association bound; rederive every `MaintenanceResultIdentifier`; validate kind/reviewed-source/content/length/media/revision/current-preview-or-application-receipt owner; require target-plus-identifier-only `MaintenanceResultMetadata` to return exactly target/identifier/kind/reviewed-source/content/length/media/revision/live-owner and expected content digest or the closed refusal with no partial metadata before `MaintenanceResultRead`; and preserve unchanged lookup/read, the sole current-preview-to-application-receipt owner/revision transition, checked-next-revision apply transfer, revision-stable replay, supersession, and atomic receipt-retirement behavior without hash inversion or an operation/artifact-slot field. Exercise each typed daemon-runtime physical SQLite dimension at exact/equality/next-unit; independently recompute 67,502,112-byte WAL, 50,626,592-byte high-water, 1,141,506,080-byte active aggregate, and 2,215,247,904-byte replacement/equal reserve; and require over-bound/checkpoint/backpressure refusal without mutation. Require the exact SQLite source/build, configuration-before-initialization, no-spill/memory temp-store, closed bounded SQL inventory, restrictive VFS whitelist, and poisoned ambient-temp invariants before migration. On nonterminal recovery, independently drift one metascope, one eligible platform root, or one selected additional author root and prove refusal before readiness, provider access, network, or durable mutation; exact equality of both trust identities and every other revision field alone resumes.
4. Hash fixtures before and after refusal to prove newer and corrupt inputs remain unchanged.
5. Retain old fixtures permanently while their versions remain supported; additions never overwrite an older fixture.

**Tests:**

- Every supported configuration version resolves deterministically and preserves exact attempt/bound/inventory/S2/diagnostic/sensitive-container/assertion/exchange/framing semantics with no partial token view, contract-bound target/authentication semantics, canonical metascope, immutable platform-only Identity Management Services trust, distinct immutable author-effective trust, both identities in the selected revision, no dynamic root reload or role swap, hostile additional/private author-CA interception refusal for Identity Management Services, and genuine same-principal rotation stability.
- Every supported database migrates to the current schema without losing or inventing an operation fact; post-success recovery uses fieldless authoritative remote success, and terminal authoritative remote success appears only with `ResultUnavailable`.
- Migration never changes a retained installation-derived agent identifier, selected-environment revision, retry deadline, cursor, digest, or snapshot watermark; independent metascope or trust-set drift refuses recovery before provider or network access and leaves the fixture bytes unchanged.
- Migration never relabels either contract digest, loses universal 431-byte continuation-authority, request-start retention, split transport-phase, the separate exact Plan 0005 author HTTP-version/head/informational/final/trailer/framing/exact-end/post-byte-certainty contract, logical outbox/physical-attempt/fence, or the exact operation-free maintenance identifier/association/metadata/read/lifecycle facts, or converts logical row bounds into physical SQLite claims. Every physical exact/next boundary follows the authenticated daemon-runtime manifest; an unavailable SQLite source/build/no-spill/SQL/VFS invariant or arithmetic mismatch refuses without mutation.
- Retained artifacts still verify by digest and length.
- Newer, corrupt, and unsupported fixtures fail with stable diagnostics and identical bytes.

- **Done when:** `cargo test -p slingshot-development --test configuration_and_storage_compatibility` authenticates profile/runtime/transport manifests and upgrades every retained fixture without losing exact configuration generation/diagnostic/sensitive-document/assertion/exchange/framing, the separate Plan 0005 author HTTP-version/head/informational/final/trailer/framing/exact-end/post-byte-certainty contract, route-distinct `VerifiedIdentityManagementTrustPolicyIdentity`/`VerifiedAuthorTrustPolicyIdentity` evidence, SQLite no-spill/arithmetic, universal continuation authority, request-start/split-phase transport, logical-outbox/fence, resumed-artifact, operation-free maintenance-result association/metadata/read, identity, revision, recovery, or result facts; byte-proves every refusal; and `scripts/quality` succeeds.
