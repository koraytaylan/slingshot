---
id: protocol-compatibility-snapshots
title: "Protocol Compatibility Snapshots"
workstream: "0038"
kind: task
depends_on:
  - model-context-protocol-fuzzing
gated: false
touches:
  - crates/slingshot-development/tests/protocol_compatibility.rs
  - "crates/slingshot-development/tests/fixtures/protocol-compatibility/**"
status: done
merged_as: ""
---
# Protocol Compatibility Snapshots

External and cross-process shapes must not change accidentally because a Rust field, registry order, or renderer changed. This task freezes every retained protocol version from independent fixtures.

**Steps:**

1. Commit compatibility snapshots first for every command's exact five-field identity—wire name, semantic version `1.0.0`, canonical `slingshot.command-contract-limits/1` digest, argument-schema digest, and result-schema digest—plus the separately authenticated canonical `slingshot.command-canonical-json/1` artifact/digest/dual annotations, with negative missing/extra/stale/role-swapped/one-bit-different provenance. The canonical-contract digest is never admitted as a sixth identity field. Include the exact twelve-row access/destructive/idempotency authority and Model Context Protocol annotation mapping; independent raw canonical-byte/typed-array-order vectors; one-at-a-time digest drift; exact `1.0.0+01`/`1.0.0-01` and `AssetByteLength` vectors; agent schemas/submitted-command digests; canonical inputs; the complete revised failure inventory; retained control/operation transcripts; stable errors; result/externalization boundaries; both Model Context Protocol revisions/resources; and workflow acknowledgement boundaries. Add Plan 0002 fixtures for exactly two named per-file attempts/two reads/three metadata observations, exactly two named whole-generation attempts, 1,048,576-byte source and 16,777,216-byte aggregate boundaries, parser-independent role-tagged inventory with same-role dedup/cross-role and fixed-location collision, S2-before-selected-JSON/PEM parsing, inclusive 31/32/33 source-reference-free diagnostics, `SensitiveConfigurationDocument` versus `SecretValue`, sampled UTC `0..=253,402,300,799` and maximum `exp = 253,402,329,599`, and the exact token exchange. Exchange fixtures require HTTP/1.1-or-HTTP/2-only negotiation; no protocol migration, `Alt-Svc`, automatic decompression, `Expect`, or upgrade; the exact 25,868-byte POST body; incremental 8,192-byte single-header, 64-field, and 32,768-byte decoded-head boundaries; compression/header-list and each decoded bound at exact/next; the 65,536-byte response-body and 16,384-byte token bounds; first 100/101/103 rejection; final `Trailer` declaration and absent/empty/nonempty trailer-section outcomes; conflicting or invalid repeated `Content-Length`; `Transfer-Encoding` plus `Content-Length`; invalid transfer-coding chains; HTTP/2 connection-specific/pseudo-header errors; and trailing-byte refusal without a partial token view. Pin the provider-policy-verified platform server-authentication root snapshot/identity for Identity Management Services separately from the effective platform-plus-selected-additional-author-CA snapshot/identity for author; include role-swap/one-at-a-time drift, hostile additional/private author-CA interception refusal for Identity Management Services, and purpose-ineligible/constrained/unevaluable/conflicting/lossy platform inputs. Also commit independent canonical bytes and SHA-256 sidecars for `slingshot.daemon-runtime-contract/1` and `slingshot.author-agent-transport-contract/1`, with repository/embedded/typed/advertised/persisted/report equality and negative byte/order/newline/sidecar/role mutations; all three named continuation deployment-profile/readiness shapes through the same cluster-capable durable linearizable authority and exact 431-byte ring; authority record/CAS/fence/rotation/replacement/scale-out/rolling vectors; request-start delayed/equality retention; distinct DNS/TCP-connect and TLS-handshake phase transcripts; exact pinned SQLite source/build/no-spill/closed-SQL/restrictive-VFS evidence and independent WAL/disk/replacement/reserve arithmetic; logical-operation/outbox/physical-attempt/worker-fence documents; same-handle artifact resume boundaries; complete inline maintenance values and operation-free `maintenance_result_access` associations/resources with exact identifier derivation, metadata/owner, URI/read, transfer/replay/supersession/retirement; CLI artifact-publication/ready-or-published-receipt/final-render race transcripts; and CLI plus both Model Context Protocol cleanup transcripts for current-nonce, retained-instance-handle, stale-nonce, replacement, and process-identifier-reuse cases.
2. Load each snapshot through its production parser and render it back through its canonical writer.
3. Hash each complete runtime/transport manifest, validate its sidecar and typed parser, then compare the distinct `DaemonRuntimeContractDigest` and `AuthorAgentTransportContractDigest` roles everywhere they are embedded, advertised, persisted, or reported before any dispatcher/repository/fake job. With all command and opposite-contract bytes fixed, independently drift only the daemon-runtime digest and only the author-agent-transport digest through the compiled CLI, current Model Context Protocol revision, and legacy revision; require each consumer to refuse operation/list/maintenance or current agent-backed interpretation before dispatch/rendering while retaining only its specified diagnostic/read-only paths. Independently authenticate the exact `slingshot.profile-authentication-contract/1` bytes and exercise its typed two-attempt/two-generation/source/aggregate/diagnostic/UTC/assertion/form/head/trailer/framing values without redeclaration. Require parser-independent role/S2 ordering, temporary-versus-long-lived secret ownership, and provider-policy proof before platform DER enters the immutable platform-only Identity Management Services store; construct the distinct immutable author-effective store only from that platform snapshot plus the selected policy-valid additional author roots after equal S2. Authenticate both role-specific identities in `SelectedEnvironmentRevision`, prohibit role swap and dynamic reload, and require no partial token view or successful hostile additional/private author-CA interception of Identity Management Services. For the runtime manifest, independently reconstruct the 32-byte WAL-header/24-byte-frame arithmetic through 67,502,112-byte WAL, 50,626,592-byte high-water, 1,141,506,080-byte active aggregate, and 2,215,247,904-byte replacement/equal-reserve result, and require exact SQLite source/build, pre-initialization `SQLITE_CONFIG_STMTJRNL_SPILL = -1`, compile option `TEMP_STORE=3`, runtime memory temp store, closed SQL inventory, restrictive VFS object whitelist, and ambient-temp traps. For the transport manifest, require the universal authority/431-byte ring, request-start retention, and split connect/TLS phases. Separately verify canonical command limits, `slingshot.command-canonical-json/1`, and role-schema bytes before comparing their tagged digests and preserve raw-before-schema-before-typed ordering.
4. Build one exhaustive registry-driven parity table. Preserve accepted SearchPhrase bytes without trim/normalization; require wire/FakeAuthor/Model Context Protocol FindAssets arrays to be unique and strictly ascending by UTF-8 bytes while the CLI constructor sorts one unique set exactly once; require every minimum/maximum asset byte endpoint to use canonical `AssetByteLength` in `0..=9_223_372_036_854_775_807` before range-order comparison; and treat continuation tokens as nonempty, control-free, bounded, and opaque outside Plan 0005. Include all three exact profile names through the identical universal authority, tampered framing/tag with all-32-byte constant-time comparison, the independently reconstructed exact 431-byte initialized key-ring encoding, monotonic checked key-identifier exhaustion, missing/unreadable/noncanonical/over-bound/unsafe-permission readiness refusal, old-or-new durable replacement cuts, restart stability, too-early/equality rotation, exact previous-key retention, current/previous/unknown keys, exact lifetime/skew, wrong target, wrong query, and expiry fixtures under the exact malformed, integrity-invalid, wrong-target, wrong-query, expired precedence. No profile uses private/node-local state or relaxes the authority based on current topology.
5. Cover every exact continuation failure; all configuration lookup/mismatch/ambiguity/duration-or-match-budget, unsupported/malformed reason, value-budget, and result-budget shapes; every content-package pattern/profile/filter/anchor/construction/cleanup/publication/eight-budget shape; and every add-component authoritative-no-effect category including `parent_not_orderable` plus `mutation_outcome_unknown`. Preserve only registered reason/budget/root-path/target-path/collection/expression-index fields and the exact terminal authority disposition; reject aliases, unknown/surplus fields, or cross-command/version failures.
6. Add independent conformance fixtures for escaped `listConfigurations`-only persistent-identifier lookup and filter injection; exact/factory persistent-identifier postchecks; exactly one `Configuration.getProperties()` call and one complete keys-only enumeration; and hostile nine-primary-type Open Service Gateway Initiative carriers/Unicode/duplicates. The keys-only phase completes bounds, folded duplicate, sensitive-name, and metatype classification before any value read; rejected/redacted keys receive zero `Dictionary.get` calls and each visible key exactly one. The metatype matrix requires exactly one acquired provider and one exact non-uninstalled bundle-location match; ordinary null factory persistent identifier with the requested instance identifier exactly once in `getPids()` and absent from `getFactoryPids()`; factory nonempty valid persistent identifier exactly once in `getFactoryPids()` and absent from `getPids()` while the instance identifier stays the result identity; one `getObjectClassDefinition(selected_identifier, null)` with no locale fallback, raw Designate parse, identifier guess, or cross-bundle search; one folded-plus-NFC attribute match; exact Password and nine NonPassword type mappings; and Unavailable before value access for every absent, duplicate, opposite-inventory, ambiguous, disappearing, failed, `BIGINTEGER`, `BIGDECIMAL`, or unknown observation. Also cover FileVault finite-set regex/XML quoting and segment adversaries/profile/version/capability/budget/no-widening/no-import behavior and non-orderable-parent no-InFlight/no-effect handling. These language-neutral fixtures are consumed by the fake agent; they do not claim the external Java bundle ran inside this Rust repository.
7. Prove maximum closed failures remain inline and fit the complete `MachineOutcomeEnvelope`, duplicated Model Context Protocol text/structured result, and pinned finite-state-machine acknowledgement. A first over-bound canonical command value chooses the daemon-owned `structured_result_artifact_access` outcome for its operation `structured_result` slot. A first over-bound complete maintenance preview or applied/replayed receipt instead chooses only the operation-free `maintenance_result_access` branch keyed by `(AuthorTargetIdentityDigest, MaintenanceResultIdentifier)`, with exact kind/reviewed-source/content/length/media/revision/owner metadata and canonical URI. Verify the identifier preimage, 257-association bound, authenticated target-plus-identifier-only `MaintenanceResultMetadata` request and exact returned field set, closed unreadable refusal with no partial metadata, metadata-supplied expected digest without hash inversion, and `MaintenanceResultRead` same-handle stream. Snapshot unchanged metadata, the sole current-preview-to-application-receipt owner/revision transition before read start, transfer after read start, every invalid transition, checked-next-revision apply ownership transfer, revision-stable exact replay, supersession, and completed-receipt retirement across CLI and both Model Context Protocol revisions/resources. The complete out-of-band bytes are never truncated, summarized, reserialized, or claimed to fit below 4096 bytes.
8. Require an observable change to introduce a new owning version fixture; retain all supported older fixtures unchanged.
9. Add negative fixtures proving a changed shape under an old version and a replaced old snapshot both fail.

The author-transport snapshots separately exercise Plan 0005's policy for every success/error route: HTTP/1.1-or-HTTP/2-only with no upgrade/`Alt-Svc`/migration/automatic decompression; 8,192 bytes for one decoded name plus value, 64 decoded fields, and 32,768 bytes independently for the raw HTTP/1.1 head, encoded HTTP/2 block, and decoded status/field/separator aggregate; first bounded informational rejection and exactly one final head; declared and actual empty/nonempty trailer refusal; conflicting/repeated `Content-Length`, `Transfer-Encoding` plus `Content-Length`, invalid transfer coding, HTTP/2 semantic errors, encoded/decoded excess, and trailing-byte refusal. Finite/submission results require exact trailer-free end, artifacts reject declaration before body and actual trailers before publication, event streams reject declaration before exposure and reconnect on actual trailers without cursor advancement/retraction, and every unvalidated response after any POST byte preserves `SubmissionUnknown` for same-identifier lookup-first reconciliation.

**Tests:**

- Every supported snapshot parses and canonicalizes byte-for-byte.
- Command, capability, local protocol, command-line, fake-agent, Model Context Protocol, and workflow views agree on the exact five-field identity plus separate canonical-contract artifact/digest/dual-annotation provenance, raw canonical requests, schemas, every closed failure field, conditional recovery/terminal evidence, nested machine-envelope result shapes, inline/externalized disposition, artifact identifiers, and stable errors. Canonical-contract drift fails independently from limits or either role schema, and raw-byte/order rejection precedes Draft 2020-12 decoded-shape and typed rejection.
- Current results retain required modern result/cache decoration, legacy results omit modern-only members, and normalized semantic payloads remain equal where the revisions expose the same operation.
- Retained control fixtures can inspect and stop every retained incompatible operation-protocol fixture.
- Every conforming Slingshot outcome, including authoritative-remote-success recovery and terminal result unavailability, preserves the exact structured object at `ack.result.structured` below the pinned finite-state-machine cap with absent `ack.result.structured_sha256`; the isolated synthetic upstream fixture alone preserves the canonical prefix plus `structured_sha256` behavior above it so a pin change is detected.
- Reordering internal maps cannot change canonical bytes or digests.
- Every continuation profile uses and byte-matches the same cluster-capable durable linearizable authority/readiness document and exact 431-byte ring; every logical outbox/physical-attempt/fence state, same-handle operation-artifact resume boundary, and complete maintenance inline/operation-free association/metadata/read branch byte-matches its owning Plan 0004/0005 contract; bounded duplicate physical records never imply more than one fenced effect attempt.
- Workflow snapshots require `workflow_effect_operation_key` and its `operation_key` projection only for registry command effects. Fixed maintenance preview/apply controls reject both key names, occurrence, operation identity, and artifact identity; preview uses exact target/criteria, apply uses exact target/reviewed digest, identical retry resolves the durable receipt/associations, and changed target/digest conflicts without a command logical operation or effect fence.
- One-at-a-time runtime-only and transport-only drift fails independently in CLI, current Model Context Protocol, and legacy Model Context Protocol with every opposite-contract and command identity byte fixed; no operation-capable dispatch or current result interpretation occurs.
- Pre-publication loss remains resumable and publishes nothing; publication wins at/after its atomic commit; incomplete rendering retains an exact receipt whose identical rerun authenticates and re-renders success without resume, remote read, republish, or collision. Cleanup uses only current nonce or the retained instance handle; stale nonce, replacement, and reused diagnostic process identifiers cannot affect an unowned process.
- Phrase whitespace is never trimmed; serialized asset-set permutations and duplicates never execute; opaque continuation bytes are never decoded outside Plan 0005; and every authentic/tampered/rotated/restarted token fixture selects exactly the registered precedence category.
- OSGi filter/PID/carrier/redaction, FileVault quoting/profile/budget/selection, and non-orderable-parent scenarios byte-match the independent conformance expectations and fake-agent trace without claiming execution by an external Java/FileVault implementation.
- Independently calculated profile-contract, principal, target, platform-only Identity Management Services trust, effective-author trust, and selected-revision preimages/digests prove the target digest has no second hash and `SelectedEnvironmentRevision` binds both trust identities. Basic username or one Cloud organization/client/`integration.id`-backed technical-account change changes target/revision, while genuine same-principal secret/key rotation remains stable. Reordered equivalent metascopes and each role-specific verified root input preserve the exact revision; adding, removing, or changing one effective metascope, platform root, or selected additional author root changes the exact owning identity and revision while preserving target identity. Additional-author-root drift leaves the platform-only Identity Management Services identity unchanged; any revision mismatch rejects retained recovery before provider construction/access, readiness, network execution, or mutation. A purpose-ineligible, provider-distrusted/denied, externally constrained, unevaluable, or conflicting duplicate platform record is rejected instead of reduced to DER. Dynamic reload and role swap are impossible, and a hostile additional/private author-CA fixture cannot intercept Identity Management Services.
- Exactly two per-file and two whole-generation attempts, exact source/aggregate bounds, parser-independent role/collision/S2 ordering, source-reference-free inclusive diagnostics, and temporary-source/long-lived-secret wrappers byte-match the profile-authentication contract. Exact assertion vectors accept only sampled UTC `0..=253,402,300,799`, reach maximum `exp = 253,402,329,599`, and construct the 25,868-byte POST maximum. Exchange vectors negotiate only HTTP/1.1 or HTTP/2; refuse protocol migration, `Alt-Svc`, automatic decompression, `Expect`, and upgrade; enforce incremental 8,192-byte single-header, 64-field, 32,768-byte decoded-head, compression/header-list, 65,536-byte body, and 16,384-byte token exact/next bounds; reject first informational heads and every declared or actual trailer before token installation; and reject every pinned ambiguous framing case without a partial status/body/token view.
- Independent transport transcripts prove DNS-plus-TCP connection and TLS handshake have separate exact deadlines with no budget borrowing, and that delayed complete-body receipt subtracts from the monotonic request-start retention anchor with equality at zero. They separately byte-match Plan 0005's author HTTP-version/head/compression/informational/final/trailer/framing/exact-end rules and post-POST-byte `SubmissionUnknown` reconciliation without borrowing Plan 0002's body/token limits. Independent storage transcripts prove the exact no-spill/closed-SQL/restrictive-VFS policy and every current WAL/disk/replacement/reserve arithmetic result; any missing invariant or one-unit drift refuses unchanged.
- Exact version vectors accept and byte-preserve build `1.0.0+01`, reject numeric prerelease `1.0.0-01`, and apply Plan 0003's complete-byte, total-identifier, and per-field digit charges independently.
- Unversioned shape changes and deletion or mutation of a retained snapshot fail with the owning compatibility surface named.

- **Done when:** `cargo test -p slingshot-development --test protocol_compatibility` independently authenticates the profile/runtime/transport contracts and drifts runtime/transport through CLI and both Model Context Protocol revisions; byte-matches exact configuration-generation/diagnostic/sensitive-document/assertion/exchange/framing plus route-distinct `VerifiedIdentityManagementTrustPolicyIdentity` and `VerifiedAuthorTrustPolicyIdentity` evidence, including hostile additional/private author-CA interception refusal for Identity Management Services, universal 431-byte continuation authority, request-start/split-phase transport, exact SQLite no-spill/arithmetic, publication/render receipt recovery, instance-bound cleanup, complete operation-free maintenance-result parity/lifecycle, and every command, failure, result, and retained protocol matrix; rejects every provenance or unversioned-shape drift before execution; and `scripts/quality` succeeds.
