---
id: present-state-product-documentation
title: "Present State Product Documentation"
workstream: "0039"
kind: task
depends_on:
  - live-adobe-experience-manager-harness
  - complete-repository-policy-audit
gated: false
touches:
  - README.md
  - CONTRIBUTING.md
  - ARCHITECTURE.md
  - docs/CONFIGURATION.md
  - docs/COMMANDS.md
  - docs/AGENT_PROTOCOL.md
  - docs/DAEMON.md
  - docs/MODEL_CONTEXT_PROTOCOL.md
  - docs/WORKFLOWS.md
  - crates/slingshot-development/tests/product_documentation.rs
status: planned
merged_as: ""
---
# Present State Product Documentation

The release documentation is part of the compatibility and safety surface: every example must parse, every behavior must exist, and every limitation must match an executable boundary.

**Steps:**

1. Add documentation contract tests first for required headings, code blocks, configuration examples, command invocations, protocol examples, state/error tables, security statements, workflow fixtures, and inbound links.
2. Align `README.md` with installation, first execution, automatic daemon behavior, command-line use, standard-input/output Model Context Protocol use, workflows, quality gates, and support boundaries that the release binaries implement.
3. Align `CONTRIBUTING.md` with fixture-first tasks, full-word identifiers, named numeric constants, complexity and file ceilings, present-state documentation, crate dependency direction, and exact local gates.
4. Align `ARCHITECTURE.md` and the six focused product documents with implemented committed-generation configuration and explicit-stop/start reload; the exact five-field command identity plus separate canonical-contract/role-schema provenance, never a sixth identity member; twelve-row safety classifications; raw-canonical-before-Draft-2020-12-before-typed validation; canonical non-trimmed phrase, ascending asset-set, and bounded `AssetByteLength` inputs; opaque continuation and Plan 0005 key/expiry ownership; the three exact deployment profiles through one universal cluster-capable durable linearizable authority, exact 431-byte key ring, and no private/node-local single-node exception; request-start retention; separate DNS/TCP-connect and TLS-handshake phases; the immutable provider-policy-verified platform server-authentication snapshot/identity used only by Identity Management Services and the distinct immutable effective platform-plus-selected-additional-author-CA snapshot/identity used by author, both from one committed Plan 0002 snapshot and both bound in the selected revision with no dynamic reload; side-effect-free escaped Open Service Gateway Initiative lookup with one property acquisition/one keys-only enumeration and finalized ordinary/factory metatype-first observation; non-widening FileVault generation/profile/import warning; non-orderable-parent refusal; exhaustive closed failures; shared inline/externalized result and CLI/Model Context Protocol parity; author-agent recovery-window; daemon retention/diagnostics/control-version; Plan 0004's exact SQLite source/build, no-spill memory policy, closed SQL inventory, restrictive VFS/object whitelist, and current WAL/disk/replacement/reserve arithmetic; supported-platform evidence; Plan 0008 provider-neutral pinned-FSM gate versus Plan 0009 owner-confirmed GitHub adapter; offline native-evidence attestation; Model Context Protocol; and workflow result-size contracts. Document exactly two named per-file attempts with two same-handle reads and pre/middle/post metadata, exactly two named whole-generation S1/inventory/S2 attempts, the 1,048,576-byte source and 16,777,216-byte aggregate limits, parser-independent role-tagged inventory with same-role deduplication and cross-role/fixed-location collision refusal, equal S2 before selected JSON/PEM parsing, and the same-account-writer boundary. Document public `ConfigurationDiagnostic` fields and the inclusive 31/32/no-marker versus 33/first-31-plus-marker rule without source references or ordering oracles, temporary `SensitiveConfigurationDocument` versus extracted long-lived `SecretValue`, sampled UTC `0..=253,402,300,799`, maximum `exp = 253,402,329,599`, the exact 25,868-byte ordered three-field `POST`, absent `Expect`/upgrade, first-informational rejection, trailer declaration/section rejection, and ambiguous-framing rejection without a partial token view. Document independently constructible contract/principal/target/two-trust-identity/revision inputs, exact compact assertion and decoded response-head rules, explicitly untrusted Plan 0002 current-native report, canonical metascope and role-specific trust stability/drift, hostile additional/private author-CA interception refusal for Identity Management Services, and pre-provider/pre-network recovery refusal. The supported-platform section must state that every release row exposes provider records with effective server-authentication purpose, distrust/deny, external application/policy/name constraints, evaluability, and same-DER conflict decisions; certificate-only enumeration and lossy DER reduction are unsupported. Plan 0009's sequential Phase R registration base must already contain Plan 0008's integrated `docs/WORKFLOWS.md`; this task updates that landed file after its own local prerequisites, because Makina task dependencies cannot cross bundles. State that Cloud service credentials establish identity while the operator must grant the technical account the least author repository and fixed agent-route privileges required by selected mutating commands; configuration validation cannot prove authorization.
5. Document that Identity Management Services permits only HTTP/1.1 or HTTP/2, disables migration/`Alt-Svc`/automatic decompression, and enforces the exact incremental 8,192-byte single-header, 64-field, 32,768-byte decoded-head, compression/header-list, 65,536-byte body, and 16,384-byte token boundaries. Separately document Plan 0005's author policy for every success and error route: HTTP/1.1-or-HTTP/2-only with no upgrade/`Alt-Svc`/migration/automatic decompression; exact/next 8,192-byte decoded-name-plus-value, 64-decoded-field, and independent 32,768-byte raw-HTTP/1.1-head, encoded-HTTP/2-block, and decoded-status/field/separator-aggregate limits; first-informational rejection, exactly one final head, trailer-declaration and actual empty/nonempty-trailer refusal, ambiguous-framing and HTTP/2-semantic refusal, and no trailing bytes. State the distinct finite/submission exact trailer-free end, artifact prebody/prepublication, event-stream pre-exposure/reconnect-without-cursor-or-retraction behavior, and `SubmissionUnknown` plus lookup-first reconciliation after any POST byte. Document native trust evidence as a real safe provider-API/enumerated-effective-field proof plus the same compiled adapter's deterministic provider-shaped decision matrix, with no operating-system trust-store mutation or assumption that negative records preexist. Document over-inline maintenance exclusively as the operation-free `maintenance_result_access` association and `MaintenanceResultRead` resource at `slingshot://profiles/{profile}/environments/{environment}/targets/{author_target_identity_digest}/maintenance/results/{maintenance_result_identifier}`, including identifier derivation, metadata/owner, apply transfer, replay, supersession, and completed-receipt retirement; it never uses an operation `structured_result` slot.

   Document that Model Context Protocol maintenance resource reads first call authenticated `MaintenanceResultMetadata` with only the target and identifier, obtain exactly target/identifier/kind/reviewed-source/content/length/`application/json`/revision/live-owner or a closed refusal with no partial metadata, and take the expected content digest from that success before `MaintenanceResultRead`. Document that read start permits unchanged identity-bound metadata or the sole current-preview-to-application-receipt owner/revision transition, while every other transition refuses and a transfer after read start does not replace the acquired same handle. The documentation permits neither identifier-hash inversion nor an operation, artifact-slot, or workflow-effect-operation-key identity for maintenance.

   Document that only registry command effects carry `workflow_effect_operation_key` as `operation_key`. Maintenance preview/apply reject both keys, occurrence, operation identity, and artifact identity; exact target/criteria drives preview, exact target/reviewed digest drives apply, identical retry resolves the durable applied/replayed receipt and associations, and target/digest drift conflicts without a command operation or effect fence.
6. Parse every TOML, JavaScript Object Notation, command-line, local protocol, agent protocol, and workflow example through production or compatibility parsers.
7. Remove claims unsupported by an executable test and keep prospective material exclusively in `docs/plans`.

**Tests:**

- Every documented configuration, command, and protocol example parses and matches current canonical output.
- Every documented operation, job, connection, error, and artifact state exists in the owning closed type.
- Every documented command identity remains exactly five fields while canonical-contract/role-schema provenance stays separate; every canonical request, configuration/FileVault/add-component/continuation failure, inline-or-operation-artifact command result, and inline-or-operation-free-association maintenance result parses through the exact schemas and byte-matches the CLI/Model Context Protocol compatibility fixture. Documentation rejects aliases, trimming, reordered wire sets, token decoding, a sixth identity member, provenance drift, or a maintenance result carrying an operation/artifact identity.
- Every documented external maintenance read uses the target-plus-identifier-only `MaintenanceResultMetadata` request, exact returned field set and expected-digest handoff, allowed unchanged-or-current-preview-to-application-receipt read-start transition, and same-handle `MaintenanceResultRead` sequence; unreadable refusal exposes no partial metadata or content and no example derives metadata by inverting the identifier hash.
- Every document passes link, structural heading, and falsifiable source-policy checks; the separate completed semantic review checklist records present-state accuracy and claim support without pretending the checker infers prose meaning.
- Security documentation states author-only traffic, operating-system-user local authorization and its same-account trust limitation, temporary-source versus extracted-secret ownership, source-reference-free public diagnostics, the role-distinct immutable platform-only Identity Management Services and effective-author root snapshots/identities from one committed snapshot, real safe provider-API/enumerated-field evidence plus deterministic same-adapter decision fixtures with lossy/certificate-only/mutation refusal on every supported row, explicit-restart-only root drift, hostile additional/private author-CA interception refusal and exact protocol/head/compression/body/token/framing refusal before any partial Identity Management Services token view, the separate exact Plan 0005 author HTTP-version/raw-and-encoded-head/informational/final/trailer/framing/exact-end/post-byte-certainty policy, Cloud technical-account authorization prerequisites, universal continuation authority with no single-node local-state exception, exact SQLite no-disk-transient refusal, read-only live verification, the provider-neutral local FSM gate and owner-confirmed GitHub adapter boundary, GitHub/attestation eligibility and trust boundaries, offline root/revocation limitations, the distinction between build provenance and end-user package signing, and the absence of Java-agent implementation in this repository.
- Documentation distinguishes hermetic protocol conformance from optional live Adobe Experience Manager evidence and names the exact supported deployment/platform matrix without extrapolating beyond tested rows.
- The documentation contract rejects a Plan 0009 workflow reference that omits or contradicts the Plan 0008 material present at the exact sequential registration base; no frontmatter pretends a cross-bundle task edge orders the shared file.

- **Done when:** `cargo test -p slingshot-development --test product_documentation` validates all nine exact product documents against production parsers and compatibility fixtures, including the exact Plan 0002 generation/limit/inventory/diagnostic/secret/assertion/exchange/framing and route-distinct `VerifiedIdentityManagementTrustPolicyIdentity`/`VerifiedAuthorTrustPolicyIdentity` contract, hostile additional/private author-CA Identity Management Services interception refusal, the separate exact Plan 0005 author HTTP-version/raw-and-encoded-head/informational/final/trailer/framing/exact-end/post-byte-certainty contract, split real/deterministic all-row provider-decision evidence, operation-free maintenance-result parity/lifecycle, five-field-plus-separate-provenance, universal 431-byte continuation authority, request-start/split-phase transport, and exact SQLite no-spill/arithmetic claims, and `cargo run --locked -p slingshot-development -- source-policy`, rustdoc, and `scripts/quality` all succeed.
