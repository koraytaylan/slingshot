# Plan 0002 — Profiles and Authentication — ✅ Complete

The roll-up row in [../STATUS.md](../STATUS.md) must stay in sync with this file. Task-level truth lives in [tasks/](tasks/) frontmatter; Makina's integration coordinator updates both layers.

- **Status:** ✅ Complete.
- **Goal:** provide deterministic profile and environment selection with deployment-correct, filesystem-safe, redacted authentication for Adobe Experience Manager 6.5 and Cloud Service.
- **Root cause:** every command requires an author environment, while Basic passwords and Adobe Experience Manager Developer Console service credentials have incompatible validation, storage, and token-acquisition rules that cannot safely remain untyped.
- **Approach:** resolve literal `~/.config/slingshot` from the sampled operating-system account; accept stable old-or-new files only through the exact two-attempt S1/source/S2 committed-generation proof; classify sources through a parser-independent profile/selection inspector; construct exact contract-bound principal/target/revision digests; bind a platform-only identity-management trust snapshot and a distinct author platform-plus-selected-additional trust snapshot into the revision; keep one warned Adobe Experience Manager 6.5 Basic cleartext opt-in; prove platform policies deterministically with only an untrusted current-native observation; pin exact compact Cloud assertions and decoded informational/final/trailer/deadline/token rules; disable ambient proxies; and expose author-only single-flight authentication; Plan 0009 owns authenticated aggregate native evidence.
- **Progress:** 15/15 tasks done; 0 blocked; 0 dropped.
- **Integration:** `complete`; run `develop`; base `main` @ `11c2e531d8a07a885321e7a09b1dccc623a733cf`; validation base `01975a11732aa9d822349218631392dd1deb4215`; mode `sequential`; final integration `3c035f3d762289c42fdbb6a691c6650832b8a6fb`.
- **Exceptions:** thirteen plan defects were found while implementing and each was fixed in its own commit before the task that needed it landed. `fix(0104)` made every workspace module-map rejection fixture one exact mutation again after a Plan 0001 leaf was removed from the tree. Nine fixes supplied manifest edges a task's recorded footprint omitted but its own tests required: `fix(0603)` (secret buffers for `slingshot-domain`; the domain edge and tracing for `slingshot-configuration`), `fix(0501)` (JavaScript Object Notation and TOML for `slingshot-domain`), `fix(0601)` (an account-database capability, plus the Windows feature selection that reaches the profile known folder and the process token), `fix(0602)` (secure hashing for `slingshot-configuration`), `fix(0504)` (secure hashing for `slingshot-domain`), `fix(0604)` (certificate decoding and each row's trust-decision capability for `slingshot-configuration`; the two inward edges for `slingshot-agent-connection`), `fix(0703)` (the development edge to the support crate), and `fix(0803)` (three inward edges and base64 for `slingshot-development`). Two fixes corrected assertions rather than manifests: `fix(0604)` narrowed a publisher scan that matched the word rather than a connection, and `fix(0501)` redacted a raw Basic user name the composed transcript found surviving into a debug rendering. `fix(0504)` also exposed the canonical metascope values the assertion builder frames. Two scope notes: `provide-environment-authentication` and `cache-cloud-access-tokens` landed in one commit because a provider that exchanged on every request rather than leasing would send a credential to Adobe once per Adobe Experience Manager call, so the cache is not separable from the provider that uses it; and the handshake half of the hostile-authority claim - that a selected author authority's certificate for `ims-na1.adobelogin.com` fails the identity-management handshake with zero request bytes - is proved here as set membership and noninterchangeable types rather than as a live handshake, because a live one needs a real protected listener and client, and is carried into Plan 0009's authenticated evidence.
- **Outcome:** Named profiles resolve reproducibly from one committed bounded source generation into immutable snapshots with independently constructible contract-bound principal/target/revision identities; genuine same-principal rotation preserves identity, canonical metascope or either route-specific verified-root drift changes revision, and Basic username or Cloud organization/client/technical-account drift changes target; platform-only identity-management trust cannot be widened by a selected author CA, while exact compact Cloud assertions, decoded response-section bounds, warned cleartext opt-in, direct author-only transport, deterministic platform policies, and conservative token leases fail closed without publisher/proxy traffic or secret/private-digest/source-reference disclosure.

_Last updated: 2026-08-29, against `main` @ `8d286e8`._
