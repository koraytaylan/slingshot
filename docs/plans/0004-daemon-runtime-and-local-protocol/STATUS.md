# Plan 0004 — Daemon Runtime and Local Protocol — 🚧 In progress

The roll-up row in [../STATUS.md](../STATUS.md) must stay in sync with this file. Task-level truth lives in [tasks/](tasks/) frontmatter; Makina's integration coordinator updates both layers.

- **Status:** 🚧 In progress.

- **Goal:** establish one exact classified source inventory with compiling shells for every genuinely new library leaf and one automatically discovered daemon per profile and environment with retained control, one digest-bound runtime contract, versioned target-matched operations, fail-closed installation and recovery identity, logically and physically bounded target-partitioned durable state, conditional terminal evidence, bounded inspection/complete maintenance, durable recovery-resume and maintenance-apply replay, prefix-authenticated result artifacts, fair scheduling, multiple waiters, and restart-safe ownership.
- **Root cause:** typed commands and target configuration have no persistent local execution authority, so short-lived clients cannot safely share work, retry identifiers, observe progress, or recover after interruption.
- **Approach:** first adopt Plan 0001's crate/module roots and retained behavioral/process-entry leaves through one exact fixture-backed Plan 0004 source inventory, create and declare only genuinely new library leaves including the test-daemon composition module, retain exactly the existing two workspace binaries, extend the exhaustive development dispatcher with an internal test-daemon subcommand in its owning feature task, require every behavioral task to descend from the scaffold, and prove the new-leaf declaration/source/footprint bijection plus ancestry for every overlap; consume Plan 0001's canonical foundation contract and platform adapters, commit one exact Plan-0004 runtime manifest/digest, extend retained current-nonce control separately from digest-bound versioned operation messages, keep process identifiers diagnostic-only, require remote-rejecting Windows pipes, accept Plan 0002's immutable snapshot only after its exact committed-generation proof, use its target digest without a second hash, and bind its profile-contract/scope plus `VerifiedIdentityManagementTrustPolicyIdentity` and `VerifiedAuthorTrustPolicyIdentity` in SelectedEnvironmentRevision into command fingerprints, admission, persistence, startup, and recovery-receipt equality; retain both exact distinct route-typed root snapshots without DER-only widening or cross-use; bound slow local peers and header-and-frame-aware SQLite database/WAL/SHM/transactions/readers/checkpoints with injected monotonic deadlines, close disk transients through a pinned memory-only SQLite build/runtime policy plus a restrictive VFS and SQL inventory, persist installation/target/lifecycle/progress/recovery/result/capacity and bounded replay receipts plus operation-free target-qualified maintenance-result associations transactionally, expose complete digest-reviewed maintenance manifests, resolve current association metadata by target and identifier before authenticating every resumed artifact or maintenance-result prefix on the same open handle, fail closed before readiness, compose unavailable execution in product and fake execution only through that internal development subcommand, then prove deterministic platform policy plus at most the current matching native row with nonce-safe or retained-handle cleanup; Plan 0009 owns authenticated all-row evidence.
- **Progress:** 26/34 tasks done; 0 blocked; 0 dropped.
- **Integration:** `in progress`; run `develop`; base `main` @ `8d286e88c06f91a1513834a4839ae36582212242`; validation base `6ac6c0b99263342468eb6275c60e731abb324da0`; mode `sequential`; final integration —.
- **Exceptions:**
  - **1710, maintenance-result associations.** This landing delivers the task's durable spine: the canonical `TerminalMaintenanceManifest` and its digest, mutation-free preview, digest-matched apply with the `DatabaseApplied` receipt committed in the same transaction as the removals, idempotent replay, and reference-checked blob release. It does not deliver the preview-result association machinery — the derived `0x00`/`0x01` target-qualified associations, current-preview supersession, and one-time ownership transfer at a checked association revision. Those are specified to order atomically against task 1708's metadata and read-start snapshots, which have not landed, so building them now would mean inventing the ordering they must be checked against. 1708 is where they belong.
  - **1503, `TEMP_STORE=3`.** The architecture requires the pinned build to report compile option `TEMP_STORE=3`. The pinned bundled SQLite (`rusqlite` 0.40.2, `bundled`) reports no `TEMP_STORE` option at all, which is the default of one, and setting it would mean changing how the dependency is compiled — outside 1503's footprint and outside this workspace's control of the crate. The invariant the requirement protects is that temporary storage never reaches disk, and that is held two ways instead: `PRAGMA temp_store` is set to `MEMORY` and read back on every connection, and a build reporting `TEMP_STORE=0` — the one build where that reading-back would prove nothing, because it forces temporary storage to disk and no pragma can override it — is refused outright at open.
- **Outcome:** One fixture-proved classification adopts the Plan 0001 roots and behavioral/process-entry leaves, gives every genuinely new Plan 0004 library leaf one compiling declaration, retains exactly the `slingshot` and `slingshot-development` binaries, reaches test-daemon composition only through the latter's internal subcommand, and makes every feature crate-reachable through a single dependency root with no unordered overlapping source footprint; concurrent clients then converge on one committed-generation-proved, opaque-target-and-revision-matched daemon using the upstream target hash directly and the distinct `VerifiedIdentityManagementTrustPolicyIdentity` and `VerifiedAuthorTrustPolicyIdentity` with their immutable route-typed root snapshots, retain control across operation-version or runtime-contract-digest incompatibility, never admit work through the unavailable product executor, and use the helper proof to demonstrate revision-bound target idempotency and recovery replay, conditional terminal evidence, exact logical and physical SQLite/storage backpressure and restart behavior, durable exact maintenance apply, complete bounded inline-or-target-qualified maintenance results readable without a synthetic operation, same-handle prefix-authenticated result transfer, genuine-rotation stability, and byte-preserving refusal after profile-contract, principal, canonical-metascope, or restart-visible trust-policy identity change.

_Last updated: 2026-08-29, against `main` @ `8d286e8`._
