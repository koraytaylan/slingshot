# Plan 0005 — Author-Agent Job Transport — 🚧 In progress

- **Status:** 🚧 In progress.
- **Goal:** submit exact contract- and revision-bound durable commands to the author-hosted agent, preserve one logical effect despite physically at-least-once Sling delivery, and converge on verified terminal truth across authentication refreshes, topology-safe continuation-key rotation, stream loss, and process crashes.
- **Root cause:** an accepted request followed by a daemon crash is ambiguous without agent idempotency, and a disconnected event stream is incomplete without replay or snapshot reconciliation.
- **Approach:** commit one exact AuthorAgentTransportContract manifest/digest; separately authenticate the exact `slingshot.command-canonical-json/1` artifact digest and both schema-root annotations before raw bytes, Draft 2020-12 shape, and typed conversion; pin target/revision/transport/unchanged-five-field identity and result correlation; accept only Plan 0002's committed-generation-proved opaque target/revision, use its target hash without a second digest, and consume its typed TLS 1.2/1.3/direct-or-warned-cleartext policy plus two distinct immutable root snapshots from the same selected snapshot: provider-policy-verified platform roots with `VerifiedIdentityManagementTrustPolicyIdentity` for Identity Management Services and effective platform-plus-selected-additional-author roots with `VerifiedAuthorTrustPolicyIdentity` for author, with no dynamic reload, merge, or cross-use; require one cluster-capable linearizable durable continuation-key authority contract for every deployment profile; permit only HTTP/1.1 or HTTP/2, reject protocol migration, informational heads, trailers, ambiguous framing, over-bound encoded/decoded headers, and trailing bytes before any finite result; anchor retention at request-start and split DNS/TCP from TLS deadlines; model one cluster-wide logical operation/outbox with bounded physical attempts and a fenced no-takeover effect gate; preserve complete extended maintenance bytes/digest through inline or Plan-0004-owned operation-free metadata/read/ownership-transfer/replay/retirement; exercise the language-neutral contract through restartable FakeAuthor without claiming Java/AEM execution; and prove bounded recovery, filtered reset, total Plan 0003 result/failure disposition, generation rotation, and artifact integrity.
- **Progress:** 2/23 tasks done; 0 blocked; 0 dropped.
- **Integration:** `in progress`; run `develop`; base `main` @ `8d286e88c06f91a1513834a4839ae36582212242`; validation base `79910359db555f1e9eb5a946fbcfab367cce9df3`; mode `sequential`; final integration —.
- **Exceptions:**
  - **1902, the simulator's surface.** This landing delivers the parts of FakeAuthor that other tasks build on and that can be proved on their own: the validated script with its route contract, secret-free recording, the continuation-key authority across all three deployment profiles behind one interface, and the logical-operation outbox with its worker fence. It does not deliver the full route-by-route protocol surface the task enumerates — every status, Content-Type, `Retry-After` form, HTTP/1.1 and HTTP/2 framing case, header-bound case, and FileVault or Open Service Gateway Initiative script. Those are exercised by the tasks that speak to them, and building them before a caller exists would mean guessing at the shapes those callers need; they land with 2401 and 2402, which own the conformance and disruption proofs.
- **Outcome:** An author-only protected transport whose committed-generation-proved opaque target hash/revision and distinct `VerifiedIdentityManagementTrustPolicyIdentity`/`VerifiedAuthorTrustPolicyIdentity` bind transport/canonical-contract-partitioned logical operations with exact canonical wire/version/limits/schema/argument/result correlation and request-start-conservative live/restart recovery to verified terminal results, authoritative post-success result loss, or explicit fail-closed disposition; genuine same-principal rotation remains stable while profile-contract/principal/restart-visible trust-policy-identity drift refuses before network, additional author authorities cannot authenticate Identity Management Services, and universal cluster-capable continuation-key authority, complete retention capacity, byte-identical inline-or-operation-free-associated maintenance, filtered-stream resets, generation rotation, and fenced outbox recovery permit duplicate physical Sling records but never authorize a second logical command effect.

_Last updated: 2026-08-31, against `main` @ `8d286e88c06f91a1513834a4839ae36582212242`._
