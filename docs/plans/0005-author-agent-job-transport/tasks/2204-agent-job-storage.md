---
id: agent-job-storage
title: "Agent Job Storage"
workstream: "0022"
kind: task
depends_on:
  - job-event-reducer
gated: false
touches:
  - crates/slingshot-storage/migrations/**
  - crates/slingshot-storage/src/database.rs
  - crates/slingshot-storage/src/lib.rs
  - crates/slingshot-storage/src/sqlite_statement_inventory.rs
  - crates/slingshot-storage/src/agent_job_repository.rs
  - crates/slingshot-storage/src/maintenance.rs
  - crates/slingshot-daemon/src/operation_maintenance.rs
  - crates/slingshot-storage/tests/agent_job_repository.rs
  - "crates/slingshot-storage/tests/fixtures/agent-job-storage/**"
  - crates/slingshot-daemon/tests/operation_maintenance.rs
  - "crates/slingshot-daemon/tests/fixtures/operation-maintenance/**"
status: planned
merged_as: ""
---
# Agent Job Storage

Remote submission, replay validation, shared-stream resumption, and restart reconciliation need durable facts that the Plan 0004 operation schema does not invent before the agent contract exists.

**Steps:**

1. Commit Plan 0004 current/retained fixtures plus generation/complete advertised current/global-prior agent capacity/no-retention-loss rotation/request-start-relative-retention, admitted command and exact CommandCanonicalJsonContractDigest/dual annotations/unchanged-five-field SelectedCommandContractIdentity/SubmittedCommandDigest, contract-and-digest-bearing mapping/tombstone/lookup/snapshot/terminal records, every legal RecoveryExecutionEvidence/category pair including all expanded configuration dictionary/metatype/value/redaction, FileVault profile/filter/publication, `parent_not_orderable`, package publication/mutation RemoteOutcomeUnknown, and PersistentCapacityUnavailable/AuthoritativeRemoteSuccess cases, retries/events/reset/compaction, missing grace, all TerminalFailureDisposition variants including ResultUnavailable/AuthoritativeRemoteSuccess, remote artifacts, namespace-wide shared-row capacity, coalesced incidents, terminal-maintenance shared-subscription cases with exact inline/operation-free-associated bytes/digest, preview/apply/replay/restart/supersession/retirement, interrupted apply and migration, and newer schema.
2. Adopt the dependency-ordered storage crate root, declare `agent_job_repository` exactly once while retaining the existing Plan 0004 `maintenance` declaration, and migrate every derivation preimage plus AgentOperationIdentifier, opaque AuthorTargetIdentity, SelectedEnvironmentRevision, AgentEventStoreGeneration, AuthorAgentTransportContractDigest, CommandCanonicalJsonContractDigest and authenticated argument/result role digests, canonical command/submission bytes, unchanged-five-field SelectedCommandContractIdentity, SubmittedCommandDigest, complete installed capacities/rotation, logical-operation/outbox revisions and states, bounded sorted physical attempts/job identifiers, worker lease/fence/no-return checkpoint, retry/evidence/request-start-derived retention, terminal snapshot/result/failure, artifact association, incident, and exact capacity counters without a second certainty field.
3. Add a subscription ledger independent of job foreign keys with subscription, event-store generation, cursor/high-water, canonical digest, optional AgentOperationIdentifier association, event disposition, per-job sequence, snapshot watermark, one unresolved integrity-incident slot, and named rolling-window/compaction metadata. Derive global partition/subscription/generation/event/remote-child/incident count and byte maxima from Plan 0004's retained-operation maximum plus one current reserve using checked arithmetic.
4. Implement capacity-reserving repository transactions for target/subscription creation, digest-bearing submission/Retired settlement, retry scheduling/pause/resume, PersistentCapacityUnavailable with AuthoritativeRemoteSuccess plus remote terminal/manifest/retention facts and no published local result, arbitrary valid unassociated-event cursor advancement without its terminal payload, digest-gated associated job fold, event-before-response association, digest-gated snapshot without ordinary cursor advancement, coalesced integrity-incident recording, atomic reset installation, ledger compaction, artifact mapping only after Plan 0003 result correlation, and conditional terminal settlement including ResultUnavailable/AuthoritativeRemoteSuccess.
5. Enforce uniqueness and referential integrity inside target/revision/transport/canonical-contract partitions for local/agent logical operation, outbox revision/attempt, physical Sling Job identifier, worker fence, subscription, cursor/sequence, and artifact slot. Multiple exact physical records may reference one logical operation; no record can authorize a second `ExecutionStarted` checkpoint/effect. Conflicting replay changes no fact.
6. Extend Plan 0004 terminal maintenance preview before remote execution is enabled: include every selected agent-operation, Sling Job, retry, incident-association, result, remote-artifact mapping, and eligible shared subscription/generation/event/resolved-incident logical key/revision in the same complete canonical manifest bytes/SHA-256 reviewed digest and before/projected/released capacity facts. If within the local machine inline bound return those exact bytes inline; otherwise use Plan 0004's reservation/install transaction and `0x00` `(AuthorTargetIdentityDigest, MaintenanceResultIdentifier)` association, returning its exact target/kind/source/content digest/length/revision/owner descriptor without operation or slot. Make that association readable through Plan 0004's target-and-identifier-only `MaintenanceResultMetadata` followed by expected-digest `MaintenanceResultRead`; persist branch/identifier/digests in preview and apply receipt; transfer preview ownership on apply at the checked next association revision, create the exact `0x01` association for an over-inline applied result, and make exact replay return the same association without another revision change. Apply removes exactly the reviewed facts, refuses every nonterminal row, and retains shared truth needed by any retained operation. Reopen revalidates association derivation/ownership, different preview supersedes only the current preview, and explicit completed-receipt retirement atomically retires its owned associations and unshared blob references.
7. Refuse a newer schema without mutation, roll back every injected migration/transaction/compaction/maintenance-apply failure, and retain every Plan 0004 operation and artifact fact during upgrade.

**Tests:**

- Every retained Plan 0004 fixture upgrades without losing an operation, installation-identifier snapshot, target partition, result disposition, or artifact reference.
- Interleaved, unknown, stale, and event-before-response records commit independent per-job facts and one monotonic subscription cursor without requiring a job foreign key.
- Exact retained replay is a no-op; conflicting operation, job, sequence, cursor, canonical event digest, SubmittedCommandDigest, target, subscription, result correlation, or artifact facts fail without partial mutation, including after reopen.
- Snapshot-ahead events are stale cursor-only no-ops unless a retained row must digest-match; ordinary snapshots preserve cursor, while reset installs captured high-water and every watermark atomically.
- Chatty-job compaction keeps each rolling digest window and the checked namespace-wide event count/byte total within their named bounds across reopen and never removes rows above the reconstructible watermark.
- Advertised current/global-prior agent-capacity and no-retention-loss generation-rotation observations survive reopen; rotation invalidates old-generation resubmission without deleting local recovery evidence or reducing a conservative unexpired remote-retention observation.
- Conditional recovery evidence, relative-retention, and missing-grace facts survive reopen while Submitting, Accepted, Running, or paused RecoveryRequired; PersistentCapacityUnavailable preserves AuthoritativeRemoteSuccess plus remote terminal/artifact facts without a result slot, and only a legal TerminalFailureDisposition clears scheduling atomically.
- Reopen recomputes target/revision/transport/canonical-contract/generation identity, authenticates both schema annotations/role digests and raw-canonical command bytes, then verifies unchanged-five-field identity and SubmittedCommandDigest; mismatch refuses byte-preservingly before provider/recovery and no drift rewrites stored provenance.
- Capacity reservation refusal and interruption leave Plan 0004 accounting exact and publish neither result nor artifact; maintenance plus exact resume reuses the same remote mapping instead of creating submission facts.
- Storage exposes exact conditional recovery evidence and terminal disposition without a generic compensation-safety field; AuthoritativeNonExecution, AuthoritativeRemoteFailure with possible partial effects, AuthoritativeRemoteSuccess, and FailClosedIndeterminate remain distinguishable after reopen.
- Exact partition/subscription/generation/event/remote-child/incident capacity admits, the next unit refuses before an unrecordable fact, repeated conflicts consume one coalesced incident slot, and eligible orphaned shared ledgers retire only through reviewed terminal maintenance.
- Terminal-maintenance preview/digest/apply is referentially complete or unchanged across interruption/replay/restart, preserves identical complete bytes and SHA-256 digest through inline-small and operation-free target-qualified association branches, returns exact current metadata without hash inversion, permits only the checked current-preview-to-application-receipt lookup/read transition, cannot select nonterminal work, includes/removes only reviewed target-partitioned remote/shared effects, preserves shared subscription replay truth plus every retained job, returns the same identity on apply replay, and makes superseded/retired associations unreadable atomically before a later read start.
- Interrupted migration and write transactions roll back fully, and newer schemas remain byte-identical.

- **Done when:** `cargo test -p slingshot-storage --test agent_job_repository` proves lossless installation/identity/artifact-and-schema/admitted-command/SubmittedCommandDigest migration, target/generation/transport/canonical-contract partitioning, namespace-wide logical capacity, atomic retry-pause/resume/submission/event/coalesced-incident/reset/digest-gated-reconciliation/result-correlation/artifact-capacity/conditional-evidence facts, request-start-conservative retention, job-independent cursor resumption, bounded rolling history, byte-identical inline/operation-free-associated complete maintenance manifests and receipts through metadata/read/preview/apply/replay/restart/retirement, snapshot-ahead/conflict preservation after restart, exact disposition without a generic compensation field, and newer-schema refusal.
