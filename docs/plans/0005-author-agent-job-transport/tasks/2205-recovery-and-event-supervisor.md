---
id: recovery-and-event-supervisor
title: "Recovery and Event Supervisor"
workstream: "0022"
kind: task
depends_on:
  - durable-idempotent-submission
gated: false
touches:
  - crates/slingshot-daemon/src/operation/recovery_and_event_supervisor.rs
  - crates/slingshot-daemon/src/operation/mod.rs
  - crates/slingshot-daemon/src/operation_maintenance.rs
  - crates/slingshot-daemon/tests/fixtures/recovery-and-event-supervisor/**
  - crates/slingshot-daemon/tests/recovery_and_event_supervisor.rs
  - crates/slingshot-daemon/tests/operation_maintenance.rs
status: done
merged_as: "f8cd0f352cc5a37361747d0b5995d68fd21c03b9"
---
# Recovery and Event Supervisor

One daemon-owned target supervisor must progress recoverable remote work while the daemon remains alive instead of making restart the retry mechanism.

**Steps:**

1. Commit deterministic fixtures for ambiguous submission, filtered-stream reconnect, exact/missing/wrong CommandCanonicalJsonContractDigest, either schema-root annotation/role digest, unchanged-five-field SelectedCommandContractIdentity, or SubmittedCommandDigest on operation/Sling Job lookup/snapshot/terminal result/failure including contract-only and limits-only mismatch, valid-shaped same-command/different-arguments substitution, equal-cursor conflict/full-subscription reset, missing-operation grace, post-success result/artifact acquisition including exact/bare/malformed/mismatched ArtifactUnavailable responses and delayed/equality request-start retention, successful recovery, complete Plan 0003 semantic/agent-capacity rejection including expanded configuration dictionary/metatype acquisition, FileVault profile/filter/staging/publication, `parent_not_orderable`, and mutation outcomes, Retired-before-terminal-truth, explicit/refused globally bounded no-retention-loss generation rotation and generation loss, integrity failure, automatic-policy pause for ambiguous/known work, definitely-not-submitted exhaustion, PersistentCapacityUnavailable/AuthoritativeRemoteSuccess with retention urgency, authoritative post-success result loss, Plan 0004 maintenance inline/externalized exact bytes plus ResumeOperationRecovery fresh compare-and-set/receipt replay/stale operation revision/wrong selected revision/wrong category, post-resume generation or persisted-identity/contract/digest mismatch, daemon restart under unchanged/forward/backward UTC, and clean shutdown.
2. Implement one RecoveryAndEventSupervisor per AuthorTargetIdentity partition over durable due-work selection, the filtered DaemonSubscriptionIdentifier stream, injected monotonic/wall clocks, and an injected random source.
3. Persist retry attempt/category, remaining delay, diagnostic UTC eligibility, exact RecoveryExecutionEvidence, and any bounded `Retry-After` contribution before waiting; compute capped full jitter with injected monotonic time/randomness under named automatic caps. For every capability/lookup/snapshot/result/artifact attempt, sample monotonic request-start before network and persist only the advertised retention minus checked total elapsed at complete authenticated receipt; equality is zero. Persist proven remote Succeeded, AuthoritativeRemoteSuccess, artifact manifest/mapping, and that conservative remaining-retention observation for post-success acquisition or PersistentCapacityUnavailable without scheduling an automatic capacity-paused transfer.
4. Before ambiguous-submission network retry, recompute target/SelectedEnvironmentRevision, AuthorAgentTransportContractDigest, CommandCanonicalJsonContractDigest, both schema-root annotations/role digests, AgentOperationIdentifier, raw-canonical command bytes, unchanged-five-field SelectedCommandContractIdentity, and SubmittedCommandDigest; require unchanged capability and perform logical lookup first. Accept Found/Retired only on exact provenance; only same-generation/transport/revision/canonical-contract/command Missing after grace may resend identical bytes while the logical record proves `ExecutionNotStarted`. Persist and query every known bounded physical Sling Job identifier; any drift uses those records or fail-closed recovery without replacement work.
5. On equal-cursor digest conflict, leave cursor unchanged, record a durable subscription integrity incident, mark stream Degraded, capture high-water, snapshot every known nonterminal operation, and resume only through the atomic reset path; if reset truth is unavailable, remain snapshot-polling or await a new generation.
6. Apply missing-operation grace and reservation semantics. Automatic caps pause SubmissionUnknown/RemoteOutcomeUnknown as RecoveryRequired; explicit same-identifier retry/health resumes. RetryPolicyExhausted is terminal only with ConfirmedNotExecuted.
7. Implement the architecture's mechanically complete registry-category mapping. Map all authoritative no-effect configuration lookup/value/result, load, discovery, and package failures—including FileVault profile/filter/root/read/build/staging-cleanup/proven-publication-failure—and create-page's seven plus add-component's eight categories including `parent_not_orderable` to Rejected/AuthoritativeNonExecution; preserve the staging-cleanup maintenance diagnosis and forbid rebuild. Map positive-prefix replication to RemoteFailed/AuthoritativeRemoteFailure. Keep replication `admission_outcome_unknown`, package `artifact_publication_outcome_unknown`, and create/add `mutation_outcome_unknown` nonterminal as RecoveryRequired/ExecutionCertainty(RemoteOutcomeUnknown), with same-operation reconciliation and no replay/rebuild/remutation before any later authoritative settlement or fail-closed loss. Map only validated identity-matching post-success ArtifactUnavailable Missing beyond grace or RetentionExpired, and other authoritative post-success result/artifact loss, to ResultUnavailable/AuthoritativeRemoteSuccess. Keep bare/malformed/mismatched artifact errors in acquisition recovery; reserve RecoveryWindowExpired for a validated Retired operation mapping before terminal truth. Never trust a wire-selected disposition or expose generic compensation safety.
8. Reconstruct restart deadlines as monotonic-now plus diagnostic-UTC residual clamped from zero through persisted delay; virtual wall-clock jumps affect scheduling only and never identity/state/certainty.
9. Consume Plan 0004 ResumeOperationRecovery only after its target/SelectedEnvironmentRevision gate and fresh durable compare-and-set. Preserve receipt replay: exact replay causes no wake; fresh apply wakes only the stored category. Before provider/network work, verify opaque target/revision, runtime/transport/canonical-contract digests, both schema annotations/role digests, generation/derived identity, raw canonical command, unchanged-five-field identity, SubmittedCommandDigest, logical/outbox revision/fence, and the complete known physical identifier set; mismatch uses existing lookup/loss recovery and never allocates current-generation replacement. PersistentCapacityUnavailable resumes only local completion of proven remote success.
10. Start due-work recovery before new admission, maintain bounded fairness across categories/operations, and detach on shutdown without cancelling remote Sling Jobs.

**Tests:**

- Every retry category progresses to success after FakeAuthor is released without restarting the daemon, and uses the same `(AgentEventStoreGeneration, AgentOperationIdentifier)`, canonical-contract/dual-annotation/five-field provenance, SubmittedCommandDigest, DaemonSubscriptionIdentifier, and artifact identifier/slot.
- Injected random samples produce the exact persisted full-jitter schedule within named caps; unchanged/forward/backward UTC restart fixtures reconstruct a clamped monotonic deadline without resetting attempts or changing operation facts.
- Every TerminalFailureDisposition schedules no later attempt and never allocates a replacement identifier.
- Cursor conflict cannot advance Last-Event-ID or heal through one-job snapshot; only full high-water/all-nonterminal atomic reset restores streaming, otherwise snapshot polling continues.
- Missing lookup inside grace retries same-generation same-identifier reconciliation; Retired and generation loss fail closed, while known jobs use their identifier route.
- Validated agent-capacity refusal is terminal authoritative nonexecution without rotation/retry; an observed explicit generation rotation never resubmits an old identifier, retains request-start-conservative advertised recovery windows including zero at delayed equality, and reconciles only through retained known-job truth before fail-closed loss.
- Exhausted automatic attempts for SubmissionUnknown/RemoteOutcomeUnknown—including replication admission, package publication, and mutation ambiguity—remain nonterminal RecoveryRequired/ExecutionCertainty and assert no remote outcome; post-success acquisition remains RecoveryRequired/AuthoritativeRemoteSuccess without certainty. Explicit resume uses the same identities and SubmittedCommandDigest. ConfirmedNotExecuted exhaustion alone may become AuthoritativeNonExecution.
- ResumeOperationRecovery rejects a wrong selected revision, stale operation revision, or wrong category without mutation; exact receipt replay is side-effect-free, and a fresh compare-and-set schedules only the persisted recovery category. A later generation or persisted-identity mismatch uses lookup/fail-closed recovery and cannot create a second job.
- PersistentCapacityUnavailable publishes no local success/result/artifact, issues no automatic transfer or POST, exposes bounded retention urgency and AuthoritativeRemoteSuccess, and after maintenance/resume acquires the same result through the same remote identities or settles authoritative retention loss as ResultUnavailable/AuthoritativeRemoteSuccess rather than unknown execution.
- Bare, malformed, wrong-generation, wrong-operation, wrong-artifact, wrong-slot, and wrong-status-reason ArtifactUnavailable responses cannot terminalize an operation; an exact post-success Missing/RetentionExpired response can produce only ResultUnavailable/AuthoritativeRemoteSuccess, while pre-truth operation Retired alone produces RecoveryWindowExpired.
- Due-work fairness remains bounded under a chatty stream, and shutdown sends no remote cancellation.

- **Done when:** `cargo test -p slingshot-daemon --test recovery_and_event_supervisor` proves live recovery, generation/transport/canonical-contract/dual-annotation/digest-gated same-identifier retry, request-start retention, request/result substitution refusal, known-job generation-loss lookup, durable jitter and pause/resume including all three RemoteOutcomeUnknown command families and byte-identical maintenance-backed authoritative-success capacity recovery, full-subscription conflict reset, missing-operation/pre-truth-Retired handling, identity-bound artifact-unavailability recovery/result mapping, total expanded category-to-kind/disposition mapping, exact conditional evidence without generic compensation safety, stable identity, fairness, restart continuation, and noncancelling shutdown.
