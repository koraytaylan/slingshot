---
id: author-agent-conformance
title: "Author-Agent Conformance"
workstream: "0024"
kind: task
depends_on:
  - author-agent-operation-executor
  - durable-idempotent-submission
  - artifact-download
gated: false
touches:
  - crates/slingshot-daemon/Cargo.toml
  - crates/slingshot-daemon/tests/fixtures/author-agent-conformance/**
  - crates/slingshot-daemon/tests/author_agent_conformance.rs
status: done
merged_as: "6fd929fa31c530e117bd655ab6b695bd40d9b10f"
---
# Author-Agent Conformance

Prove the complete target daemon and fake author agree on capabilities, authentication, submission, events, snapshots, structured results, and artifacts.

**Steps:**

1. Commit scenario fixtures for exact transport-manifest digest and every bound; exact command-canonical artifact digest, both schema-root annotations/role digests, and contract-only drift; raw command/result canonicality before Draft 2020-12 decoded shape and typed conversion; Plan 0002 selected-snapshot TLS 1.2/1.3, platform-only `VerifiedIdentityManagementTrustPolicyIdentity`, distinct platform-plus-selected-additional-author `VerifiedAuthorTrustPolicyIdentity`, direct-or-explicitly-warned-cleartext, no root reload/merge/cross-use, explicit-restart either named identity drift, and proxy/publisher/untrusted-root/additional-author-CA-only-IMS-interception/old-TLS/hostname canaries; complete/incomplete/mismatched committed generations; direct no-second-hash target digest; equal versus independently changed opaque target/revision values including profile-contract, canonical-metascope/either named trust identity, Basic username, and Cloud organization/client/`integration.id`-backed technical-account upstream vectors; all three continuation deployment profiles through the same cluster-capable authority with multi-node CAS/fence/replacement/rolling/readiness/431-byte/rotation vectors; cluster-wide logical operation/outbox, zero/one/multiple physical matches, worker-fence and no-takeover checkpoints; protected author routes; unchanged-five-field/SubmittedCommandDigest provenance; exact current/prior capacities; request-start delayed/equality retention; HTTP/1.1-or-HTTP/2-only negotiation with no upgrade/migration, encoded/decoded head bounds, first-informational/trailer/framing/trailing-byte refusal; distinct DNS/TCP/TLS/request/header/body deadlines; response/status/query/event/reset/restart cases; exact side-effect-free escaped OSGi lookup plus one `Configuration.getProperties` dictionary/two-phase value acquisition, provider/designate/factory/bundle-location applicability, and metatype-first redaction; 262,144/262,145 load disposition; hostile FileVault regex/XML/profile/budget/no-widening vectors; pre-mutation `parent_not_orderable`; every other closed result/failure/disposition; byte-identical inline/operation-free-associated maintenance through metadata/read/preview/apply/replay/restart/supersession/retirement including the sole ownership race; inline/externalized/remote artifacts; and terminal/recovery truth.
2. Start one production-composed target-scoped daemon for a fixture profile/environment and one FakeAuthor, then execute each scenario through the daemon's Plan 0004 operation interface.
3. Assert capability discovery authenticates exact canonical artifact/dual annotations, matches generation and all five unchanged SelectedCommandContractIdentity fields, bounded current-generation lifetime reservation/execution-detail/subscription/event/snapshot/result/artifact capacity, quota/capacity/reset-only event replay with no temporal-retention claim, global prior-generation operation-detail/snapshot/result/artifact capacity, no-retention-loss explicit rotation, Retired, dual lookup, and request-start-relative detailed-operation/snapshot/result/artifact retention before first submission; refresh generation and every provenance value before recovery submission.
4. Follow the filtered subscription generation/cursors and per-job progress, inject unassociated/event-before-response/stale events, force snapshot-ahead and high-water reset, and observe terminal data only after durable persistence.
5. Record all fake-author routes and diagnostic output for author-only and redaction assertions.

**Tests:**

- Basic and Cloud credential providers each complete their scenario from one complete committed generation and its two distinct immutable root snapshots. Same-principal rotation preserves target/revision and remote identities; profile-contract or principal change alters the upstream opaque target/revision; canonical metascope, `VerifiedIdentityManagementTrustPolicyIdentity`, or `VerifiedAuthorTrustPolicyIdentity` drift changes only the revision; an incomplete/mismatched generation refuses before admission/provider/network. A live platform-root or additional-author-CA-source edit changes neither client, while explicit restart either named identity drift refuses old nonterminal work. An additional-author-CA-only Identity Management Services interception receives no request. A too-short Cloud lifetime produces one identity-management exchange, no installed lease, no author request, and no automatic refresh loop.
- Every representative command uses the current persisted event-store generation, exact canonical-contract artifact digest and both schema annotations/role digests, correct unchanged wire/version/limits/argument-schema/result-schema identity, exact ExpectedArtifactManifest, canonical validated arguments, and their exact SubmittedCommandDigest persisted by both sides, a daemon-derived generation-scoped target-partitioned AgentOperationIdentifier, and one DaemonSubscriptionIdentifier. FakeAuthor treats the identifier as opaque, independently validates/recomputes each provenance layer, rejects contract-only drift before decoded schema, and scopes the reservation to the explicit generation/contract/digest.
- Unknown/event-before-response/stale events with the exact explicit generation advance only their legal independent-ledger disposition, a missing/mismatched JobEvent generation or associated terminal SubmittedCommandDigest advances nothing, snapshot-ahead/reset replay converges through digest-matching snapshots, and a chatty job remains within the rolling window.
- Same-generation restarted FakeAuthor retains active mapping or Retired tombstone; generation loss recovers known Sling Job truth where present and preserves indeterminate certainty when absent, with no replacement submission.
- Exact agent-store capacity admits while the next current-generation operation/execution-detail/subscription/event/snapshot/result/artifact count or byte unit refuses before effect; no pressure evicts or rotates. Repeated rotation refuses nonterminal work, generation-number exhaustion, and the next global prior-generation/operation-detail/snapshot/result/artifact row or byte unit without mutation, preserves every request-start-conservatively unexpired advertised window, persists each checked monotonic generation atomically only after terminal-only state, derives disjoint identifiers for identical new local inputs, and never resubmits an old pair.
- Physical Sling requeue remains logical Running; exhausted automatic recovery for ambiguous/known work remains nonterminal RecoveryRequired and asserts no terminal remote outcome.
- ResumeOperationRecovery applies Plan 0004's target/selected-revision/operation-revision/category compare-and-set and receipt replay exactly; a fresh apply wakes only the persisted category, then validates current generation and stored remote identities without creating replacement work.
- PersistentCapacityUnavailable exposes no terminal result, preserves AuthoritativeRemoteSuccess/retention urgency, and after terminal maintenance plus exact resume acquires only the same result/artifact without another job POST; authoritative expiry becomes ResultUnavailable with no certainty field.
- Every route obeys the shared HTTP/1.1-or-HTTP/2-only encoded/decoded response-head, informational, trailer, and unambiguous-framing policy. No finite route exposes a status/body/result and no artifact publishes before a trailer-free exact boundary; an invalid post-byte submission response remains SubmissionUnknown. Artifact Missing/RetentionExpired becomes terminal after proven success only from the exact generation/operation/artifact/slot-bearing document and maps to ResultUnavailable/AuthoritativeRemoteSuccess; bare or mismatched errors remain recovery, while pre-truth Retired alone maps to RecoveryWindowExpired.
- Machine-inline, generic local JSON artifact, and exact precomputed-manifest/fixed-route remote JSON/opaque package artifacts pass artifact/annotation authentication, SubmittedCommandDigest, and Plan 0003 `validate_result_for_command`, preserve complete logical schemas—including the load Artifact result's requested path—and both presentation budgets; load is Inline only through 262,144 canonical document bytes and remote at 262,145 despite the general 1,048,576 ceiling; valid-shaped same-command/different-arguments substitutions persist nothing, and package bytes are never automatically extracted.
- OSGi lookup never calls a creating API and rejects filter injection, mismatch, ambiguity, over-count, and timeout without partial data. It calls selected `Configuration.getProperties()` once, completes bounded key enumeration plus all provider/designate/factory/bundle-location/object-class/attribute applicability and redaction decisions before any value access, then gets only visible NonPassword innocuous values once; dictionary/key change, early/extra/redacted access, absence, ambiguity, failure, or conflict discards all. PASSWORD/unavailable/sensitive canaries are Redacted without access and illegal carriers/evidence never reach storage/log/output.
- Small and over-inline extended maintenance previews resolve to identical complete canonical bytes and SHA-256 reviewed digest; operation-free target-qualified metadata and descriptors match exact target/kind/source/content digest/length/revision/owner, the metadata-supplied digest drives read without hash inversion, apply permits only the checked current-preview-to-application-receipt lookup/start transition, replay/restart preserves the same identifier and bytes without truncation or regeneration, a different preview makes only the superseded association unreadable, and completed-receipt retirement atomically retires its result association before a later read start.
- All network routes target author directly, every canonical-query attack fails before route aliasing, publisher/proxy canaries receive zero connections, and whole-output scans find no credential material.

- **Done when:** `cargo test -p slingshot-daemon --test author_agent_conformance` passes exact manifest/artifact/dual-annotation/raw-canonical gates, committed-generation-gated opaque target/revision with direct no-second-hash partitioning, distinct immutable Identity Management Services platform-root and author effective-root selected-snapshot policies with no cross-use (including the additional-author-CA-only Identity Management Services canary), universal 431-byte continuation authority and replacement, logical/outbox/fenced one-effect safety with duplicate physical records, bounded capacity/rotation/request-start-retention/strict-framing/split-phase response/query/event recovery, complete dictionary/OSGi/FileVault/add-component and other Plan 0003 mappings, exact load/maintenance/result/artifact disposition including operation-free maintenance metadata/read/retirement, direct author-only traffic, and classified-secret exclusion without claiming Java execution.
