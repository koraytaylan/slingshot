---
id: fake-author
title: "Fake Author"
workstream: "0019"
kind: task
depends_on:
  - agent-store-and-logical-execution-contract
gated: false
touches:
  - crates/slingshot-test-support/src/fake_author.rs
  - crates/slingshot-test-support/src/lib.rs
  - crates/slingshot-test-support/tests/fixtures/fake-author/**
  - crates/slingshot-test-support/tests/fake_author.rs
status: planned
merged_as: ""
---
# Fake Author

Provide one deterministic author-servlet substitute that can prove protocol and recovery claims without a Java project or live Adobe Experience Manager instance.

**Steps:**

1. Commit script fixtures for the separate exact command-canonical artifact digest, both schema-root annotations, unchanged five-field capabilities/generation including independent contract/annotation/limits/role drift, opaque `(AgentEventStoreGeneration, AgentOperationIdentifier)` reservation plus exact SelectedCommandContractIdentity/SubmittedCommandDigest recomputation/persistence/echo and wrong-generation/contract/digest rejection, same-command/different-validated-arguments result substitution, checked monotonic generation allocation/restart/maximum exhaustion, exact Plan 0003 continuation-token construction/validation/precedence, fresh/current/previous/too-early/exhausted/crash-interrupted/restarted/missing/corrupt/authorization-unsafe initialized key-ring scenarios with nonproduction canaries, exact/over current-generation operation/execution-detail/subscription/event/snapshot/result/artifact count/byte retention capacity including token bounds and fixed key-state readiness, exact/over global prior-generation/operation-detail/snapshot/result/artifact row/byte capacity, atomic worst-case branch reservation and no-effect refusal, repeated explicit rotation refused by nonterminal work or unexpired-retention capacity and atomically accepted otherwise, Basic and Bearer requirements, selected-snapshot TLS 1.2/1.3/trust/direct/cleartext-warning policy, context-prefix cross-site request-forgery token fetch/expiry/restart, exact current-author Referer acceptance/rejection, absent agent/token ingress routes, equal versus changed opaque target/revision partitions, stable subscription registration, idempotent acceptance, Retired tombstone, continuation success/wrong-query/wrong-target/expired/malformed/integrity-invalid/mutation-between-pages, discovery anchor failures, conflict, physical Sling retry, explicit-generation JobEvents with contract/digest-bound terminal data, event-before-response, canonical/hostile filtered stream/high-water/operation/Sling-Job queries and artifact segments, unknown operations, quota-and-watermark-bounded replay/high-water/reset, chatty jobs, request-start anchored relative-retention snapshots/results/artifacts with delayed/equality receipt, valid/bare/malformed/mismatched ArtifactUnavailable responses, exact two-phase Open Service Gateway Initiative `getProperties` dictionary/PID/type/carrier/redaction failures, load 262,144/262,145 Inline/artifact plus count/byte/duration exhaustion, exact FileVault profile/filter/build/staging/publication failures and eight budget literals, create/add mutation outcome unknown plus `parent_not_orderable`, every route/status/Content-Type/Content-Encoding and `Retry-After` form, HTTP/1.1/HTTP/2 equivalence and other-version/upgrade/`Alt-Svc` refusal, first bounded 100/101/103, trailer declaration and absent/empty/nonempty actual trailer sections, ambiguous HTTP/1.1 framing, invalid HTTP/2 header semantics, trailing bytes, exact/over HTTP/1.1 raw/decoded and HTTP/2 encoded/decoded single-header/count/aggregate response-head limits, distinct DNS/TCP/TLS/request/header/body deadlines, malformed/secret-bearing data, disconnects, delays, same-generation restart, generation loss with known/missing Sling Job, persistent-capacity refusal/resume, complete inline/externalized maintenance bytes/digest, post-success result loss, and location-free stable artifacts before implementation.
2. Implement all three continuation-key deployment profiles in the simulator through the identical cluster-capable durable linearizable authority interface, including the AEM 6.5 single-node profile; cover read/CAS/fence/lease, concurrent bootstrap, orphan/corrupt refusal, multi-node issue/validate/rotate, node replacement, rolling-version compatibility, exact 431/768-byte bounds, and absence/authorization/readiness refusal. No profile can weaken the provider contract based on current node count or deployment observation. Keep the simulation explicitly language-neutral; do not claim that Java or a provider secret service executed.
3. Implement the cluster-wide logical operation/outbox and worker-fence simulator. Let scripts create zero, one, or multiple physical Sling Job records/attempts for one logical operation; exact bounded postcheck associates all matching records, mismatch/over-count/timeout fails closed, and only one CAS can cross `ExecutionNotStarted` to `ExecutionStarted`. A lost lease after that transition never permits a second effect.
4. Implement FakeAuthor on loopback with an ephemeral port and exact routes from the plan architecture.
5. Drive behavior from a validated script and expose recorded requests, expected/echoed artifact manifests, connections, credential-presence flags, target partitions/revisions, remote identifiers, logical-operation/outbox revisions, physical job/attempt sets, subscription filters, and effect count to tests without retaining secret values.
6. Use controllable channels, injected wall/monotonic clocks, retention-checkpoint/restart controls, and request-byte/header/body/idle checkpoints for delays, heartbeats, stream closure, and response release; do not use correctness-sensitive sleeps.
7. Refuse every publisher-shaped route and record any attempted route outside the author contract.

**Tests:**

- Every script action produces the exact protocol fixture bytes.
- Basic and Bearer modes distinguish missing, wrong, and accepted credentials without recording values.
- Every POST accepts only a freshly issued bounded token in exact `CSRF-Token` plus the normalized current-author-origin Referer; expired/missing/duplicate/injected headers fail, and no token or credential value is recorded.
- Repeated identical same-generation submissions return one logical operation only after the exact artifact digest and both schema annotations/role digests authenticate, raw command bytes pass `slingshot.command-canonical-json/1`, the unchanged five-field identity and transport digest match, and SubmittedCommandDigest recomputes; acceptance, duplicate, Retired, lookup, snapshot, event, and terminal data echo all provenance. Contract-only drift refuses independently. Crash/ambiguity may yield multiple bounded physical records, but competing/stale-fenced consumers cause exactly one logical effect; an expired mapping retains its tombstone and never starts another effect.
- Exact agent-store capacity admits; the next current-generation operation/execution-detail/subscription/event/snapshot/result/artifact count or byte reservation returns validated no-effect `agent_capacity_unavailable` without a job or partial record and never evicts or rotates. Repeated rotation rejects nonterminal/in-flight work, retires only expired prior records oldest-first, refuses byte-identically at the next global prior-generation/operation-detail/snapshot/result/artifact row or byte unit, preserves every unexpired advertised window, persists each checked next generation across restart, refuses byte-identically at unsigned-64-bit generation exhaustion, and rejects every old `(generation, opaque identifier)` submission because its explicit generation is no longer current while retaining its read-only recovery routes.
- Every scripted post-byte invalid status/header/body/identity response can coexist with a created job; authoritative lookup finds that job or Missing, and no script permits the client to terminally discard or replace ambiguous work.
- Every route can emit the strict HTTP-version/informational/trailer/framing cases without the simulator or client collapsing them into one final collected head; finite and artifact scripts expose no accepted completion before the exact trailer-free boundary, while an event-stream trailer ends only that connection after already committed valid events.
- Events are visible only on the registered DaemonSubscriptionIdentifier, event-before-response is keyed by AgentOperationIdentifier, terminal data echoes that operation's SubmittedCommandDigest, and replay honors generation plus Last-Event-ID across interleaved jobs.
- Every emitted JobEvent carries the scripted generation, and every scripted artifact 404/410 can emit only its exact identity-bearing missing/retention-expired document unless the scenario deliberately requests a bare, malformed, or mismatched protocol-invalid response.
- Replay never claims a duration: compaction stays within the reserved event quota and row/byte maxima, retains every row above the reconstructible snapshot watermark, and returns the bounded reset contract whenever a requested cursor is outside that structural window.
- Closing/reopening FakeAuthor in one generation retains active mappings or retired identifier tombstones and never creates a second logical effect; interrupted remaining-duration checkpoints and arbitrary UTC jumps only extend retained windows, while daemon-side delayed responses subtract from the pre-request monotonic anchor and equality yields zero. A scripted generation change recovers known physical Sling Job records through their identifiers and reports authoritative missing for the missing case.
- A physical Sling retry/requeue emits another logical Running observation with monotonic attempt/progress and never Running to Queued.
- Cursor expiry captures generation/high-water, snapshot state can advance beyond unseen events, and chatty scripts compact a bounded rolling window.
- Continuation scripts use the exact Plan 0003 canonical header/payload, 900,000-millisecond lifetime, 60,000-millisecond skew, role-tagged HMAC input, unpadded base64url framing, target/version/non-window-query/Initial-limit/resume-key binding, and failure precedence. Current/previous 32-byte keys survive restart, rotation retains the previous key for 960,000 milliseconds, equality/exhaustion/crash/permission/missing-state boundaries fail exactly, tag comparison uses a constant-time primitive, and no scenario exposes key bytes or claims external Java execution. Later pages use the protected limit without caller offset/limit or repeated initial offset and preserve strict per-page ordering while allowing repository mutation to affect later pages.
- Query scripts accept only canonical single-decode ordered pairs and distinguish malformed escape, lowercase hexadecimal, overencoded unreserved, plus-space, duplicate, missing, surplus, and double-encoded attacks; artifact scripts accept only safe canonical operation/slot segments and retain one identifier/slot across exact, truncated, oversized, wrong-digest, interrupted, and restarted transfers.
- OSGi scripts escape backslash, asterisk, and both parentheses once in a bounded `(service.pid=...)` filter, invoke only bounded `listConfigurations`, postcheck exact PID, and classify zero/mismatch/ambiguity/timeout without partial properties. Provider/designate/factory/bundle-location/null-locale/object-class/attribute absence, conflict, disappearance, or failure becomes Unavailable before any value-access trace; hostile carriers/Unicode and sensitive names expose no value.
- FileVault scripts independently pin selected versus structural-directory serialization, exact `\A\Q...\E\z` and `\E` replacement, XML codepoint/entity escaping, ordering, installed-version/capability/profile negotiation, and all regex/XML adversaries; unsupported profiles and every budget fail before artifact publication. Add-component scripts prove `parent_not_orderable` before mutation.

- **Done when:** cargo test -p slingshot-test-support --test fake_author passes transport/separate-canonical-contract/dual-annotation/unchanged-five-field compatibility, raw-canonical-first command validation, opaque target/revision and SubmittedCommandDigest echo/substitution, universal-authority 431-byte vectors, request-start retention, split transport phases, selected-snapshot TLS/trust/direct/cleartext canaries, logical-operation/outbox/fenced single-effect behavior with possible duplicate physical records, bounded current/prior capacity and rotation, explicit-generation terminal events, canonical exact routes, strict HTTP/1.1-or-HTTP/2 informational/trailer/framing and encoded/decoded response-head cases, reset/restart, complete dictionary/metatype/FileVault/add-component failure inventory, load threshold and maintenance/result/artifact truth, and deterministic no-Java-execution simulation.
