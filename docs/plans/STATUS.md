# Plans — roll-up board

One row per Makina plan bundle. Task counts and outcomes mirror each plan's `STATUS.md`.

Registration follows the closed sequential Phase R contract in [README.md](README.md): only one numbered bundle may be registered without successful final integration; Plan 0001 binds the exact reviewed current commit containing its source-digest-matching bundle and descended from its recorded authored base; and every Plan N greater than 0001 binds a validation-base OID exactly equal to Plan N-1's verified final-integration OID after every earlier plan is complete. Phase R refuses gaps, concurrent or later active registrations, incomplete or unverifiable predecessors, a non-full or unequal base OID, and root/plan status disagreement. Task dependencies remain bundle-local.

| Plan | Title | Status | Tasks | Outcome | Status doc |
|---|---|---|---|---|---|
| 0001 | Foundations and Walking Skeleton | ✅ Complete | 16/16 | A pinned unpublished workspace with exact abstract platform/artifact contracts, one canonical wire/security/process limit source, exact-snapshot-only RustSec input, deterministic all-row policy fixtures, and at most one untrusted current-native proof that 20 explicit starts converge, ping never spawns, nonce-bound stop cannot hit a replacement, and supervised cleanup never signals by PID; Plan 0009 must authenticate every native release row. | [status](0001-foundations-and-walking-skeleton/STATUS.md) |
| 0002 | Profiles and Authentication | ✅ Complete | 15/15 | Named profiles resolve reproducibly from one committed bounded source generation into immutable snapshots with independently constructible contract-bound principal/target/revision identities; genuine same-principal rotation preserves identity, canonical metascope or either route-specific verified-root drift changes revision, and Basic username or Cloud organization/client/technical-account drift changes target; platform-only identity-management trust cannot be widened by a selected author CA, while exact compact Cloud assertions, decoded response-section bounds, warned cleartext opt-in, direct author-only transport, deterministic platform policies, and conservative token leases fail closed without publisher/proxy traffic or secret/private-digest/source-reference disclosure. | [status](0002-profiles-and-authentication/STATUS.md) |
| 0003 | Command Contracts and Catalog | 🚧 In progress | 14/20 | A bounded typed command model with exact Semantic Versioning, deterministic operation contracts, continuation-aware discovery, finite load/replication/package production, side-effect-free exact configuration lookup with conservative reproducible metatype evidence/redaction, non-widening FileVault generation, order-safe component mutation, and one exact-version/limits/canonical-byte-bound wire-name registry with honest Draft 2020-12 decoded-shape schemas plus a separate language-neutral raw-byte/typed-order validator and external-agent conformance set for every requested Slingshot operation. | [status](0003-command-contracts-and-catalog/STATUS.md) |
| 0004 | Daemon Runtime and Local Protocol | 📋 Planned | 0/34 | One fixture-proved classification adopts the Plan 0001 roots and behavioral/process-entry leaves, gives every genuinely new Plan 0004 library leaf one compiling declaration, retains exactly the `slingshot` and `slingshot-development` binaries, reaches test-daemon composition only through the latter's internal subcommand, and makes every feature crate-reachable through a single dependency root with no unordered overlapping source footprint; concurrent clients then converge on one committed-generation-proved, opaque-target-and-revision-matched daemon using the upstream target hash directly and the distinct `VerifiedIdentityManagementTrustPolicyIdentity` and `VerifiedAuthorTrustPolicyIdentity` with their immutable route-typed root snapshots, retain control across operation-version or runtime-contract-digest incompatibility, never admit work through the unavailable product executor, and use the helper proof to demonstrate revision-bound target idempotency and recovery replay, conditional terminal evidence, exact logical and physical SQLite/storage backpressure and restart behavior, durable exact maintenance apply, complete bounded inline-or-target-qualified maintenance results readable without a synthetic operation, same-handle prefix-authenticated result transfer, genuine-rotation stability, and byte-preserving refusal after profile-contract, principal, canonical-metascope, or restart-visible trust-policy identity change. | [status](0004-daemon-runtime-and-local-protocol/STATUS.md) |
| 0005 | Author-Agent Job Transport | 📋 Planned | 0/23 | An author-only protected transport whose committed-generation-proved opaque target hash/revision and distinct `VerifiedIdentityManagementTrustPolicyIdentity`/`VerifiedAuthorTrustPolicyIdentity` bind transport/canonical-contract-partitioned logical operations with exact canonical wire/version/limits/schema/argument/result correlation and request-start-conservative live/restart recovery to verified terminal results, authoritative post-success result loss, or explicit fail-closed disposition; genuine same-principal rotation remains stable while profile-contract/principal/restart-visible trust-policy-identity drift refuses before network, additional author authorities cannot authenticate Identity Management Services, and universal cluster-capable continuation-key authority, complete retention capacity, byte-identical inline-or-operation-free-associated maintenance, filtered-stream resets, generation rotation, and fenced outbox recovery permit duplicate physical Sling records but never authorize a second logical command effect. | [status](0005-author-agent-job-transport/STATUS.md) |
| 0006 | Command-Line Experience | 📋 Planned | 0/20 | Every exact-version/limits-bound catalog operation is available from the command line with canonical phrase/asset/token inputs, reaches one profile/environment daemon, survives detachment or interrupt, exposes complete maintenance results through exact target-qualified operation-free identities, and renders every registered closed result or failure byte-stably without aliases, lost fields, or secret exposure. | [status](0006-command-line-experience/STATUS.md) |
| 0007 | Model Context Protocol Server | 📋 Planned | 0/16 | Modern clients receive the exact two-item discovery/error revision order, fsm-compatible legacy clients negotiate `2025-06-18` correctly, and both eras discover the same exact-version/limits-bound tools with five required and seven optional operation keys plus target-qualified operation and maintenance-result resources, retain each supplied or once-generated request operation identifier through reconnect/retry, retrieve lifecycle-valid canonical maintenance JSON, submit canonical phrase/asset/token requests, receive only command-specific closed results/failures byte-identical to CLI JSON, cannot overrun or alias requests, detach safely, and observe protocol-only stdout even when stderr is unavailable. | [status](0007-model-context-protocol-server/STATUS.md) |
| 0008 | FSM Workflow Integration | 📋 Planned | 0/12 | A pinned process-level FSM integration and repository-local gate in which exact daemon-runtime and author-transport contract digests reject provenance drift before execution, stable store namespaces make registry-command retries and daemon restarts converge on one durable logical operation and at most one fenced command effect despite bounded duplicate physical Sling records, keyless maintenance apply replay derives only from exact target/reviewed digest plus its durable receipt/association lifecycle and `MaintenanceResultMetadata`-then-read equality, compensation requires verified remote-failure evidence plus separate approval, and `ack.result.structured` retains exact `structured_result_artifact_access` or operation-free `maintenance_result_access` outcomes with `structured_result` reserved solely for the deterministic operation artifact slot; hosted adaptation remains owner-gated in Plan 0009. | [status](0008-fsm-workflow-integration/STATUS.md) |
| 0009 | Hardening and Release Contract | 📋 Planned | 0/24 | One rootless network-none acceptance command proves bounded/recoverable behavior, exact command-contract and operation-key-free maintenance-result association/metadata/read presentation and durable-receipt replay parity, and deterministic archives from exact inputs, with every abstract target backed by owner-mapped provider-authenticated evidence for its real safe provider API/enumerated fields plus deterministic same-adapter decision matrix, separate exact Plan 0002 Identity Management Services and Plan 0005 author transport contracts, Windows remote-client refusal, one same-run owner-reviewed RustSec pin bound to authenticated release-time evidence, and the single compatible-row FSM report. | [status](0009-hardening-and-release-contract/STATUS.md) |
