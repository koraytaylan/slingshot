---
id: inspect-open-service-gateway-initiative-configuration
title: "Inspect Open Service Gateway Initiative Configuration"
workstream: "0010"
kind: task
depends_on:
  - command-module-scaffold
gated: false
touches:
  - crates/slingshot-domain/src/command/inspect_open_service_gateway_initiative_configuration.rs
  - crates/slingshot-domain/tests/fixtures/commands/inspect_open_service_gateway_initiative_configuration/**
  - crates/slingshot-domain/tests/inspect_open_service_gateway_initiative_configuration.rs
status: planned
merged_as: ""
---
# Inspect Open Service Gateway Initiative Configuration

Represent side-effect-free inspection of one effective configuration by its exact persistent identifier and a dedicated bounded Open Service Gateway Initiative typed/redacted property model.

**Steps:**

1. Commit canonical command/result fixtures and independently authored agent scenarios before implementation. Cover filter literals and each `\|*|(|)` escape, maximum escaped filter, null/empty/single/multiple/over-limit `listConfigurations` returns, exception and pre/post-call duration boundaries, exact/mismatched/ambiguous `getPid()`, exactly one `getProperties()` after selection with null/deleted/exception/late returns, null/failed key Enumeration, empty/complete/framework-supplied/invalid/case-fold-duplicate/over-count key snapshots, redacted-key zero-value-access and visible-key exactly-one-get traces, ordinary null and valid/empty/failed factory PID, exact/absent/duplicate/failed bundle location, zero/one/multiple/failed MetaTypeService providers, ordinary/factory PID inventories, duplicate/contradictory designate inventories, exact null-locale object-class lookup, absent/duplicate/invalid attribute definitions, all exact metatype type mappings, and a call trace proving no `getProcessedProperties`, second key enumeration, value iterator, `getConfiguration`, `getFactoryConfiguration`, create, bind, raw-Designate parse, locale fallback, or cross-bundle lookup is invoked.
2. Implement OpenServiceGatewayInitiativePersistentIdentifier as nonempty, control-free valid Unicode through its manifest bound with exact spelling preservation. Construct only the exact `(service.pid=<escaped>)` filter by prefixing each backslash, asterisk, opening parenthesis, or closing parenthesis with one backslash, copy every other scalar, and reject a complete filter above its bound.
3. Specify the external lookup as one `ConfigurationAdmin.listConfigurations` call plus bounded `Configuration.getPid()` postchecks under injected monotonic pre/post boundaries. Null means absent. More than the exact match bound fails before a returned-object call. Any null/unequal/failed PID postcheck is `configuration_lookup_mismatch`; two exact PIDs are `configuration_lookup_ambiguous`; one exact PID alone is inspected. Immediately after that selection, call its `getProperties()` exactly once under the same deadline and before factory/bundle/metatype/value work. A late return loses to duration before its value/error; a within-time null, deleted/IllegalState, or failed call is fieldless `configuration_lookup_failed`. `getFactoryPid()`/`service.factoryPid` never substitutes for the requested instance PID. Define closed no-partial `configuration_lookup_failed|configuration_lookup_mismatch|configuration_lookup_ambiguous|configuration_lookup_budget_exceeded`, with the last budget exactly `lookup_duration|matching_configurations`.
4. Implement OpenServiceGatewayInitiativeConfigurationPropertyKey independently of JCR PropertyName and take a complete two-phase snapshot from that one Dictionary. Call `keys()` once; under post-call duration checks, loop `hasMoreElements()`, reserve the next property-count unit before `nextElement()`, then require a nonnull Java String that is nonempty, control-free valid Unicode through its bound and preserve original case. A null/failed Enumeration is lookup failed, equality/late duration wins first, the next count above the maximum is result-budget/property-count before retrieving a key, invalid key is malformed/invalid-property-key, and the second Unicode-15.1-default-fold-plus-NFC identity is malformed/duplicate-property-key. Include framework-supplied keys, emit accepted keys in ascending original UTF-8 bytes, and invoke no Dictionary value access during this phase.
5. Implement OpenServiceGatewayInitiativeConfigurationScalar with exact Java identities and lossless wire values: String/Boolean/Character; Byte/Short/Integer/Long as canonical base-ten strings in exact i8/i16/i32/i64 ranges; Float/Double as exactly eight/sixteen lowercase hexadecimal raw IEEE 754 bit digits. Preserve signed zero, infinities, NaN payloads, and one non-surrogate Unicode scalar for Character; perform no numeric widening or inference.
6. Implement OpenServiceGatewayInitiativeConfigurationValue as exact `scalar|primitive_array|scalar_array|collection` cardinality over one retained scalar type. Arrays and Collections may be empty and preserve retrieved order. Primitive arrays exclude String; wrapper/String arrays are scalar arrays; Collection preserves iterator order. An empty array takes its type from the runtime component class, while an empty Collection requires the exact unambiguous NonPassword metatype mapping below. Enforce manifest-owned string, sequence-item, canonical-sequence-byte, property-count, and complete-result bounds before admission.
7. Before any property-value access, implement the architecture's exact metatype applicability procedure over every validated snapshot key: obtain factory PID and bundle location once; use requested PID for ordinary configuration or the nonempty factory PID only for metatype inventory; require exactly one corresponding ordinary/factory inventory entry and no opposite entry; require exactly one successfully acquired MetaTypeService and one exact non-uninstalled bundle-location match; request the selected object-class definition with literal null locale and no fallback; and require one attribute definition matching the property key by the same Unicode-15.1-fold-plus-NFC identity. Never parse raw Designate XML or guess another PID, locale, provider, or bundle. Map exact PASSWORD to Password and exact STRING/BOOLEAN/CHARACTER/BYTE/SHORT/INTEGER/LONG/FLOAT/DOUBLE to NonPassword with their nine scalar identities. Every absent, duplicate, conflicting, unsupported, unknown, disappearing, or failed provider/bundle/inventory/object-class/attribute/type observation, including BIGINTEGER/BIGDECIMAL, becomes Unavailable before the value is touched. Then apply the sensitive-name policy: ASCII-lowercase, discard non-ASCII-alphanumeric separators without Unicode folding, match exactly `password|passwd|secret|token|credential|privatekey|apikey|accesskey`, and treat empty normalized spelling as sensitive. Password, Unavailable, or a sensitive name emits only content-free Redacted and performs no Dictionary get. Visible is legal only for exact NonPassword evidence under an innocuous original key and calls `Dictionary.get(original_key)` exactly once.
8. Define closed no-partial conversion failures and exact first-fault order. Dictionary access failure is fieldless lookup failed and null is malformed/null-value. Unsupported reasons are exactly `non_primary_type|non_unicode_value`. Malformed reasons are exactly `invalid_property_key|duplicate_property_key|null_value|mixed_types|nested_container|carrier_type_mismatch|empty_collection_type_unavailable`. Value-budget literals are exactly `scalar_string_bytes|sequence_items|sequence_canonical_bytes`; result-budget literals are exactly `property_count|serialized_result_bytes`. Duration wins before a lookup/snapshot return; count admission wins before the next key; key shape then duplicate identity precede metatype/name classification; redaction suppresses every value fault; eligible values apply access, null/container/carrier, primary/Unicode, value-budget, then complete-result-budget checks. Exact non-time maxima succeed and the next unit fails. None exposes a property key/value or partial map. Unknown, mixed, null, nested, non-primary, non-Unicode, carrier/type-conflicting, and over-bound values hidden by mandatory redaction stay redacted rather than being examined.
9. Implement the result with exact requested persistent identifier, presence state, and an ordered property map that is empty when absent. Supply request-context validation requiring that identifier to equal the originating command before persistence or forwarding. Do not claim detection of arbitrary secret bytes mislabeled NonPassword under an innocuous name by a compromised trusted agent.

**Tests:**

- Ordinary and factory-instance persistent identifiers round-trip exactly; empty, control-containing, non-Unicode, and over-bound identifiers fail before filter construction.
- Exact filter-byte fixtures pin ordinary Unicode and every isolated/combined/repeated `\|*|(|)` escape, including a factory instance and maximum expansion. The configuration lookup invokes only `listConfigurations`/`getPid()` followed by the one selected `getProperties()` and one complete keys-only snapshot; the separate metatype trace uses only the exact factory-PID, bundle-location, provider, inventory, null-locale object-class, and attribute-definition calls above and never a configuration-creating/binding API.
- Null/empty returns are absent; one exact PID is present; a mismatched postcheck fails before ambiguity, two exact matches are ambiguous, over-match and duration bounds fail closed, and late values/errors are discarded. Factory PID never replaces instance PID.
- Every scalar type has scalar, primitive-array where legal, scalar-array, and Collection vectors. Empty/nonempty sequences preserve exact carrier, scalar identity, element order, integer width/range, and IEEE bits; no JCR PropertyValue mapping or widened JSON number is accepted.
- Null/deleted/failed/late property-Dictionary acquisition, null/failed Enumeration, exact/over property count, invalid/case-fold-equivalent property keys, unpaired surrogates, non-primary objects, null/mixed/nested values, carrier disagreement, and untyped empty Collections map only to their exact precedence/category/reason. Original accepted key case is preserved and keys serialize in ascending UTF-8 bytes.
- Ordinary/factory, provider, bundle-location, designate inventory, null-locale, object-class, attribute-definition, and exact nine-type/PASSWORD fixtures independently determine evidence for the complete key snapshot; every absent/ambiguous/failed or unsupported type becomes Unavailable. PASSWORD, Unavailable, every exact sensitive-name literal/case/separator spelling, and empty-normalized names redact with zero Dictionary gets; each visible key has exactly one get. Fixtures prove an otherwise malformed/unsupported sensitive value is never exposed or classified by value. Illegal evidence/visibility pairs fail.
- String, sequence, property-map, and complete-result values immediately below and at every manifest bound succeed; the next charge produces only its exact value/result budget failure with no key, partial map, filter, or configuration data. Checked arithmetic never wraps.
- Present and absent results enforce their exact map shapes; unknown/surplus fields and noncanonical type/cardinality/hex/integer/key order are rejected.
- The catalog/schema proof computes the maximum legal single observation plus complete envelope, rejects an outer maximum one byte too small, and exposes all and only the lookup/value/result failures and literals for exact version `1.0.0`.
- A structurally valid result carrying another command's persistent identifier is rejected before any property observation can enter persistence.

- **Done when:** cargo test -p slingshot-domain --test inspect_open_service_gateway_initiative_configuration validates exact side-effect-free PID filter/lookup/property-Dictionary snapshot traces, ordinary/factory metatype applicability with conservative unavailable fallback, all nine scalar identities and four cardinalities, empty sequences/order, key uniqueness/case/order, zero-read redaction and exactly-once visible access, closed lookup/unsupported/malformed/budget precedence, result correlation, and every canonical boundary fixture without claiming Rust executes Configuration Admin or MetaType Service.
