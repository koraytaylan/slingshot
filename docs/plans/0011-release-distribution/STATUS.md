# Plan 0011 — Release Distribution — 📋 Planned

The roll-up row in [../STATUS.md](../STATUS.md) must stay in sync with this file. Task-level truth lives in [tasks/](tasks/) frontmatter; Makina's integration coordinator updates both.

- **Status:** 📋 Planned.
- **Goal:** make a release something a stranger can obtain and authenticate, and give every claim on that path the assertion this repository requires of a claim.
- **Root cause:** Plan 0009 ended at evidence by design, and the step from evidence to a download was then built while discovering that none of it had ever run. It works and nothing proves it, which is the state this repository refuses everywhere else.
- **Approach:** assert the refusals the path already makes, hold the hosted gate's tool provisioning to the manifest it reads, and publish the download and verification instructions against the verifier's real interface.
- **Progress:** 0/6 tasks done; 0 blocked; 0 dropped.
- **Integration:** `planned`; run `develop`; base `main` @ `11c2e531d8a07a885321e7a09b1dccc623a733cf`; mode `sequential`.
- **Exceptions:** none recorded yet.
